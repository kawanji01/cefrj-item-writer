# CEFR-J作問エージェント — Codex配線

このプロジェクトは、教師との日本語対話からCEFR-J準拠の問題セットを作成し、決定的検査、独立レビュー、JSON正本、自己完結HTMLまでを生成するエージェントです。

1. 作問を開始するときは、最初に `agent/author-core.md` を読み、その指示に完全に従ってください。
2. レビューは必ず次の `codex exec` 起動手順で新しい独立サブプロセスとして実行し、自分でレビュー判定をしてはなりません。起動プロンプトは `docs/cross-agent-compatibility.md` の3要素だけを含むテンプレートで作成してください。この固定コマンドを実行するホスト側シェル呼出しだけは親サンドボックス外での実行を申請し、ユーザーの承認を得てください。別コマンドへ承認を広げたり、子プロセスの `--sandbox read-only` を緩和したりしてはいけません。

   ```text
   codex exec \
     --ephemeral \
     --cd "<リポジトリルートの絶対パス>" \
     --sandbox read-only \
     --skip-git-repo-check \
     --output-last-message "<最終メッセージ出力ファイルの絶対パス>" \
     - < "<起動プロンプトファイルのパス>"
   ```

3. 決定的処理は必ず `scripts/` のCLIで行い、CLIの検査違反、判定、エラーコードをLLM判断で上書きしてはなりません。
4. 共通挙動は `agent/` と `scripts/` にだけ置かれています。このファイルには挙動規則を追記せず、起動・権限・参照の配線だけを置いてください。
