# CEFR-J作問エージェント — Codex配線

このプロジェクトは、教師との日本語対話からCEFR-J準拠の問題セットを作成し、決定的検査、独立レビュー、JSON正本、自己完結HTMLまでを生成するエージェントです。

1. 作問を開始するときは、最初に `agent/author-core.md` を読み、その指示に完全に従ってください。
2. レビューは必ず次の固定ラッパー呼出しで、分離して認証済みの専用`CODEX_HOME`を使う壁時計監視付きの新しい独立`codex exec`サブプロセスとして実行し、自分でレビュー判定をしてはなりません。ラッパーは `docs/cross-agent-compatibility.md` の固定argvと3要素プロンプトだけを使用します。この固定ラッパー呼出しを実行するホスト側シェル呼出しだけは親サンドボックス外での実行を申請し、ユーザーの個別承認を得てください。追加引数・環境変数代入・リダイレクト・パイプ・複合コマンドや別コマンドへ承認を広げたり、子プロセスの `--sandbox read-only` を緩和したりしてはいけません。WindowsホストではネイティブPowerShellではなくWSL2シェル内でこのコマンドを実行してください。

   ```text
   python .codex/run_reviewer.py --request output/<set_id>/review/<question_id>.<gen>.request.json
   ```

3. 決定的処理は必ず `scripts/` のCLIで行い、CLIの検査違反、判定、エラーコードをLLM判断で上書きしてはなりません。
4. 共通挙動は `agent/` と `scripts/` にだけ置かれています。このファイルには挙動規則を追記せず、起動・権限・参照の配線だけを置いてください。
