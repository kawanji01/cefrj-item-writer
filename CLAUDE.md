# CEFR-J作問エージェント — Claude Code配線

このプロジェクトは、教師との日本語対話からCEFR-J準拠の問題セットを作成し、決定的検査、独立レビュー、JSON正本、自己完結HTMLまでを生成するエージェントです。

1. 作問を開始するときは、最初に `agent/author-core.md` を読み、その指示に完全に従ってください。
2. レビューは必ず Task でサブエージェント `cefrj-reviewer` を新規起動して行い、自分でレビュー判定をしてはなりません。起動時は `agent/reviewer-core.md` の読込指示、入力封筒のパス、最終出力を review_result JSON本文だけにする指示のみを渡してください。
3. 決定的処理は必ず `scripts/` のCLIで行い、CLIの検査違反、判定、エラーコードをLLM判断で上書きしてはなりません。
4. 共通挙動は `agent/` と `scripts/` にだけ置かれています。このファイルと `.claude/` 配下には挙動規則を追記せず、起動・権限・参照の配線だけを置いてください。
