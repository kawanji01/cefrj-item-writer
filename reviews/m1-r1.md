# M1 コードレビュー R1

- **対象コミット**: `63052b5e3080be425f1674a152c9a906fcf127b7`
- **対象コミット件名**: `M1: 正規化とdoctorを実装`
- **仕様の正**: `docs/cefrj-validation-spec.md`、`docs/architecture.md`、`schemas/`
- **M1完了条件**: `IMPLEMENTATION_PLAN.md` M1 DoD
- **レビュー結果**: P1 2件、P2 3件。M1 DoDは厳密には5/6であり、DoD 5のエラーコード目録準拠が未充足。

## R1-01

- **重大度**: P1
- **該当ファイルと行**:
  - `scripts/doctor.py:204-242`
  - `scripts/doctor.py:261-267`
  - `CHANGELOG.md:35-37`
- **指摘内容**: 正規化データ欠落時に、エラーコード目録の発生条件と異なるコードが併記される。`meta.json` が存在しない場合、D07はチェックサムの期待値を取得できないことを `E-DATA-02` として報告する。しかし `E-DATA-02` は実ファイルのSHA-256とmeta記録値が不一致の場合に限定され、meta自体の欠落は `E-DATA-03` である。またD09は `validate_normalized_set()` が送出した `E-DATA-03` を包括的な `except Exception` で捕捉し、常に `E-DATA-04` へ置き換える。その結果、正規化データ欠落の模擬環境で `E-DATA-02`、`E-DATA-03`、`E-DATA-04` が同時に出力される。`CHANGELOG.md` はこの結果をpassとしているが、目録上、欠落に対して正しいのは `E-DATA-03` であるため、M1 DoD 5の「目録どおり」を満たさない。
- **根拠規則ID**:
  - `PLN-02`（DoD全項目の実行・確認）
  - `PLN-08`（全CLIのエラーコード目録準拠）
  - `IMPLEMENTATION_PLAN.md` M1 DoD 5
  - `CLI-10`、`CLI-11`
  - `ERR-01`、`ERR-03`
  - `E-DATA-02`、`E-DATA-03`、`E-DATA-04`
  - `CI-NRM-06`、`CI-CLI-03`
- **修正案**: doctorが後続診断を継続する構造は維持しつつ、前提ファイル欠落時は根本原因のコードを保持する。D07は原本欠落なら `E-DATA-01`、meta欠落なら `E-DATA-03` を返し、実際に期待値と実測値を比較できた場合だけ `E-DATA-02` を返す。D09は少なくとも `CliFailure` を別途捕捉して `error_code` を保持し、予期しない例外だけを内部エラーとして扱う。診断項目ごとに固定コードを返す意図があるなら、実装変更前にPLN-05でCLI-10とエラーコード目録の矛盾を解消する。

## R1-02

- **重大度**: P1
- **該当ファイルと行**:
  - `scripts/build_normalized.py:1284-1299`
  - `scripts/build_normalized.py:1416-1426`
  - `scripts/build_normalized.py:1442-1450`
- **指摘内容**: 出力ディレクトリの作成、一時ファイル作成、書込み、`fsync`、`os.replace` のいずれかで `OSError` が発生しても、定義済み `E-ENV-05` へ変換されない。汎用例外処理へ到達して終了コード2とスタックトレースを返す。実際に `--out-dir /dev/null/cefrj-normalized` を指定すると `NotADirectoryError`、終了コード2となった。出力先の作成・書込み失敗は目録で定義済みの環境エラーであり、内部バグではない。
- **根拠規則ID**:
  - `PLN-08`
  - `CLI-02`、`CLI-05`、`CLI-08`
  - `ERR-01`、`ERR-02`、`ERR-03`
  - `E-ENV-05`
- **修正案**: 出力先の準備と原子的書込みで発生する `OSError` を捕捉し、対象パスとOSエラー内容を `detail` および日本語 `message` に含む `CliFailure("E-ENV-05", ...)` へ変換する。`build_normalized.py` のremedy目録にも `E-ENV-05` を追加し、終了コード1でCLI-05形式のエラーJSONを返す。複数ファイルの途中まで書き込んだ場合に混在状態が残る可能性もあるため、書込み開始前の出力先検査または3ファイル単位の安全な切替も併せて検討する。

## R1-03

- **重大度**: P2
- **該当ファイルと行**:
  - `scripts/build_normalized.py:27-31`
  - `scripts/build_normalized.py:1118-1153`
  - `scripts/build_normalized.py:1368-1380`
- **指摘内容**: `--accept-source-change` は既存metaと原本のチェックサム差を無条件に受理する一方、旧metaからの `data_version` 更新を検査しない。生成される `data_version`、`pipeline_version`、`version_label` は初版のハードコード値であるため、内容が異なる承認済み原本を `wl1.6+gp20200220+norm1.0.0` のまま書き込める。これにより、異なる正規化データが同一 `data_version` を名乗り、原本更新手順の版追跡を破壊し得る。
- **根拠規則ID**:
  - `CLI-14`
  - `VER-04`、`VER-05`
  - `OPS-01`
  - `NRM-02`、`NRM-29`
- **修正案**: 承認済み原本更新時に、旧metaと新規生成metaの `data_version` が異なること、および `data_version` 内のWordlist版・Grammar Profile版・norm版と `version_label`／`pipeline_version` が整合することを検査する。現在の `sources.json` には版情報がなく、版更新忘れに割り当てる定義済みエラーコードも明確でないため、具体的な入力元と停止コードはPLN-05で先に決定し、その決定に基づいてガードを実装する。

## R1-04

- **重大度**: P2
- **該当ファイルと行**:
  - `scripts/build_normalized.py:116-124`
  - `scripts/build_normalized.py:575-593`
  - `scripts/build_normalized.py:776-801`
- **指摘内容**: `ITEM_HEADERS_EXACT` が `set` で定義され、`ensure_exact_headers()` がその反復中に最初の欠落・重複ヘッダーで停止する。Pythonのハッシュシードによってsetの反復順が変わるため、複数ヘッダーが欠落した同一xlsxでも、実行ごとに `E-DATA-06` のmessageとdetailで最初に報告される列名が変わり得る。また、目録が要求する不一致列の全件列挙にもならない。`PYTHONHASHSEED=1..4` で `ITEM_HEADERS_EXACT` の反復順が異なることを確認した。
- **根拠規則ID**:
  - `PLN-07`
  - `CLI-04`、`CLI-12`
  - `E-DATA-06`
  - `NRM-02`
- **修正案**: 必須ヘッダー目録を順序固定のtupleとして定義するか、検査前に明示的にソートする。検査関数は最初の1件で停止せず、欠落・重複ヘッダーをすべて収集し、固定順で1件の `E-DATA-06` に格納する。これによりエラーJSONの決定性と全件列挙要件を同時に満たす。

## R1-05

- **重大度**: P2
- **該当ファイルと行**:
  - `scripts/build_normalized.py:483-497`
  - `scripts/build_normalized.py:688-725`
- **指摘内容**: ALLシートの `headword` を `normalized_text()` に通した値を、そのまま `headword_joined` に保存している。`normalized_text()` はNFC正規化と前後空白のtrimを行うため、`headword_joined` は仕様が要求する「ALL行原表記そのまま」にならない。現行原本では差が顕在化していないが、前後空白または非NFC文字を含む更新原本では正規化成果物が変換規則から逸脱する。
- **根拠規則ID**:
  - `NRM-10` 1、3
  - `NRM-12`
- **修正案**: ALLセルの文字列を原表記保存用と照合用に分離する。`headword_joined` にはセルの文字列を変更せず保存し、variant分割・空判定・ALL_sep対応付けにのみtrimおよび必要なNFC正規化を適用する。原表記が文字列でない場合や空の場合は、現在と同様に `E-DATA-06` で停止する。

## 確認できた正常系

- `.venv/bin/python scripts/doctor.py` は現環境で12件すべてpass、終了コード0。
- コミット済み `lexicon.json`、`grammar.json`、`meta.json` のSHA-256は `CHANGELOG.md` 記載値と一致。
- `.venv/bin/python scripts/build_normalized.py --diff` は全区分で差分ゼロ、`written=[]`、終了コード0で、実行前後の3ファイルは不変。
- 正常入力から生成される3ファイルについて、上記以外の実行時非決定要因は確認できなかった。
