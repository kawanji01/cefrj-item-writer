# M1 コードレビュー R12

- **対象差分**: `36f0ceb7afe8cf57e9d4b2217ad0f20db22c5a99`（設計成果物の初回コミット）から `63052b5e3080be425f1674a152c9a906fcf127b7` および現在の作業ツリーまで
- **レビュー日**: 2026-08-16
- **仕様の正**: `docs/`、`schemas/`
- **前回レビュー**: `reviews/m1-r11.md`
- **対応記録**: `reviews/m1-r11-resolutions.md`
- **前回指摘の解消判定サマリー**: R11-01・R11-02はいずれも解消を確認した。
- **レビュー結果**: 独立検査でminor 1件を検出した。未解消指摘の合計はblocker 0件、major 0件、minor 1件。

## R11指摘の解消確認

### R11-01

- **判定**: 解消
- **差分確認**: `validate_meta_document()`が`spacy_model.name`と`spacy_model.version`を固定要求値へ照合するようになった。正常な正規化セットの`meta.json`でモデル版だけを`0.0.0`へ変更した一時環境では、doctorは12項目を完走し、D03はpass、D07・D09は要求版3.8.0と実測版0.0.0をmessageに含む`E-DATA-04`、終了コード1、stderr空となった。

### R11-02

- **判定**: 解消
- **差分確認**: `safe_data_version()`により、文書全体のスキーマ合否と独立して型安全な`data_version`が現在値比較と3ファイル相互比較へ参加する。lexiconを`entries=[]`かつ旧`data_version`とした複合破損環境では、D09の同一`E-DATA-04` messageに`/entries`のスキーマ違反、3ファイルの内部不一致、`lexicon.data_version`の期待値・実測値が全て含まれた。

## 独立検査の新規指摘

## R12-01 — minor — `scripts/build_normalized.py:1474-1485`, `scripts/build_normalized.py:1611-1620`, `scripts/build_normalized.py:2816-2820`

1. **指摘内容**: EFLコーパス総語数は`normalized_grammar.schema.json`の`efl_corpus.words`で0以上の整数に制約されているが、原本セルの先行検査は整数性・有限性だけを確認し、負数を値域外セルとして収集しない。`EFL SUMMARY (FULL)`のG1とH1をそれぞれ`-1`、`-2`にした制御済みworkbookでは、`build_grammar()`が負数を含む文書を生成し、後段のスキーマ検証で初めて`E-DATA-06`となった。そのエラーのdetailには`/efl_corpus/words/A1`と`/efl_corpus/words/A2`がある一方、CLI-05のmessageは「正規化結果がnormalized_grammarスキーマに適合しません」のみで、値域外セル`G1`・`H1`を列挙しない。

2. **影響**: 不正原本と成果物の書き込みは拒否されるため正本の安全性は維持されるが、E-DATA-06が要求する値域外セルの全件列挙をmessageで満たさず、教師はJSONポインタから原本セルを逆算しなければならない。

3. **根拠となる設計文書の規則ID**: `NRM-04`、`NRM-27`、`CLI-05`、`CLI-12`、`E-DATA-06`、`PLN-08`。

4. **具体的な修正案**: `corpus_fields`の総語数仕様へ`minimum: 0`相当を追加し、`grammar_value_reason()`で負数を値域外として他のGrammarセル問題と同時収集する。複数レベルの負数を投入し、`EFL SUMMARY (FULL)!G1`・`H1`が固定列順で単一の`E-DATA-06` message/detailへ全件含まれる回帰検査を追加する。

## 確認できた正常系・DoD再確認

- 独立した2つの一時出力先への通常ビルドは各々終了コード0・stderr空で、3成果物が相互に、かつ作業ツリーの正規化正本とバイト一致した。SHA-256はlexicon=`11ac8d1d6b42e5fbd37baa1005b55d7904f42f2753e0720018bbc9edb977c3c7`、grammar=`6a435941ff1105a78b76fae0c141288a783d31148449302621d3b42a8ebbff62`、meta=`fd8c51b2f664f5eaef04c73936927fcd9cb1eb1c2bae56b65df9ad53ec0f0fd4`。
- lexicon・grammarのスキーマ適合、entries=7,988、A1=1,200 / A2=1,443 / B1=2,486 / B2=2,859、`(headword,pos)`ユニーク=7,988、ALL行=7,801、groups=179、grammar=501、親263・枝番238、target_eligible=256、単一値152・範囲値104、未付与親16件を確認した。
- 現作業ツリーのdoctorは12 pass / 0 fail・終了コード0・stderr空だった。`build_normalized.py --diff`はlexicon / grammarのadded / removed / level_changedが全て0件、`written=[]`、終了コード0・stderr空だった。
- `scripts/setup.py`・`scripts/build_normalized.py`・`scripts/doctor.py`の構文コンパイルと`git diff --check`はpassした。
- M1 DoDの通常ケースは6/6を再確認した。R12-01は診断内容のminorであり、正規化成果物の生成・拒否ゲート・決定性を損なわない。

## 要約

- blocker: 0件
- major: 0件
- minor: 1件
- **判定**: 収束。コミット可。
