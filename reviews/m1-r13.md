# M1 コードレビュー R13

- **対象差分**: `36f0ceb7afe8cf57e9d4b2217ad0f20db22c5a99`（設計成果物の初回コミット）から `63052b5e3080be425f1674a152c9a906fcf127b7` および現在の作業ツリーまで
- **レビュー日**: 2026-08-16
- **仕様の正**: `docs/`、`schemas/`
- **前回レビュー**: `reviews/m1-r12.md`
- **対応記録**: `reviews/m1-r12-resolutions.md`
- **前回指摘の解消判定サマリー**: R12-01は解消を確認した。
- **レビュー結果**: 独立検査でmajor 1件を検出した。未解消指摘の合計はblocker 0件、major 1件、minor 0件。

## R12指摘の解消確認

### R12-01

- **判定**: 解消
- **差分確認**: EFLコーパス総語数の検査仕様に`minimum: 0`が追加され、`grammar_value_reason()`が負数を値域外として扱う。正常原本から読み込んだ行データのG1・H1相当を`-1`・`-2`へ差し替えて先行検査を実行すると、`EFL SUMMARY (FULL)!G1`、`EFL SUMMARY (FULL)!H1`の順で両方の問題が収集され、各セル・フィールド・最小値・実測値がdetailへ格納された。

## 独立検査の新規指摘

## R13-01 — major — `scripts/build_normalized.py:693-768`, `scripts/build_normalized.py:2703-2725`

1. **指摘内容**: 正規化3ファイル全欠落時のGit履歴検査は、`--out-dir`で指定された対象の所属リポジトリではなく、常に`Path.cwd()`から得た現在のリポジトリの`HEAD`だけを照会する。別のGitリポジトリに`data/normalized/meta.json`を含む3成果物をコミットしてから作業ツリー上の3ファイルを削除し、その絶対パスを`--out-dir`へ指定すると、対象側の`HEAD`にはmetaが存在するにもかかわらず`path_exists_in_git_head()`は`False`を返した。さらにWordlist xlsxのZIPコメントだけを変えてSHA-256を`e41033a1...90e1`から`6c385be1...b2d7`へ変更し、`version_label=1.6`を据え置いた原本で通常ビルドすると、終了コード0・stderr空で3ファイルを再生成し、`data_version`を据え置いたまま変更後SHA-256をmetaへ記録した。対象リポジトリをカレントディレクトリにして同じ履歴を照会すれば停止対象であるため、`--out-dir`の配置だけでM1D-15の履歴境界と原本変更ガードを迂回できる。

2. **根拠となる設計文書の規則ID**: `FR-35`、`CLI-14`、`E-DATA-02`、`E-DATA-04`、`M1D-15`、`M1D-16`、`PLN-08`。

3. **具体的な修正案**: 対象metaの字句パスと解決後パスについて、それぞれを包含する最寄りのGitリポジトリを`git -C <対象親> rev-parse --show-toplevel`等で特定し、その`HEAD`にも対象パスが存在しないことを確認してから初回ビルドを許可する。対象が現在のリポジトリ外またはネストした別リポジトリ内にある場合も、現在のリポジトリの照会結果だけで不在と確定しない。既定出力先、同一リポジトリ内の新規カスタム出力先、別リポジトリの追跡済み全削除、Git管理外の新規一時出力先、symlink経由の各条件を回帰検査する。

## 確認できた正常系・DoD再確認

- 独立した2つの一時出力先への通常ビルドは各々終了コード0・stderr空で、3成果物が相互に、かつ作業ツリーの正規化正本とバイト一致した。SHA-256はlexicon=`11ac8d1d6b42e5fbd37baa1005b55d7904f42f2753e0720018bbc9edb977c3c7`、grammar=`6a435941ff1105a78b76fae0c141288a783d31148449302621d3b42a8ebbff62`、meta=`fd8c51b2f664f5eaef04c73936927fcd9cb1eb1c2bae56b65df9ad53ec0f0fd4`。
- lexicon・grammarは各スキーマに適合した。lexiconはentries=7,988、A1=1,200 / A2=1,443 / B1=2,486 / B2=2,859、`(headword,pos)`ユニーク=7,988、groups=179。grammarはentries=501、親263・枝番238、target_eligible=256、直接付与の単一値152・範囲値104で、meta.countsも実件数と一致した。
- 現作業ツリーのdoctorは12 pass / 0 fail・終了コード0・stderr空だった。`build_normalized.py --diff`はlexicon / grammarのadded / removed / level_changedが全て0件、`written=[]`、終了コード0・stderr空だった。
- `.venv/bin/python -m py_compile scripts/setup.py scripts/build_normalized.py scripts/doctor.py`と`git diff --check 36f0ceb`はpassした。
- M1 DoDの通常ケースは6/6を再確認したが、R13-01で追跡済み正規化セットの全削除と原本変更拒否を`--out-dir`の別リポジトリ指定により迂回できるため、レビューは未収束と判定する。

## 要約

- blocker: 0件
- major: 1件
- minor: 0件
- **判定**: `/m-fix` で修正が必要。
