# M1 コードレビュー R9

- **対象差分**: `36f0ceb7afe8cf57e9d4b2217ad0f20db22c5a99`（設計成果物の初回コミット）から `63052b5e3080be425f1674a152c9a906fcf127b7` および現在の作業ツリーまで
- **レビュー日**: 2026-08-16
- **仕様の正**: `docs/`、`schemas/`
- **前回レビュー**: `reviews/m1-r8.md`
- **対応記録**: `reviews/m1-r8-resolutions.md`
- **前回指摘の解消判定サマリー**: R8-01〜R8-03は、前回が示した各再現条件について解消を確認した。R8-01のcasing違いは閉じているが、同じGit履歴境界をシンボリックリンクで迂回できる別条件をR9-01へ記録する。
- **レビュー結果**: 独立検査でmajor 3件、minor 2件を検出した。未解消指摘の合計はblocker 0件、major 3件、minor 2件。

## R8指摘の解消確認

### R8-01

- **判定**: 解消（前回のcasing違い条件。別境界はR9-01）
- **差分確認**: case-insensitiveファイルシステムでは、直接の`git ls-tree`が空でもHEADの全追跡パスを取得し、`casefold()`一致する対象metaを追跡済みと判定する実装になった。大小文字を変えた`DATA/NORMALIZED/meta.json`と、HEAD上の`data/normalized/meta.json`を用いたcase-insensitive条件の模擬実行で`path_exists_in_git_head()`が`True`を返すことを確認した。

### R8-02

- **判定**: 解消
- **差分確認**: ITEM LIST・教員版・EFLの全IDを先行走査し、各シート内重複と教員版／EFLからITEM LISTへ結合できない行を、シート・行・IDの固定順で単一の`E-DATA-06`へ格納する。3シートすべてに重複・未結合IDを含む合成入力で、検出対象9件が全て固定順で返ることを確認した。

### R8-03

- **判定**: 解消
- **差分確認**: `git_head_query_failure()`のmessageはJSON配列表現の実行コマンドを含み、起動時`OSError`ではOSエラー、非0終了では終了コードとstderr要約を含む。`git rev-parse --verify HEAD^{commit}`・終了コード128・stderrを与えた実行で、4要素すべてがmessageへ出ることを確認した。

## 独立検査の新規指摘

## R9-01 — major — `scripts/build_normalized.py:650-717`, `scripts/build_normalized.py:720-750`

1. **指摘内容**: 正規化3ファイル全欠落時のGit履歴検査は、対象metaを`Path.resolve()`してからGitルート相対パスへ変換するため、作業ツリー上のシンボリックリンクを追跡してしまう。HEADに通常ファイルの`data/normalized/meta.json`をコミットした一時リポジトリで3ファイルを削除し、同じmetaパスをリポジトリ外の欠落ファイルを指すbroken symlinkへ置換すると、`Path.exists()`は偽になり、`path_exists_in_git_head()`もリンク先のリポジトリ外パスを検査して偽を返した。続けてWordlistのZIPコメントだけを変え、`version_label=1.6`を据え置いて通常ビルドしたところ、終了コード0で3ファイルが再生成され、symlinkは新しいmetaへ置換された。Git自身はHEAD上の対象metaを返すため、作業ツリーのsymlinkだけでM1D-15の履歴境界と原本変更ガードを迂回できる。

2. **根拠となる設計文書の規則ID**: `CLI-14`、`E-DATA-02`、`E-DATA-04`、`FR-35`、`GUA-01`、`M1D-15`、`M1D-16`、`PLN-08`。

3. **具体的な修正案**: Gitへ渡す追跡対象は作業ツリーのsymlinkを解決せず、GitルートとCLIで指定された出力先から字句的に正規化した相対パスとして構築する。既存のbroken symlinkや通常ファイル以外が対象metaパスにある場合は、初回ビルドへ倒さず明示的な`E-DATA-04`で停止する。通常削除・casing違い・meta自身のbroken symlink・出力ディレクトリのsymlink・履歴にない空出力先を回帰検査する。

## R9-02 — major — `scripts/build_normalized.py:239-247`, `scripts/build_normalized.py:2104-2109`, `scripts/doctor.py:264-279`

1. **指摘内容**: JSON読取り境界がPythonの寛容な`json.loads()`を既定設定のまま使用し、非標準トークン`NaN` / `Infinity`を拒否しない。また、巨大整数に対して`json.loads()`が送出する通常の`ValueError`は捕捉対象外である。正本grammarのEFL相対頻度1値を`NaN`へ置換した一時環境では、そのファイルが標準JSONではないにもかかわらず、doctorはD09をpassとして12/12・終了コード0を返した。別途、grammarの整数1値を5,000桁にするとD09から`ValueError`が漏れ、doctorは12項目レポートを出さず、スタックトレース・終了コード2で中断した。したがって、同じJSON境界が破損入力を見逃す場合と、定義済み`E-DATA-04`へ変換できない場合の両方がある。

2. **根拠となる設計文書の規則ID**: `NRM-03`、`NRM-30`、`CLI-04`、`CLI-10`、`CLI-11`、`E-DATA-04`、`ERR-03`、`PLN-08`。

3. **具体的な修正案**: 共通JSONローダーを標準JSONに限定し、`parse_constant`で`NaN` / `Infinity` / `-Infinity`を拒否する。整数変換上限などパース中の`ValueError`もJSONパース不能として対象の`E-DATA-*`を持つ`CliFailure`へ変換する。normalized・meta・sources・configの各読取りで非標準定数と巨大数を投入し、doctorが全12項目を完走した上で対象診断を定義済みコードのfailにすることを確認する。

## R9-03 — major — `scripts/build_normalized.py:1253-1262`, `scripts/build_normalized.py:1321-1446`, `scripts/build_normalized.py:2219-2250`, `scripts/build_normalized.py:2471-2480`

1. **指摘内容**: Grammar Profile数値セルの検査は型と整数性だけを確認し、有限値かを確認しない。EFL相対頻度セルのxlsxキャッシュ値を`1e309`へ変えた一時原本はopenpyxlで`inf`として読み込まれ、`numeric_value()`とJSON Schema検証を通過した。その後、出力ループは先に`lexicon.json`を書き込み、`grammar.json`の`canonical_json_text(..., allow_nan=False)`で`ValueError`となる。この例ではCLIがスタックトレース・終了コード2を返し、出力先には新しい`lexicon.json`だけが残った。原本の値域外セルは`E-DATA-06`で停止すべきであり、予期できる入力不正が内部バグ扱いになる上、失敗後に部分更新セットを残す。

2. **根拠となる設計文書の規則ID**: `NRM-03`、`NRM-26`、`CLI-04`、`CLI-12`、`E-DATA-06`、`ERR-03`、`PLN-08`。

3. **具体的な修正案**: `numeric_value()`で全floatに`math.isfinite()`を適用し、非有限値をセル位置付きの値域外問題として他の不正セルとともに`E-DATA-06`へ収集する。さらに、3出力の正準JSONテキストを最初のファイル置換前に全て直列化して、直列化可能性の問題で部分更新が起きないようにする。EFL相対頻度・range・corpus母数へ正負の非有限値を投入し、全件が固定順で終了コード1になることを確認する。

## R9-04 — minor — `scripts/build_normalized.py:1265-1274`

1. **指摘内容**: `parse_level()`は範囲の大小検査を`minimum != maximum and rank(minimum) >= rank(maximum)`とするため、同値範囲`A1.1-A1.1`を受理する。LVL-05はハイフンを持つ範囲値`X-Y`について常に`rank(X) < rank(Y)`を要求する。教員版の単一値1セルを`A1.1-A1.1`へ変更した一時原本は終了コード0でビルドされ、`level_raw="A1.1-A1.1"`、`min=max="A1.1"`の直接付与項目が出力された。入力拒否が必要な構文を単一値相当として数えるため、レベル原文の妥当性検査が仕様より緩い。

2. **根拠となる設計文書の規則ID**: `LVL-05`、`NRM-19`、`NRM-23`、`CLI-12`、`E-DATA-06`。

3. **具体的な修正案**: `match.group(2) is not None`で範囲構文かを判定し、範囲なら同値を含む`rank(minimum) >= rank(maximum)`を拒否する。単一値、正しい昇順範囲、同値範囲、逆順範囲をそれぞれ回帰確認する。

## R9-05 — minor — `scripts/build_normalized.py:1131-1183`, `scripts/build_normalized.py:1220-1227`

1. **指摘内容**: R7/R8対応でセル値とGrammar IDの全件収集は実装されたが、ALL併記グループのvariant対応検査は依然として最初の不一致で即時停止する。ALLの2行へ、それぞれALL_sepに存在しない`missing-one`と`missing-two`を含む併記headwordを設定した一時原本では、`E-DATA-06`のmessage/detailに先行する`missing-one`だけが出て、`missing-two`は列挙されなかった。入力自体は拒否されるため正本の安全性は保たれるが、併記variant対応不能についてE-DATA-06の全件列挙要件を満たさない。

2. **根拠となる設計文書の規則ID**: `NRM-10`、`CLI-12`、`E-DATA-06`、`PLN-08`。

3. **具体的な修正案**: グループを構築してentriesへ`group_ids`を反映する前に、全slash行の空variant・対応不能variant・重複variant・group ID重複・レベル不一致を行・variant出現順で収集し、単一の`E-DATA-06`へ格納する。複数行・同一行の複数不一致と、異なる`PYTHONHASHSEED`でmessage/detailが全件かつバイト一致することを確認する。

## 確認できた正常系・DoD再確認

- 独立した2つの一時出力先への通常ビルドは各々終了コード0・stderr空で、3ファイルが相互に、かつ作業ツリーの正規化正本とバイト一致した。SHA-256はlexicon=`11ac8d1d6b42e5fbd37baa1005b55d7904f42f2753e0720018bbc9edb977c3c7`、grammar=`6a435941ff1105a78b76fae0c141288a783d31148449302621d3b42a8ebbff62`、meta=`fd8c51b2f664f5eaef04c73936927fcd9cb1eb1c2bae56b65df9ad53ec0f0fd4`。
- lexicon・grammarは各スキーマに適合した。lexiconは7,988件、A1=1,200 / A2=1,443 / B1=2,486 / B2=2,859、`(headword,pos)`ユニーク7,988件、ALL行=7,801、groups=179かつ全member 2件以上。grammarは501件、親263件・枝番238件、全親解決、target_eligible=256件、直接付与の単一値152件・範囲値104件、未付与親16件のID集合も仕様値と一致した。
- `gp:1-1` / `gp:1-2` / `gp:1-3`は`gp:1`の範囲を継承し、`source=kyoinban_inherited`、`inherited_from=gp:1`だった。meta.countsも実件数と一致した。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 fail・終了コード0・stderr空だった。独立一時環境で、正規化3ファイル欠落はD07〜D09=`E-DATA-03`、原本改変はdoctor/build=`E-DATA-02`、`limits.json`欠落はD10=`E-DATA-05`となり、いずれも終了コード1だった。
- `.venv/bin/python scripts/build_normalized.py --diff`は全added / removed / level_changedが0、`written=[]`、終了コード0だった。`--dry-run`も`written=[]`、終了コード0だった。
- `scripts/setup.py`・`scripts/build_normalized.py`・`scripts/doctor.py`の構文コンパイルと`git diff --check 36f0ceb`はpassした。
- M1 DoDの通常ケースは6/6を再確認できたが、R9-01で原本変更拒否を迂回でき、R9-02でdoctorが破損JSONを正常判定または中断し、R9-03で原本不正が部分更新を伴う内部エラーになるため、レビューは未収束と判定する。

## 要約

- blocker: 0件
- major: 3件
- minor: 2件
- **判定**: `/m-fix` で修正が必要。
