# M2 コードレビュー R1

- **対象差分**: `18841cc03759822d77893f327a7545d4d4c5fde3`（M1確定コミット）から `12ca41723270b818de0e390d4692fe6cff080834`（レビュー開始時の作業ツリー。新規レビューファイルを除く）まで
- **レビュー日**: 2026-08-16
- **仕様の正**: `docs/`、`schemas/`
- **M2完了条件**: `IMPLEMENTATION_PLAN.md` M2 DoD
- **レビュー結果**: blocker 0件、major 1件、minor 5件。R1-01により、仕様違反の候補を機械検査で`pass`にできるため未収束。

## 独立検査の新規指摘

## R1-01 — major — `scripts/machine_check.py:828-845`

1. **指摘内容**: `pos_pool_relaxed=true` のとき、各誤答の品詞が対象と同じ互換品詞群に属することしか検査しておらず、実際に品詞緩和が必要・実施済みかを検査しない。対象 `lex:book:noun`（A1）には同レベル・同品詞で対象自身を除く候補が652件あるにもかかわらず、4肢を全てA1 nounのまま `pos_pool_relaxed=true` とした候補をCLIへ渡すと、`V-DIS-02`なし・`violations=[]`・`verdict=pass`となった。GEN-15は、同レベル・同品詞プールが3語未満の場合に限って緩和を許し、緩和しなかった場合はフラグをfalseにするよう要求しているため、現実と異なる緩和記録を受理している。

2. **根拠となる設計文書の規則ID**: `GEN-15`、`MC-23-2`、`MC-28`（`V-DIS-02`）、`CI-MCH-11`、`MC-02`、`MC-03`。

3. **具体的な修正案**: ターゲットを除く同レベル・同品詞の実在エントリ数をlexiconから決定的に算出する。`pos_pool_relaxed=true` は、その数が3未満かつ誤答に対象と異なる互換品詞が少なくとも1件ある場合だけ許可し、それ以外は `V-DIS-02` とする。false時の品詞不一致、true時の互換群外、trueだが緩和不要、trueだが全誤答同品詞、正当に緩和した場合を回帰検査する。

## R1-02 — minor — `scripts/machine_check.py:14`

1. **指摘内容**: `datetime.UTC` はPython 3.11で追加された名前であるため、Python 3.10ではモジュール読込み時に `ImportError` となり、`main()` 内の前提検査へ到達しない。その結果、要求版未満のPythonを `E-ENV-01` のCLI-05形式エラーJSONで停止させる契約を満たせず、スタックトレースだけを出す。`lookup.py` と共有前提検査自体はPython 3.10で版不足を判定できるため、M2の2本で挙動も一致しない。

2. **根拠となる設計文書の規則ID**: `CLI-02`、`CLI-05`、`CLI-08`、`E-ENV-01`、`ERR-03`、`PLN-08`。

3. **具体的な修正案**: Python 3.10でもimport可能な `datetime.timezone.utc` を使用し、版判定後だけUTC日時を生成する。少なくともPython 3.10で `machine_check.py` と `lookup.py` を起動し、両方が検出版・要求版・具体的remedyを含む `E-ENV-01`、終了コード1を返す回帰検査を追加する。

## R1-03 — minor — `scripts/machine_check.py:744-763`, `scripts/machine_check.py:846-855`

1. **指摘内容**: ターゲットがlexiconに存在しないと、`target_entry is not None` のガードにより、ターゲット実在性とは独立に判定できる2検査まで省略される。存在しない `target.ref`、空欄置換結果と異なる `sentence_complete`、正解肢のアンカーが `target.ref` と異なる候補を実行すると、`V-TGT-03` と出現0回の `V-TGT-02` は出る一方、完成文不整合の `V-TGT-02` と正解アンカー不一致の `V-DIS-03` は出なかった。判定自体は既にfailだが、MC-04の検出可能な全違反列挙を満たさず、再生成入力から2つの修正点が欠落する。

2. **根拠となる設計文書の規則ID**: `MC-04`、`MC-19-4`、`MC-23-3`、`MC-28`（`V-TGT-02` / `V-DIS-03`）。

3. **具体的な修正案**: 空欄置換結果と `sentence_complete` の比較、および正解・誤答アンカーIDと宣言 `target.ref` の同一性検査は、ターゲットエントリの取得成否に依存させない。実値 `headword`・level・互換品詞群のようにエントリを要する検査だけをガード内に残し、複数の独立違反を同時投入して全コードが固定順で列挙されることを確認する。

## R1-04 — minor — `scripts/machine_check.py:138-139`, `scripts/machine_check.py:891-900`

1. **指摘内容**: `answer` / `answer_equivalents` の重複判定に、MC-24-2が要求する大文字小文字無視・前後空白除去に加えて、NFC正規化と内部の連続空白1個化まで適用している。`answer="I  am"`、`answer_equivalents=["i am"]` のスキーマ適合候補は、仕様上の比較では異なる文字列だが、実装は両方を `"i am"` に変換して `V-CLZ-02` とした。機械検査failは後続工程で覆せないため、定義されていない正規化で候補を不可逆に拒否する。

2. **根拠となる設計文書の規則ID**: `MC-24-2`、`MC-03`、`MC-28`（`V-CLZ-02`）。

3. **具体的な修正案**: MC-24専用の比較キーを用意し、`strip()` と大文字小文字無視だけを適用する。NFC・trim・小文字化・連続空白正規化を要求するMC-26の書き換え比較では、現在の `normalized_comparison()` を維持する。内部空白違い、前後空白違い、大文字小文字違い、NFC/NFD違いを分けた回帰検査を追加する。

## R1-05 — minor — `scripts/lookup.py:95-101`, `scripts/lookup.py:187-192`

1. **指摘内容**: 不正な `--pos bogus` は `E-INPUT-04`・終了コード1で拒否できるが、message/detailの許容値は「Wordlist pos 15種」としか示されず、15値を列挙しない。エラーコード目録はE-INPUT-04で許容値の列挙または範囲を明記するよう要求しており、この文言だけでは利用者が正しい値を特定できない。

2. **根拠となる設計文書の規則ID**: `CLI-29`、`ERR-02`、`E-INPUT-04`、`PLN-08`。

3. **具体的な修正案**: `POS_VALUES` を決定的な順で `noun|verb|...|infinitive-to` のようにmessageとdetailへ列挙する。不正posのCLI回帰検査で、受取値・15許容値・具体的remedy・終了コード1を確認する。

## R1-06 — minor — `scripts/machine_check.py:197-209`

1. **指摘内容**: 非標準JSON定数や有限floatへ変換できない数値は `strict_json_loads()` から通常の `ValueError` として返るため、`parse_candidate()` は行・列を取得せず「位置情報を取得できません」と出力する。実際にstdinへ `{"x": NaN}` を渡すと `E-INPUT-03` では停止するものの、detailにsourceとエラー文しかなく、E-INPUT-03のmessage要件であるパーサ位置情報（行・列）が欠落した。

2. **根拠となる設計文書の規則ID**: `CLI-05`、`CLI-07`、`ERR-02`、`E-INPUT-03`、`PLN-08`。

3. **具体的な修正案**: strict JSON境界で非標準定数・有限表現不能数・巨大整数を検出したトークンのオフセットを保持し、元テキストから行・列を算出して `JSONDecodeError` と同じdetail/messageへ載せる。ファイル・stdinそれぞれで構文エラー、`NaN` / `Infinity`、オーバーフロー数値を投入し、全条件で対象・行・列・remedyを持つ `E-INPUT-03` になることを確認する。

## 確認できた正常系・DoD再確認

- `18841cc` から `12ca417` のM2差分を対象とし、レビュー開始時の作業ツリーが同コミットと一致していることを確認した。M2は初回ラウンドのため前回指摘はない。
- `lookup.py` のCI-LKP-01〜04代表経路をCLIで再実行し、`abandon`のB1 verb、A1指定0件、`Tokyo` 0件、`watch`のA1 noun/verb、`gp:13`と`gp:1-1`の教員版情報・パターン略記・継承レベルを確認した。
- 妥当な `grammar_cloze` 候補を `machine_check.py --candidate -` で2回実行し、両方が `verdict=pass`、machine_reportスキーマ違反0件、`generated_at`除去後の正準JSONがバイト一致した。
- `.venv/bin/python scripts/doctor.py` は12 pass / 0 fail、`.venv/bin/python -m py_compile scripts/lookup.py scripts/machine_check.py` と `git diff --check 18841cc 12ca417` はpassした。
- M2 DoDの通常経路は記録どおり動作したが、R1-01によりCI-MCH-11が表す誤答プール規則を満たさない候補がpassする。またR1-02・R1-05・R1-06はCLI契約・エラーコード目録の未充足である。

## 要約

- blocker: 0件
- major: 1件
- minor: 5件
- **判定**: `/m-fix` で修正が必要。
