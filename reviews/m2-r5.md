# M2 コードレビュー R5

- **対象差分**: `18841cc03759822d77893f327a7545d4d4c5fde3`（M1確定コミット）から `bd5580aa525c72f0e6f46cdae90f7407add9920e` およびレビュー開始時点の作業ツリー（clean）まで
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M2完了条件**: `IMPLEMENTATION_PLAN.md` M2 DoD
- **前回レビュー**: `reviews/m2-r4.md`
- **対応記録**: `reviews/m2-r4-resolutions.md`
- **前回指摘の解消判定サマリー**: R4-01〜R4-03は、対応記録・承認済み決定・仕様差分・実装差分・前回の再現条件を突き合わせ、全件解消を確認した。
- **レビュー結果**: 独立検査で新規指摘なし。未解消指摘の合計はblocker 0件、major 0件、minor 0件。

## R4指摘の解消確認

### R4-01

- **判定**: 解消
- **差分確認**: candidateと期待条件のscaleが同じ場合に `validation_level` として期待レベルを選び、その値をS4の語数上限、S5の語彙照合、S6の語彙・文法ターゲット照合および誤答アンカー照合へ一貫して渡す実装になった。scaleが異なる場合はcandidate側のscale・値で実行可能な検査を継続するため、MC-01・MC-04の全件列挙規則とも整合する。
- **実行確認**: B1の `abandon` 語彙候補へ期待A1を渡し、level不一致の `V-COND-01` とA1基準の `V-LEX-02`・`V-TGT-03` が同一レポートに併記されることを確認した。対応記録の11語以上フィクスチャでは `V-LEN-01` も含むCI-MCH-17の全条件が記録されており、文法対象・語彙4択誤答・scale相違の各分岐も仕様と実装が一致する。

### R4-02

- **判定**: 解消
- **差分確認**: 承認済みM2D-10により、`docs/cefrj-validation-spec.md` MC-10は機械検査の参照キーへ `set_question_max` を追加し、`machine_check.py` の参照をS1の `--requested-count` 値域検査に限定した。`docs/architecture.md` CLI-16および `config_limits.schema.json` の目録とも矛盾しない。
- **実装確認**: `scripts/machine_check.py` における `set_question_max` の参照は `validate_set_conditions()` 内の1箇所だけであり、要求範囲外への参照拡大はない。

### R4-03

- **判定**: 解消
- **差分確認**: 承認済みM2D-11により、candidate JSON整数の上限は符号を除く10進4,300桁へ固定され、CLI-07・MC-05・CI-MCH-18へ同期された。実装は数値変換前に字句桁数を検査し、プロセス内の整数文字列変換上限も4,300へ固定する。
- **実行確認**: 余分フィールド値が4,300桁の入力は整数桁数を理由とする停止ではなく `E-CONTRACT-01` のスキーマ検証へ進み、4,301桁・5,000桁は位置・上限・実測桁数を持つ `E-INPUT-03` となった。5,000桁入力は `PYTHONINTMAXSTRDIGITS=4300` と `0` でエラーJSONがバイト一致した。

## 独立検査の新規指摘

新規指摘なし。

## 確認できた正常系・DoD再確認

- M2差分全体とR4修正差分を読み、`machine_check.py` の19違反コード・3警告コードが `docs/cefrj-validation-spec.md` MC-28/29および `machine_report.schema.json` のquestion分岐と一致することを確認した。9形式の抽出・構造・語彙照合・ターゲット・選択肢・誤答由来・整序・穴埋め・書き換えの各経路に、新たな仕様不整合はなかった。
- `lookup.py` は通常照会の正規化順、複数条件AND、headword併記グループ展開後の絞り込み、誤答プールの対象除外・同レベル同品詞・カテゴリ優先、文法のNFC/casefold部分一致・レベル範囲包含・先行文脈要求除外をCLI-29〜31どおり実装している。`abandon`、`Tokyo`、`watch`、`gp:13`、`gp:1-1` の代表照会も期待値と一致した。
- `docs/question-generation-spec.md` の9形式candidate例は全件candidateスキーマに適合し、各例から生成したmachine reportも全件 `machine_report.schema.json` 1.1.0に適合した。語彙4択2例は文書に明記された実データとのアンカー差分による `V-DIS-01` のみ、残る7形式は違反なしだった。
- `.venv/bin/python scripts/doctor.py` は12 pass / 0 failだった。9スキーマはDraft 2020-12として自己妥当、`scripts/lookup.py`・`machine_check.py`・`build_normalized.py`・`doctor.py` はPython 3.7 grammar指定の `ast.parse` と `py_compile` を通過した。`git diff --check 18841cc..HEAD` もpassした。
- `CHANGELOG.md` のR4修正後DoD記録（CI-MCH-01〜18、CI-LKP-01〜04、36件のmachine reportスキーマ適合、`generated_at` 除外後の決定性）と、上記の独立再確認に矛盾はない。M2 DoD 1〜4に未充足項目はない。

## 要約

- blocker: 0件
- major: 0件
- minor: 0件
- **判定**: 収束。コミット可。
