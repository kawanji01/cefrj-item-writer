# M3 コードレビュー R1

- **対象差分**: `cf2307514bbb8f501d02bbdfce6dbb724ba3f16c`（M2確定コミット）から `714efe3d542fd06eb1d27c5d9e9d22174688a84b`（M3実装コミット）およびレビュー開始時点の作業ツリー（clean）まで
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M3完了条件**: `IMPLEMENTATION_PLAN.md` M3 DoD
- **前回レビュー**: なし（M3 R1）
- **レビュー結果**: major 1件を検出した。blocker 0件、major 1件、minor 0件。

## 独立検査の指摘

## R1-01 — major — `scripts/validate.py:259, 438-450`

1. **指摘内容**: 通常モードの入力ファイル判定に使う `Path.is_file()` と、状態確認モードの `set.json` 判定に使う `Path.exists()` / `Path.is_symlink()` / `Path.is_file()` が `OSError` の捕捉範囲外にある。そのため、パスは存在するが親ディレクトリを探索できない等の読取り不能条件で、定義済みの `E-INPUT-02` ではなく未定義内部エラーの終了コード2とtracebackになる。実際に、妥当なset_id名のディレクトリへファイルを置き、ディレクトリをread-onlyかつ非searchable（mode `0400`）にしてPython 3.13.1で実CLIを実行すると、通常モードは259行目の `is_file()`、状態確認モードは439行目の `exists()` から `PermissionError` が漏れ、双方とも終了コード2になった。状態確認モードでは直前の `os.scandir()` は成功するため、425〜436行目のディレクトリ検査でも捕捉されない。入力の読取り不能は仕様が明示する通常の入力エラーであり、内部バグ扱いにしてはならない。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` CLI-02・CLI-08・CLI-27A・ERR-03・E-INPUT-02、`IMPLEMENTATION_PLAN.md` PLN-08。

3. **具体的な修正案**: 通常モードでは `is_file()` を含むファイル種別確認と `read_bytes()` を同じ `try` / `except OSError` 境界に入れ、対象パスを持つ `E-INPUT-02` へ変換する。状態確認モードでは `set.json` の `lstat` / 種別確認 / 読取りを一つの捕捉境界で扱い、`FileNotFoundError` のときだけ `status=incomplete`、その他の `OSError` と通常ファイルでない場合は `E-INPUT-02` にする。存在確認後の競合でも例外が漏れないようにし、通常モードと状態確認モードの両方へ読取り不能パスの回帰検査を追加する。

## 確認できた正常系・DoD再確認

- M3差分全体（`cf23075` から `714efe3`）と、`scripts/validate.py`、承認済みM3D-01、同期されたCLI-26〜27A・CI-CLI-04を確認した。通常モードと状態確認モードの引数排他、9識別子とスキーマファイルの対応、スキーマsemver取得、違反の決定的ソート・50件上限、正準JSON出力は仕様と一致する。
- `docs/question-generation-spec.md` の9形式candidate例は全件 `validate.py --schema candidate` で終了コード0となり、`docs/json-output-spec.md` のset例も `--schema set` で合格した。コミット済みの正規化2文書・設定2文書も各対応スキーマに合格した。
- JSON構文不正・`NaN`・有限floatに変換不能な `1e400` は、stdin経路で位置付き `E-INPUT-03` となった。未知スキーマは許容9値を列挙する `E-INPUT-04`、必須引数欠落は `E-INPUT-01` となった。妥当な監査のみのディレクトリは `--set-dir` で終了コード0・`status=incomplete`・`set_json_path=null`・`validation=null` を返した。
- Python 3.8・3.9・3.10では、有効なvalidate.py呼出しがstdout空・終了コード1・`E-ENV-01`となった。Python 3.7 grammar指定の構文解析、現行Pythonでのコンパイル、`git diff --check cf23075..714efe3` はpassした。
- `doctor.py` は12 pass / 0 failで、D11により9スキーマ全てがJSON Schema draft 2020-12として自己妥当であることを再確認した。上記R1-01を除き、M3 DoD 1〜4の記録と独立再確認に矛盾はない。

## 要約

- blocker: 0件
- major: 1件
- minor: 0件
- **判定**: `$m-fix` で修正が必要。
