# M3 コードレビュー R2

- **対象差分**: `cf2307514bbb8f501d02bbdfce6dbb724ba3f16c`（M2確定コミット）から `714efe3d542fd06eb1d27c5d9e9d22174688a84b`（M3実装コミット）およびレビュー開始時点の作業ツリー（`scripts/validate.py` のR1修正、未追跡のR1レビュー・解決記録）まで
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M3完了条件**: `IMPLEMENTATION_PLAN.md` M3 DoD
- **前回レビュー**: `reviews/m3-r1.md`
- **対応記録**: `reviews/m3-r1-resolutions.md`
- **前回指摘の解消判定サマリー**: R1-01は、対応記録と実差分を突き合わせ、通常モード・状態確認モードの読取り不能再現条件を実CLIで再検査して解消を確認した。
- **レビュー結果**: 独立検査でminor 1件を検出した。未解消指摘の合計はblocker 0件、major 0件、minor 1件。

## R1指摘の解消確認

### R1-01

- **判定**: 解消
- **差分確認**: 通常モードは `Path.stat()` と `Path.read_bytes()`、状態確認モードは `Path.lstat()`・通常ファイル判定・`Path.read_bytes()` をそれぞれ同じ `OSError` 捕捉境界に収めている。妥当なset_id名のディレクトリに `set.json` を置いてmode `0400`にした前回再現条件を実CLIへ再投入し、通常モード・`--set-dir` の双方がstdout空・終了コード1・`E-INPUT-02`となり、tracebackと終了コード2が解消されたことを確認した。状態確認モードは `FileNotFoundError` の場合だけ `status=incomplete` へ変換し、通常ファイルでない `set.json` とその他の読取り失敗を `E-INPUT-02` にしている。

## 独立検査の新規指摘

## R2-01 — minor — `scripts/validate.py:167-168, 251-252, 294, 319, 412-420, 465-466`

1. **指摘内容**: CLIエラーの `message` へ、入力パスやスキーマ違反理由に含まれるユーザー制御文字列を改行除去せず埋め込んでいるため、`message` が1行要約にならない。実際に、改行を含む不存在パス `/tmp/missing\nfile.json` を通常モードへ渡すと `E-INPUT-02` の `message` 値にLFが残った。また、妥当なcandidateへ余分なプロパティ名 `bad\nkey` を追加すると、stdoutの違反理由とstderrの `E-CONTRACT-01.message` の双方にLFが残った。JSON自体はエスケープされてパース可能で終了コードも正しいため検証ゲートは機能するが、CLI-05・ERR-02が定める `message` の「日本語1行要約」契約に違反し、行単位でエラー要約を扱う呼出し側やログで1件のエラーが複数行に分断される。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` CLI-05・ERR-02・E-INPUT-02・E-CONTRACT-01、`IMPLEMENTATION_PLAN.md` PLN-08。

3. **具体的な修正案**: `message` を構成する直前に、パス・OSエラー文字列・違反理由などの動的値を共通の1行化関数へ通し、CR/LFを可視な `\\r` / `\\n` 表記へ変換する。元の値は `detail` や構造化された `errors` に保持してよい。改行を含む不存在パスとadditionalProperties名を実CLIへ渡し、stderr JSONをパースした `message` にCR/LFがなく、対象値を識別でき、従来どおり `E-INPUT-02` / `E-CONTRACT-01`・終了コード1となる回帰検査を追加する。

## 確認できた正常系・DoD再確認

- M3差分全体（`cf23075`から現在の作業ツリー）とR1修正差分を確認した。`scripts/validate.py` 以外のコード変更はなく、`schemas/` 9本にもM3差分はない。
- 9スキーマはJSON Schema draft 2020-12として自己妥当で、`$id` は規定URLとsemver書式に一致した。`doctor.py` は12 pass / 0 failだった。
- 設計文書内の妥当例とコミット済み正規化・設定データを使い、9識別子全てを実CLIで2回ずつ検証した。全件が終了コード0・stderr空・`valid=true`となり、各識別子の2回のstdoutはバイト一致した。
- 9識別子それぞれで必須欠落・型不正・additionalProperties違反を作成し、27/27が違反パスを持つstdout、stderrの `E-CONTRACT-01`、終了コード1となった。
- `docs/question-generation-spec.md` の9形式candidate例は9/9合格した。同文書のcandidateを実 `machine_check.py` へ投入し、そのstdoutを `validate.py --schema machine_report --file -` で再検証して、スキーマ1.1.0への適合と終了コード0を確認した。
- R1修正後も、監査のみのディレクトリは `--set-dir` で `status=incomplete`、通常ファイルでない・読取り不能な `set.json` は `E-INPUT-02` となる。`PYTHONINTMAXSTRDIGITS=4300|0` の差によらず同じJSON入力の検証結果が一致し、Python 3.7 grammar指定の構文解析、現行Pythonのコンパイル、`git diff --check cf23075` はpassした。

## 要約

- blocker: 0件
- major: 0件
- minor: 1件
- **判定**: 収束。コミット可（`$m-commit`）。
