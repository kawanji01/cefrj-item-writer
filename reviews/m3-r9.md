# M3 コードレビュー R9

- **対象差分**: `cf2307514bbb8f501d02bbdfce6dbb724ba3f16c`（M2確定コミット）から `714efe3d542fd06eb1d27c5d9e9d22174688a84b`（M3実装コミット）およびレビュー開始時点の作業ツリー（`scripts/validate.py` のR1〜R7修正、`scripts/build_normalized.py` のR8修正、未追跡のR1〜R8レビューと解決記録）まで
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M3完了条件**: `IMPLEMENTATION_PLAN.md` M3 DoD
- **前回レビュー**: `reviews/m3-r8.md`
- **対応記録**: `reviews/m3-r8-resolutions.md`
- **前回指摘の解消判定サマリー**: R8-01は解消。`machine_report.scope` は文字列かつ既知値の場合だけ対応分岐を選択し、配列・オブジェクト・JSON実数・null・未知文字列はルートスキーマ検証へフォールバックする。前回の再現入力をstdin／ファイルの両経路へ再投入し、全10条件がstdoutに不当結果、stderrに`E-CONTRACT-01`、終了コード1を返し、tracebackと終了コード2がないことを確認した。
- **レビュー結果**: 独立検査でminor 1件を検出した。未解消指摘の合計はblocker 0件、major 0件、minor 1件。

## R8指摘の解消確認

### R8-01

- **判定**: 解消
- **差分確認**: `scripts/build_normalized.py` の `selected_schema_branch()` は、`scope` に対して `isinstance(scope, str)` を先に確認し、`question|set` の場合だけ分岐名を生成する。非文字列と未知文字列はルートスキーマを返すため、通常の型・const・必須違反として処理される。
- **実行確認**: `scope=[]`、`{}`、`1.5`、`null`、`"unknown"` を実CLIのstdin／ファイルへ投入した。全件が終了コード1・`valid=false`・`E-CONTRACT-01`となり、stderrにtracebackはなかった。

## 独立検査の新規指摘

## R9-01 — minor — `CHANGELOG.md:9`

1. **指摘内容**: M3の実装説明が、現在の数値受理契約と実装に追随していない。同行は「有限floatへ変換不能な数値を位置付き`E-INPUT-03`で拒否する」と記載するが、R6・R7修正後の `validate.py` は標準JSON実数をbinary64へ変換せず、正規化係数と任意精度指数で正確に保持する。実際に、公式`config_limits`の`distractor_reuse_max`を`1e400`および`1e9223372036854775806`へ置換した入力は、どちらも同フィールドの`type: integer`・`minimum: 1`に従って`valid=true`・終了コード0となった。これは現行設計に適合する挙動だが、CHANGELOGだけが廃止済みのbinary64上限を入力制約として記録しており、保守時の契約理解と回帰判定を誤らせる。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` CLI-07・CLI-27・E-INPUT-03、`IMPLEMENTATION_PLAN.md` PLN-02・PLN-06、`schemas/config_limits.schema.json` `distractor_reuse_max`。

3. **具体的な修正案**: `CHANGELOG.md` の当該文を、非標準数値定数は位置付き`E-INPUT-03`で拒否する一方、標準JSON数値は任意精度で解釈してスキーマの数値制約で判定する現在の挙動へ更新する。M3のレビュー修正をまとめる際にも、`1e400`と任意精度指数が妥当になることを実行記録へ残す。

## 確認できた正常系・DoD再確認

- 9スキーマはJSON Schema draft 2020-12として自己妥当で、`$id`は規定URL＋semver書式だった。
- `docs/json-output-spec.md` の公式JSON例11件（9識別子、machine_reportとsetは各2件）をstdin／ファイルの両経路で検証し、全22条件が合格した。コミット済みのnormalized 2件・config 2件も実CLIで合格した。
- 9識別子それぞれの必須欠落・型不正・additionalProperties違反を2回ずつ実CLIへ投入した全54実行は、終了コード1・`E-CONTRACT-01`・期待するJSONポインタとなり、同一入力のstdout／stderrはバイト一致した。
- 公式candidateを `machine_check.py` へ投入したmachine_reportは、`validate.py --schema machine_report` で合格した。
- `--set-dir` は、同一セットディレクトリについてset.json欠落を`status=incomplete`、妥当なset.jsonを`status=complete`かつvalid、不当なset.jsonをcomplete状態のまま`E-CONTRACT-01`として処理した。
- 公式妥当例を決定的に変異させた4,471件について、現行検証器とDraft 2020-12参照検証器の合否を比較し、意図したECMA-262／Python正規表現差を除いて不一致はなかった。
- `doctor.py` は12 pass / 0 fail。Python 3.10.18では`validate.py`が`E-ENV-01`で停止した。現行Pythonでの構文コンパイルと`git diff --check cf23075`もpassした。

## 要約

- blocker: 0件
- major: 0件
- minor: 1件
- **判定**: 収束。コミット可（`$m-commit`）。
