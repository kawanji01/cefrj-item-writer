# M4 コードレビュー R1

- **対象差分**: `b6c84b9c238de72853b763d85aa28b9ce6cbc1e8`（M3確定コミット）から `edb6a0c29e695066cc47b1fdfd5b8fc2164a0ff4`（M4実装コミット、レビュー開始時点の作業ツリーはclean）まで
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M4完了条件**: `IMPLEMENTATION_PLAN.md` M4 DoD
- **前回レビュー**: なし（R1）
- **レビュー結果**: 独立検査でmajor 4件、minor 1件を検出した。未解消指摘の合計はblocker 0件、major 4件、minor 1件。

## 独立検査の新規指摘

## R1-01 — major — `agent/author-core.md:192-225`

1. **指摘内容**: 明示指定された文法IDについて、`target_eligible=false` だが親から有効なレベルを継承している枝番を拒否する対話分岐が定義されていない。例えば `gp:1-1` は `level.source=kyoinban_inherited`、`min=A1.1`、`max=A1.2` だが `target_eligible=false` である。実CLIでは `lookup.py gp --id gp:1-1 --level A1.2` が0件、レベル指定なしでは当該項目と継承レベルを返す。現行コアはレベル不一致と「教員版レベル未付与で原本のレベル根拠がない項目」だけを具体化しているため、このケースをレベル不一致と誤分類するか、継承レベルを根拠に採用する余地がある。絶対規則には256項目限定が書かれているものの、S32の実行可能な分類・説明・解決遷移に落ちておらず、ITEM LISTのみの継承項目220件に対する明示指定フローが閉じていない。

2. **根拠となる設計文書の規則ID**: `docs/cefrj-validation-spec.md` NRM-21-2・NRM-22、`docs/interaction-flow.md` IF-18・IF-20、`IMPLEMENTATION_PLAN.md` M4 DoD 1・5。

3. **具体的な修正案**: S32ではレベル包含判定より先に `target_eligible` を検査し、`false` は常に「教員版256項目の対象外」として採用しない分岐を追加する。`level.source=kyoinban_inherited` の場合は「親のレベルは参照用に継承されているが、当該枝番は教員版の直接割当項目ではないためターゲット不可」と説明し、代替・取り下げ・当該枠のみ提案の選択肢へ進める。`level.source=null` の場合は既存の未付与説明を使う。`gp:1-1` のような継承項目と `gp:47` のような未付与項目を別々に通す回帰確認を追加する。

## R1-02 — major — `agent/author-core.md:454-468`

1. **指摘内容**: 語彙4択で同レベル・同品詞プールが3件未満のとき、互換品詞へ緩和したアンカーを `lookup.py` から取得する配線がない。記載されている唯一の取得コマンド `lookup.py lex --pool-for <target.ref>` は CLI-29 の契約どおり同レベル・同品詞だけを返すため、`lex:been:be-verb` と `lex:to:infinitive-to` では実際に `total=0`、`lex:might:modal-auxiliary` でも `total=1` となる。一方、後続文は互換品詞群への緩和を許すだけで、どの `lookup.py` 呼び出しから実在アンカーの全フィールドを得て、カテゴリ優先順位とNRM-13順をどう合成するかを定めていない。生成器は正規化データ全体を直接読まない前提なので、希少品詞を明示指定した4択問題では、3誤答を生成できないか、照合していないアンカーを補うことになる。

2. **根拠となる設計文書の規則ID**: `docs/question-generation-spec.md` GEN-13・GEN-14・GEN-15・PRM-07、`docs/architecture.md` CLI-29・CLI-31、`IMPLEMENTATION_PLAN.md` M4 DoD 3・4。

3. **具体的な修正案**: `--pool-for` の `total<3` を検出した後、互換品詞群の各posについて既存の `lookup.py lex --level ... --pos ...`（必要に応じて対象の非nullカテゴリ値ごとの `--category`）を実行し、対象自身と既出IDを除外してGEN-14のカテゴリ順位、同順位NRM-13順で決定的にマージする手順をコアへ明記する。既存CLIだけで必要な候補順を完全に取得できないと判断する場合は、PLN-05で `lookup.py` の緩和プール契約を先に決定し、関連仕様を同期してから配線する。上記3ターゲットを含む `pos_pool_relaxed=true` の実candidateを `validate.py` と `machine_check.py` に通す確認を追加する。

## R1-03 — major — `agent/author-core.md:188-196`

1. **指摘内容**: 教師入力や正規化値をCLIへ渡すコマンド例が引数安全になっていない。`--headword <headword>`、`--keyword <文字列>`、および後段の `--pool-for <target.ref>` を引用符なしで置換すると、仕様が明示的に許す複数語見出しが複数引数へ分割される。実際に記載形へ `CD player` を代入した `lookup.py lex --headword CD player --limit 200` は、`player` を未知の引数として `E-INPUT-01`・終了コード1になり、引用した場合だけ `total=1` で成功した。空白だけでなくシェルメタ文字を含む自由入力を文字列連結すると、意図しないシェル解釈も起こり得る。

2. **根拠となる設計文書の規則ID**: `docs/interaction-flow.md` QT-31a・QT-31b・IF-17、`docs/architecture.md` CLI-06・CLI-29・CLI-30、`IMPLEMENTATION_PLAN.md` PLN-08・M4 DoD 1。

3. **具体的な修正案**: コマンド文字列へ値を直接連結せず、ホストが構造化argvを渡せる場合は引数配列を使用し、シェル経由しかない場合は対象シェルに対応した安全な引用を必須とする規則をコアへ追加する。headword・keyword・category・pool-for・ファイルパスを同じ境界で扱い、`CD player`、`ought to`、空白を含む文法キーワードが単一引数として照会される回帰確認を追加する。

## R1-04 — major — `agent/author-core.md:586`

1. **指摘内容**: 終了コード1の処理が「stderr最終行のエラーコードとremedy」を読むよう指示しているが、全CLIのエラーJSONはCLI-04の正準形（インデント2）で複数行に直列化されるため、物理的な最終行は `}` だけである。実際の `lookup.py` の `E-INPUT-01` も複数行JSONで、最終行からは `error_code` も `remedy` も取得できない。このままではmachine_checkやlookupの定義済み停止時に、教師へ必須のコードと日本語対処手順を提示できず、FMT-80bのエラー事象にも必要値を埋められない。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` CLI-02・CLI-04・CLI-05、`docs/interaction-flow.md` IF-04・IF-42（FMT-80b事象16）、`IMPLEMENTATION_PLAN.md` PLN-08。

3. **具体的な修正案**: stderrの物理的な最終行ではなく、stderr全体をUTF-8のJSON文書として解析し、`error_code`・`message`・`remedy`・`detail` を取得するよう指示を改める。終了コード1でJSON解析にも失敗した場合だけ内部契約違反として扱い、成功や業務上の不合格へ読み替えない。lookup・validate・machine_checkの代表的な終了コード1を使い、コードとremedyが表示文へ入る回帰確認を追加する。

## R1-05 — minor — `CHANGELOG.md:20`

1. **指摘内容**: M4 DoD 4の記録が `proper_nouns.json` の50語を「機能語allowlist」と記載している。実ファイルは人名・地名・国名・言語名などの固有名詞50語であり、生成プロンプトのPRM-04も固有名詞allowlistとして扱う。機能語群はGEN-15の互換品詞群で、当該設定とは別概念であるため、検証記録が確認した対象を誤って説明している。

2. **根拠となる設計文書の規則ID**: `docs/question-generation-spec.md` GEN-10・PRM-04、`docs/cefrj-validation-spec.md` MC-18、`docs/json-output-spec.md` NDS-04、`IMPLEMENTATION_PLAN.md` PLN-02。

3. **具体的な修正案**: 「実行時に全件展開する50語の固有名詞allowlist」と訂正し、必要なら `data/config/proper_nouns.json` の `words` 件数が50であることを確認した旨を明記する。

## 確認できた正常系・DoD再確認

- 対象差分はM3確定コミットからM4実装コミットまでの4ファイル（`agent/author-core.md`、`docs/interaction-flow.md`、`DECISIONS.md`、`CHANGELOG.md`）で、レビュー開始時点の作業ツリーはcleanだった。`git diff --check b6c84b9..HEAD` はpassした。
- `doctor.py` は12 pass / 0 failで、固定依存、原本チェックサム、正規化データ、設定2件、9スキーマ、レビュアー配線を確認した。
- M4D-01は `DECISIONS.md` に承認決定として記録され、`docs/interaction-flow.md` IF-21a・DLG-45と `agent/author-core.md` の複数一致選択へ同期されていた。文法部分一致はlookup返却順を維持し、先頭候補の自動採用を禁止している。
- `agent/author-core.md` はS00〜S99、1ターン1質問、S10〜S70の主要な固定質問・再質問、S70の依存リセット、S80の進捗書式、PRM-01〜PRM-14、9形式のbodyフィールド、および `validate.py`→`machine_check.py` の正常系配線を記載している。
- `abandon` / A1 と `Tokyo` の明示指定拒否、教員版直接割当だけを使う文法提案、`grammar_reorder` / `grammar_rewrite` での先行文脈要求項目除外は仕様どおり記載されている。
- candidate監査JSONのUTF-8・非ASCII非エスケープ・キー辞書順・インデント2・LF・末尾改行という保存形は `docs/json-output-spec.md` JS-01/JS-02と一致し、machine_checkには確定済みformat・level・要求問題数を必須引数として渡している。

## 要約

- blocker: 0件
- major: 4件
- minor: 1件
- **判定**: `$m-fix` で修正が必要。
