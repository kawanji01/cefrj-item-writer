# M4 コードレビュー R2

- **対象差分**: `b6c84b9c238de72853b763d85aa28b9ce6cbc1e8`（M3確定コミット）からレビュー時点の作業ツリーまで。作業ツリーのHEADは `edb6a0c29e695066cc47b1fdfd5b8fc2164a0ff4`（M4実装コミット）で、R1対応の未コミット差分を含む。`reviews/m4-r1.md` と `reviews/m4-r1-resolutions.md` は前回指摘照合の入力として扱った。
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M4完了条件**: `IMPLEMENTATION_PLAN.md` M4 DoD
- **前回レビュー**: `reviews/m4-r1.md`（major 4件、minor 1件）
- **レビュー結果**: 前回5件は全件解消を確認した。独立検査で新規major 1件を検出した。未解消指摘の合計はblocker 0件、major 1件、minor 0件。

## 前回指摘の解消判定

| 指摘 | 判定 | 確認内容 |
|---|---|---|
| R1-01 | 解消 | 文法IDはレベル包含より先に `target_eligible` を判定し、`gp:1-1` の `kyoinban_inherited` と `gp:47` の `level.source=null` を別理由でDLG-42へ送る手順になった。実lookupでも両項目は `target_eligible=false`、`gp:1-1 --level A1.2` は0件だった。 |
| R1-02 | 解消 | 同品詞プール不足時に互換品詞群を既存lookupで全取得し、カテゴリ順位＋NRM-13順で決定的に統合する手順が追加された。`lex:been:be-verb` とA2 verb 3件を使う `pos_pool_relaxed=true` の実candidateは `machine_check.py` で `verdict=pass`、既存の `watch`→`watched` もpassし、表層形優先修正とレンマ照合の回帰がないことを確認した。 |
| R1-03 | 解消 | 全CLI呼び出しに構造化argvを要求し、自由入力・ID・カテゴリ・パスを各1要素で渡す規則が追加された。`CD player` は単一引数としてlookup成功、`ought to` も引数分割エラーなしで正常終了した。 |
| R1-04 | 解消 | 終了コード1ではstderr全体をUTF-8 JSONとして解析し、`error_code`・`message`・`remedy`・`detail` を取得するよう修正された。物理的な最終行への依存は除去されている。 |
| R1-05 | 解消 | M4 DoD 4の記録が `data/config/proper_nouns.json` の50語の固有名詞allowlistへ訂正され、実データ件数も50語だった。 |

## 独立検査の新規指摘

## R2-01 — major — `agent/author-core.md:615`

1. **指摘内容**: candidateスキーマ不通過時の実行分岐が相互に矛盾している。第6節の手順4（598行目）は、`validate.py` 不通過をT2/T3として同一世代内で1回再指示し、2回目で世代を消費する。一方、後段の共通規則は「doctor以外のCLIが終了コード1で停止した場合」を一律にFMT-80b事象16の中止へ送る。`validate.py --schema candidate` は不当candidateに対して、仕様どおりstdoutへ `valid=false` と全違反を返し、stderrへ `E-CONTRACT-01`、終了コード1を返す。実CLIへ `{}` を入力してもこの経路になったため、現行指示のままでは同じ終了コード1をT2の再指示とセッション中止の双方に分類できる。実行ホストの解釈によっては、通常回復可能な生成JSON不備でセットを即時中止し、許可された同一世代内再指示を行わない。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` CLI-27、`docs/subagent-review-spec.md` T2・T3・RG-04・RG-05、`docs/interaction-flow.md` IF-42（FMT-80b事象2・3・16）、`IMPLEMENTATION_PLAN.md` M4成果物2・M4 DoD 3。

3. **具体的な修正案**: 終了コード1の共通処理に明示的な優先分岐を設ける。`validate.py --schema candidate` が `valid=false` / `E-CONTRACT-01` を返した場合は中止事象16へ送らず、stdoutの全スキーマ違反をT2の再指示へ渡し、1回目・2回目の生出力と検証エラーをAU-03/AUD-09の `candidate.invalid1.txt` / `candidate.invalid2.txt` に保存する。2回目だけT3として世代を消費する。それ以外の定義済み停止、およびmachine_report内部生成結果の `E-CONTRACT-01` は既存の中止処理へ送る、と対象CLI・対象スキーマ・エラーコードで分岐を閉じる。candidate不通過1回目、2回目、lookupの定義済み停止の3条件で遷移とFMT-80b事象文を回帰確認する。

## 確認できた正常系・DoD再確認

- `git diff --check b6c84b9` はpassした。
- `doctor.py` は12 pass / 0 fail、終了コード0だった。
- `gp:1-1` は `target_eligible=false` / `level.source=kyoinban_inherited`、`gp:47` は `target_eligible=false` / `level.source=null` であり、R1-01の2分岐に必要な実値を確認した。
- `lex:been:be-verb` の表層形 `been` は `matched_entry_id=lex:been:be-verb` / `decision=target` となり、互換品詞アンカー3件を含む実candidateが違反0件でpassした。`lex:watch:verb` の活用形 `watched` も違反0件でpassした。
- 構造化argvの実行で、空白を含む `CD player` は `lex:CD player:noun` へ正常照合された。
- `proper_nouns.json.words` は50件で、R1-05の修正文言と一致した。

## 要約

- blocker: 0件
- major: 1件
- minor: 0件
- **判定**: `$m-fix` で修正が必要。
