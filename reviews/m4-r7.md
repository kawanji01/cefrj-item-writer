# M4 コードレビュー R7

- **対象差分**: `b6c84b9c238de72853b763d85aa28b9ce6cbc1e8`（M3確定コミット）からレビュー時点の作業ツリーまで。作業ツリーのHEADは `edb6a0c29e695066cc47b1fdfd5b8fc2164a0ff4`（M4実装コミット）で、R1〜R6対応の未コミット差分を含む。`reviews/m4-r6.md` と `reviews/m4-r6-resolutions.md` は前回指摘照合の入力として扱った。
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M4完了条件**: `IMPLEMENTATION_PLAN.md` M4 DoD
- **前回レビュー**: `reviews/m4-r6.md`（major 1件）
- **レビュー結果**: 前回1件は解消を確認した。仕様逸脱、M4 DoD未充足、決定性の穴、CLI契約不一致、前回修正による新規リグレッションは検出しなかった。未解消指摘の合計はblocker 0件、major 0件、minor 0件。

## 前回指摘の解消判定

| 指摘 | 判定 | 確認内容 |
|---|---|---|
| R6-01 | 解消 | 承認済みM4D-07/M4D-08に従い、対象出現フィールドでは宣言ターゲット候補を一般複数語候補と同じ最長一致で比較し、同長時だけ対象IDを優先する。採用区間の全トークンへ同じ対象ID・`decision=target`を記録し、原文スライスと`target_surface`を完全一致で検査する。`vocab_mcq_ja2en`の選択肢では、実在anchorの記録値と選択肢表記が実値に一致する場合だけ、フィールド全体を宣言アンカー区間として同じentry ID・`wordlist_match`で消費する。実データ7,988エントリを宣言ターゲット・宣言アンカーの双方で総当たりし、固定spaCyモデルが複数トークンへ分割する`lex:wed:verb`と`lex:'m:be-verb`を含めて取りこぼし0件だった。`all right`では、非対象時のID辞書順、短い`right`より長い一般候補の優先、同長時の`lex:all right:adverb`ターゲット優先もそれぞれ維持されている。 |

## 独立検査の新規指摘

指摘なし。

## 確認できた正常系・DoD再確認

- `git diff --check b6c84b9c238de72853b763d85aa28b9ce6cbc1e8` はpassした。
- `machine_check.py`、`lookup.py`、`validate.py`、`doctor.py` はPython構文検査を通過した。
- `doctor.py` は12 pass / 0 fail、終了コード0だった。
- 9本のスキーマはJSON Schema draft 2020-12として自己妥当だった。`docs/question-generation-spec.md` の9形式のJSON例もcandidateスキーマに9/9で適合した。
- `abandon` / A1は0件、レベル指定なしでは `lex:abandon:verb` / B1、`Tokyo`は0件となり、M4 DoD 2の照合前提を確認した。
- 文法提案は9レベル全てで `target_eligible=true` の項目だけを返し、適格件数はA1.1=28、A1.2=50、A1.3=61、A2.1=65、A2.2=63、B1.1=76、B1.2=67、B2.1=53、B2.2=34だった。`gp:1-1` と `gp:47` はいずれも `target_eligible=false` で、明示要求の拒否分岐が維持されている。
- `agent/author-core.md` はS00〜S99の16状態、1ターン1質問、PRM-01〜PRM-14、candidate生出力のパース前検証、動的`set_question_max`境界、M4D-07/M4D-08の対象・アンカー照合規則を保持している。

## 要約

- blocker: 0件
- major: 0件
- minor: 0件
- **判定**: 収束。コミット可（`$m-commit`）。
