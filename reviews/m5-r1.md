# M5 コードレビュー R1

- **対象差分**: `b146041bb3a78c2eb62f9fcafe1294bb63e22c4e`（M4確定コミット）から `686922f9d1c128d9d348d5c6aede6d2e1b7a2a54`（M5実装コミット）まで。レビュー開始時の作業ツリーはcleanだった。
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M5完了条件**: `IMPLEMENTATION_PLAN.md` M5 DoD
- **前回レビュー**: なし（M5初回レビュー）
- **レビュー結果**: 独立検査でmajor 4件を検出した。未解消指摘の合計はblocker 0件、major 4件、minor 0件。

## 独立検査の新規指摘

## R1-01 — major — `scripts/finalize_set.py:181`

1. **指摘内容**: FIN-01メタデータの列挙値を型検査する前にsetの所属判定へ渡しているため、JSONとしては正常だが型が不正な入力が定義済み契約エラーにならない。`format`、`mode`、`tool` をそれぞれ `[]` に置き換えた入力を `validate_metadata` へ渡すと、いずれも `TypeError: unhashable type: 'list'` になった。`level.value` も対応する `scale` が正しい状態で配列やobjectなら同じ経路に入る。`main` はこの例外を一般例外として扱うため、FIN-01が要求する `E-CONTRACT-01` のエラーJSON・終了コード1ではなく、スタックトレース・終了コード2になる。

2. **根拠となる設計文書の規則ID**: `docs/json-output-spec.md` FIN-01、`docs/architecture.md` CLI-02・CLI-05・CLI-21手順1・ERR-03、`IMPLEMENTATION_PLAN.md` M5成果物5。

3. **具体的な修正案**: `format`、`mode`、`tool` と `level.scale` / `level.value` を、所属判定より前に明示的に文字列型検査する。他のFIN-01フィールドも、値を下流処理へ渡す前に型を確定させ、不正値は全て `problems` に収集して `contract_failure("E-CONTRACT-01", ...)` で返す。各列挙値とlevel内の値について、配列・object・数値・boolean・nullを入力する回帰確認を追加し、終了コード1とCLI-05準拠のstderrだけになることを確認する。

## R1-02 — major — `scripts/set_support.py:219`

1. **指摘内容**: `validate_set_dir` は末尾のディレクトリ名と可読性しか検査せず、そのディレクトリがリポジトリの `output/` 直下か、シンボリックリンクでないかを検査していない。したがって、有効なset_id名を持つリポジトリ外の任意ディレクトリや、外部ディレクトリを指す `output/<set_id>` / `review` のシンボリックリンクを `set_check.py` と `finalize_set.py` が受理する。後者はその場所へ `set.json` を書き、CLI-22が規定する `output/<set_id>/set.json` 以外のパスも正常結果として返せる。監査の配置境界と問題データの正本位置がCLI入力だけで迂回可能になっている。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-01・CLI-01・CLI-19・CLI-21・CLI-22、`docs/subagent-review-spec.md` AU-01、`docs/json-output-spec.md` ID-07、`IMPLEMENTATION_PLAN.md` M5成果物4・5・6。

3. **具体的な修正案**: `validate_set_dir` に検証済みの `repo_root` を渡し、字句上の親が厳密に `repo_root/output`、実体解決後の親も厳密に同じ `output` である直接の子だけを受理する。`set_dir` 自身と `review/` はシンボリックリンクを拒否し、通常ディレクトリであることを確認してから監査読取・確定書込へ進む。リポジトリ外の同名ディレクトリ、`output/<set_id>` のリンク、`review` のリンクをそれぞれ拒否する回帰確認と、正常時の `set_json_path` が常にCLI-22の相対形になる確認を追加する。

## R1-03 — major — `scripts/finalize_set.py:303`

1. **指摘内容**: 確定器が要求スロット全体の終端状態や、教師が減数を選択した証跡を検証できない。現在は監査上の合格ID集合が `final_question_ids` と一致し、件数が1以上 `requested_count` 以下なら確定できる。例えば `requested_count=2` で `q01` だけが合格し、`q02` の監査が全く無い場合でも、`final_question_ids=["q01"]` とq01だけを対象にした最終set_checkがあれば通過する。また `load_audit_state` は `candidate.invalid1` だけの未完了世代も配置エラーにしないため、処理途中のスロットがあっても同じ結果になる。一方、`agent/author-core.md` の減数処理は表示とID除外だけで、RG-08が要求する各スロットの帰結と教師の減数選択を監査へ保存しない。現行ID-07/AU-02/AU-03のファイル目録にもその証跡を表す形式がなく、中断状態を正規の減数完了として確定できる。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RG-04・RG-07・RG-08・AU-02・AU-03・AU-04・AU-07、`docs/json-output-spec.md` ID-07・FIN-01・FIN-02、`docs/architecture.md` ARC-07・CLI-21手順4〜5、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5成果物3・5・6。

3. **具体的な修正案**: まずPLN-05に従い、要求スロットごとの終端結果（accepted・全世代消費による不成立・補充/代替・教師承認済み減数など）と減数選択を表す監査契約、固定ファイル名、書込タイミングを設計として確定する。オーケストレータは遷移直後にその証跡を排他的保存し、`finalize_set.py` は `requested_count` に対応する全スロットが終端し、要求数未満ならS6の教師承認が存在する場合だけ確定する。スロット欠落、`candidate.invalid1` のみ、途中世代のみ、承認なし減数を拒否し、正規の不成立後減数だけを受理する回帰確認を追加する。

## R1-04 — major — `scripts/finalize_set.py:389`

1. **指摘内容**: 原子的確定の一時ファイルを固定名 `set.json.tmp` へ通常の `open("wb")` で開くため、既存シンボリックリンクを追跡してリンク先を確定前に切り詰める。また、二度目の `set_path.exists()` と `os.replace()` の間に排他制御がなく、同じset_idへの2プロセスが両方の存在検査を通過した後、後続プロセスが先行プロセスの確定済み `set.json` を `os.replace` で上書きできる。どちらも「既存セットを保持し、上書きしない」というE-CONTRACT-05の保証を破り、固定一時名が確定対象外ファイルの破壊経路にもなる。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-07・CLI-21手順3/8・E-CONTRACT-05、`IMPLEMENTATION_PLAN.md` M5目的・成果物5。

3. **具体的な修正案**: 同一set_idの確定処理をプロセス間ロックで直列化し、ロック取得後から `set.json` 存在確認・一時書込・`os.replace` 完了まで保持する。`set.json.tmp` は `O_CREAT|O_EXCL` と利用可能なら `O_NOFOLLOW` で排他的に作成し、通常ファイルであることを確認する。例外時は自プロセスが作成した一時ファイルだけを削除し、既存一時ファイルやリンクを上書きしない。同一set_idへ同時に2回finalizeしたとき一方だけが成功して他方がE-CONTRACT-05になる確認と、事前配置した `set.json.tmp` シンボリックリンクのリンク先が不変である確認を追加する。

## 確認できた正常系・DoD再確認

- `git diff --check b146041bb3a78c2eb62f9fcafe1294bb63e22c4e..686922f9d1c128d9d348d5c6aede6d2e1b7a2a54` はpassした。
- `set_support.py`、`set_check.py`、`finalize_set.py`、`doctor.py`、`lookup.py` はPython構文検査を通過した。
- `doctor.py` は12 pass / 0 fail、終了コード0だった。
- セット横断検査は共有実装を使い、対象重複・例文使い回し・誤答ID再利用の3検査を正規化済み値から決定的に組み立てている。増分モードと最終モードの選択規則、合格世代のmachine/review/set_check三者判定も実装されている。
- レビュアー入力は固定の `review_request` 封筒と許可された8リソースに限定され、生成履歴・他問題・過去世代を渡さない指示になっている。machine failをreview passで上書きせず、review_resultの受理失敗を最大2回再試行して3失敗時にセット中止する分岐も記述されている。

## 要約

- blocker: 0件
- major: 4件
- minor: 0件
- **判定**: `$m-fix` で修正が必要。
