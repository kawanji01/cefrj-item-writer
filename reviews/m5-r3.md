# M5 コードレビュー R3

- **対象差分**: `b146041bb3a78c2eb62f9fcafe1294bb63e22c4e`（M4確定コミット）からレビュー時点の作業ツリーまで。作業ツリーのHEADは `686922f9d1c128d9d348d5c6aede6d2e1b7a2a54`（M5実装コミット）で、R1/R2対応の未コミット差分と `reviews/m5-r1*.md`・`reviews/m5-r2*.md` を含む。
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M5完了条件**: `IMPLEMENTATION_PLAN.md` M5 DoD
- **前回レビュー**: `reviews/m5-r2.md`
- **対応記録**: `reviews/m5-r2-resolutions.md`。R2時点で持ち越されていたR1-03/R1-04は `reviews/m5-r1-resolutions.md` の追記も確認した。
- **前回指摘の解消判定サマリー**: R2が追跡したR1-01〜R1-04とR2-01〜R2-04の8件は、対応記録と実差分を突き合わせ、すべて解消または解消状態の維持を確認した。R2終了時点で未解消だった6件も全件解消している。
- **レビュー結果**: 前回指摘のリグレッションはない。独立検査で新規major 5件・minor 2件を検出した。

## 前回指摘の解消判定

| 指摘 | 判定 | 確認内容 |
|---|---|---|
| R1-01 | 解消維持 | FIN-01メタデータの文字列型・列挙・件数検査は、所属判定や後続比較より前に `E-CONTRACT-01` へ集約される。前回修正に対する後続差分のリグレッションはない。 |
| R1-02 | 解消維持 | setディレクトリをリポジトリの `output/` 直下へ限定し、`output/`・set・`review/`・監査ファイルのシンボリックリンクを拒否する経路が維持されている。 |
| R1-03 | 解消 | M5D-04とAUD-11に基づくスロット終端監査が追加された。`validate_terminal_outcomes` は要求Nスロット、全試行IDの一意な所属、世代の連続性・消費状態、採用集合、教師承認済み減数を確定前に検査する。 |
| R1-04 | 解消 | `finalize_set.py` はランダム名の一時ファイルを `O_EXCL`・`O_NOFOLLOW` で排他的作成し、確定済み `set.json` を上書きできないハードリンク公開に変更された。失敗時の一時ファイル回収もある。 |
| R2-01 | 解消 | CHK-07は `grammar.json` から対象パラダイムを特定し、3誤答を同一カテゴリ内操作・代入時不成立・排除知識レベルの順で個別検査する指示になった。 |
| R2-02 | 解消 | 検証済み `generation_max` が `set_check.py` と `finalize_set.py` から監査収集へ渡され、上限を超える世代名は `E-CONTRACT-03` で拒否される。 |
| R2-03 | 解消 | review_resultの最終自己検査は、必須9フィールドの件数と全フィールド名を一致して列挙する。 |
| R2-04 | 解消 | レビュアーの直接参照はRC-10の8リソースに限定され、許可外の `question-generation-spec`・`json-output-spec`・`schemas/` を追加読取りしない指示へ統一された。 |

## 独立検査の新規指摘

## R3-01 — major — `scripts/set_support.py:549`

1. **指摘内容**: review_requestと監査正本の関係検査が、識別情報・`target_ref`・埋め込みcandidate/machine・読み取り許可一覧だけに限られ、レビュー判定の基準そのものである `level_limits` と `constraints_snapshot` を検証していない。現行スキーマは各値の型・列挙・下限だけを検査するため、例えばA1の封筒に `vocabulary_level_max=B2`、`grammar_intro_level_max=B2.2`、実設定と異なる語数・解説字数・固有名詞一覧、教師指定と異なる `topic=null` を入れてもスキーマ適合になる。CHK-03/04/19は封筒値との比較だけを行うため、上限超の文法・語義やトピック不一致をreview passにできる。`finalize_set.py` は正しい `config_snapshot` とFIN-01のtopicを受け取るが、それらを各requestへ照合せず、そのpass監査を最終採用できる。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RC-06項3・項6、CHK-03・CHK-04・CHK-19、`docs/cefrj-validation-spec.md` LVL-09〜LVL-11、`docs/json-output-spec.md` AUD-08・FIN-01・FIN-02、`IMPLEMENTATION_PLAN.md` M5成果物2・M5 DoD 2。

3. **具体的な修正案**: 監査収集時にformat/levelから `level_limits` と実効 `sentence_word_limit` / `explanation_char_limit` を決定的に再導出し、`proper_nouns` を検証済み設定スナップショット全文と比較する。finalize時には全requestのtopicもFIN-01のtopicと一致させる。不一致は封筒対応関係の破損として `E-CONTRACT-03` で停止する。A1封筒へB2/B2.2を入れる、limitを1件だけ変える、allowlistを欠落・追加する、topicをnull/別文字列へ変える各条件が、スキーマ通過後でもset_check/finalizeの確定境界で拒否される回帰確認を追加する。

## R3-02 — major — `scripts/set_support.py:647`

1. **指摘内容**: スロット終端監査は全試行IDの所属と `q01` からの連続性を検査するが、試行対象総数が要求数Nの2倍以下であることを検査しない。このため、要求数1・`generation_max=1`でq01/q02を不合格消費し、q03を採用した3対象の状態を `validate_terminal_outcomes(... requested_count=1 ...)` へ与えると例外なく受理された。これはRG-03の上限を超えた、本来到達不能な監査状態であり、オーケストレータの誤動作後でも `finalize_set.py` が正本を確定できる。RPL-10の最悪コスト境界を確定境界が立証できていない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RG-03・RG-14・RG-17、`docs/testing-and-acceptance.md` RPL-10、`docs/json-output-spec.md` AUD-11・FIN-02、`IMPLEMENTATION_PLAN.md` M5成果物3・5・M5 DoD 1。

3. **具体的な修正案**: `validate_terminal_outcomes` で、全終端監査に列挙された一意な試行ID数が `2 * requested_count` 以下であることを検査し、超過は到達不能な終端状態として `E-CONTRACT-04` にする。要求数1で3対象、要求数nで2n+1対象を拒否し、ちょうど2n対象は従来どおり受理する負例・境界例を追加する。

## R3-03 — major — `agent/author-core.md:522` / `agent/reviewer-core.md:115`

1. **指摘内容**: 文法解説へ明記する「教員版の文法項目名」の参照フィールドが、設計文書間で矛盾している。生成側の正であるGEN-22とauthor coreは `kyoinban.name_ja`（「文法項目」列）を要求する一方、レビュー側の正であるCHK-10手順2は `kyoinban.name_simple_ja`（「文法項目(平易版)」列、教師向け表示名）を要求し、reviewer coreは参照フィールドを特定していない。現行正規化データではtarget_eligibleな項目のうち223件で両値が異なるため、生成仕様どおりname_jaだけを含む解説をレビュー仕様がfailにする、またはレビュアーごとにどちらを要求するかが変わる。M5のレビュー合否を一意に実装できない設計矛盾である。

2. **根拠となる設計文書の規則ID**: `docs/question-generation-spec.md` GEN-22、`docs/subagent-review-spec.md` CHK-10手順2、`docs/cefrj-validation-spec.md` NRM-24、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5成果物1。

3. **具体的な修正案**: PLN-05で、解説に必須とする正規化フィールドを `name_ja` / `name_simple_ja` のどちらか一方へ決定する。承認後にGEN-22、CHK-10、author/reviewer coreを同じフィールド名へ揃える。両値が異なる実在 `gp:` 項目をfixtureにし、選択した名称だけを含む解説が生成・レビュー双方で合格し、もう一方だけの解説がCHK-10で不合格になることを確認する。

## R3-04 — major — `agent/reviewer-core.md:83`

1. **指摘内容**: CHK-04の集約指示がmachine reportのトークン表と個別Wordlist語の語義・用法だけを対象にし、仕様がこのチェックへ割り当てた「個々は易しい語でも、語連鎖として上限超になる複数語表現・イディオム・句動詞」の検査を欠いている。lexiconに複数語見出しがない表現を `reviewer_estimate` で判定する手順もない。そのため、各単語は上限内だが連鎖の意味理解に上位レベルを要する例文をmachineとreviewの両方がpassにできる。とくに⑧について元文・目標文の双方へ適用する範囲も明記されていない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-04手順1〜3、`docs/cefrj-validation-spec.md` MAT-02、`IMPLEMENTATION_PLAN.md` M5成果物1・M5 DoD 2。

3. **具体的な修正案**: reviewer coreのCHK-04へ、対象語以外の全語と全語連鎖を通読し、複数語見出しは `lex:` IDのレベル、未収録のイディオム等は推定レベルと根拠で判定する手順を集約する。⑧は元文・目標文の双方が対象であることも明記する。個別語は上限内だが連鎖が上限超の例、未収録イディオムの推定例、⑧の元文だけに上限超表現がある例でCHK-04 failになることを確認する。

## R3-05 — major — `agent/reviewer-core.md:137`

1. **指摘内容**: CHK-16の集約指示は、指示の明示、元文に対象構造がないこと、目標文に対象構造があること、意味関係の保持を確認するが、目標文の空欄が書き換えの核心部分を覆うという必須条件を欠いている。例えば対象受動態が完成目標文の別位置に存在し、空欄は無関係な名詞だけを問うcandidateでも、現行CHK-01/16の文言上は対象構造の実現と書き換え関係を満たしてpassし得る。機械検査MC-26は元文と完成目標文が異なることだけを検査するため、この欠落を補わない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-16手順4、`docs/cefrj-validation-spec.md` MC-26・MAT-01 #19・MAT-02、`IMPLEMENTATION_PLAN.md` M5成果物1・M5 DoD 2。

3. **具体的な修正案**: reviewer coreのCHK-16へ、`target_sentence_with_blank` の空欄位置が対象文法操作の実現スパン内部にあり、無関係な語彙・周辺要素だけを問う場合はfailとする手順を追加する。対象構造は目標文にあるが空欄が無関係な例を負例にし、核心部分を空欄にした例とのCHK-16判定差を確認する。

## R3-06 — minor — `agent/author-core.md:673`

1. **指摘内容**: M5D-03とRG-01により `generation_max` は1〜3の設定値として実装され、1または2ではその世代で打ち切る一方、author coreは不成立時にDLG-81/DLG-82とFMT-80b事象9/10を文言どおり使うよう指示する。これらの固定文言は常に「3世代不合格」「gen1〜gen3」「新しい対象で最大3世代」と表示するため、`generation_max=1|2` の正常セッションで未実行世代・存在しない監査ファイルを教師へ案内する。実際の停止制御は上限値に従っていても、対話ログと監査参照が事実と食い違う。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RG-01、`docs/interaction-flow.md` IF-42（FMT-80b事象9・10）・DLG-81・DLG-82、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5成果物3。

3. **具体的な修正案**: PLN-05で、これらのテンプレートを検証済み `generation_max` と実際の最終世代でパラメータ化する設計を承認し、interaction-flowとauthor coreを同時に改訂する。上限1・2・3の各条件で、世代数、列挙するgen行、監査ファイル範囲、代替対象の最大世代が実行実績と一致することを確認する。

## R3-07 — minor — `agent/author-core.md:547`

1. **指摘内容**: `machine_check_disputes[]` は次世代の再生成入力へ渡す指示だけがあり、セット全体で件数を追跡し、完了または中止報告へFP-04の固定文言を含める指示がない。FMT-90とIF-52の固定様式にも当該行がなく、author coreは中止時にそれらへ従うため、誤検出疑いを監査に保存しても教師へ必要な件数通知を行わずに終了する。これは監査から機械検査改善を開始するための必須導線を失わせる。なおM5成功時はM6までS90へ遷移しないが、M5中のS99終了ではすでに再現する。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` FP-04、`docs/interaction-flow.md` IF-50（FMT-90）・IF-52、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5成果物1・3。

3. **具体的な修正案**: PLN-05でFP-04の行をFMT-90/IF-52のどの位置へ追加するか決定し、author coreに全受理review_resultの非空dispute件数を累積する指示と、完了・中止時に「機械検査誤検出疑い n件（詳細は監査ファイル参照）」を出す指示を追加する。複数問題・複数世代のdisputeを重複なく合算し、0件時の表示有無も決定したうえで、完了とS99の双方を確認する。

## 確認できた正常系・DoD再確認

- `git diff --check b146041bb3a78c2eb62f9fcafe1294bb63e22c4e` はpassした。
- `scripts/doctor.py` は12 pass / 0 fail、終了コード0だった。関連Pythonスクリプトは構文検査を通過した。
- R1/R2対応として、スロット終端監査、上書き不能な原子的公開、`generation_max`超過監査の拒否、CHK-07、review_resultの9フィールド自己検査、RC-10の読み取り境界が実差分どおり機能していることを確認した。
- machine/review/set_checkの三者がpassした世代だけを採用する合成規則、candidate・machine・requestの主要識別情報、最終setの内部再検査とスキーマ検証は維持されている。
- 負例確認では、review_requestスキーマがA1に対するB2/B2.2上限と不一致な制約値を `valid: true` とし、`validate_terminal_outcomes` がN=1に対する3試行対象を受理することを再現した。
- 正規化文法データでは、target_eligibleかつ `kyoinban.name_ja != kyoinban.name_simple_ja` の項目が223件あることを確認した。

## 要約

- blocker: 0件
- major: 5件
- minor: 2件
- **判定**: `$m-fix` で修正が必要。
