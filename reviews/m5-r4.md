# M5 コードレビュー R4

- **対象差分**: `b146041bb3a78c2eb62f9fcafe1294bb63e22c4e`（M4確定コミット）からレビュー時点の作業ツリーまで。作業ツリーのHEADは `686922f9d1c128d9d348d5c6aede6d2e1b7a2a54`（M5実装コミット）で、R1〜R3対応の未コミット差分と `reviews/m5-r1*.md`〜`reviews/m5-r3*.md` を含む。
- **レビュー日**: 2026-08-17
- **仕様の正**: `docs/`、`schemas/`
- **M5完了条件**: `IMPLEMENTATION_PLAN.md` M5 DoD
- **前回レビュー**: `reviews/m5-r3.md`
- **対応記録**: `reviews/m5-r3-resolutions.md`
- **前回指摘の解消判定サマリー**: R3-01とR3-02は実差分で解消を確認した。R3-03〜R3-07は対応記録に記載がなく、対象箇所も未変更のため未解消である。未解消5件はR4-01〜R4-03、R4-06、R4-07として再掲する。
- **レビュー結果**: 前回未解消のmajor 3件・minor 2件に加え、独立検査で新規major 2件を検出した。

## 前回指摘の解消判定

| 指摘 | 判定 | 確認内容 |
|---|---|---|
| R3-01 | 解消 | `load_audit_state` がformat/levelから `level_limits` を再導出し、実効limitsと固有名詞allowlistを設定スナップショットへ照合するようになった。`finalize_set.py` も全requestのtopicをFIN-01メタデータへ照合する。 |
| R3-02 | 解消 | `validate_terminal_outcomes` が一意な試行question_id数を `2 * requested_count` 以下に制限し、超過を `E-CONTRACT-04` で拒否する。 |
| R3-03 | 未解消 | author coreは引き続き `kyoinban.name_ja` を要求し、レビュー仕様の `name_simple_ja` との設計矛盾が残っている。R4-01として再掲する。 |
| R3-04 | 未解消 | reviewer coreのCHK-04は、複数語表現・イディオム・句動詞、未収録表現の推定、および⑧の両文適用を依然として指示していない。R4-02として再掲する。 |
| R3-05 | 未解消 | reviewer coreのCHK-16は、目標文の空欄が対象文法操作の核心部分を覆うことを依然として検査しない。R4-03として再掲する。 |
| R3-06 | 未解消 | `generation_max=1|2` でも、開始表示・不成立表示・教師照会が固定の「3世代」「gen1〜gen3」のままである。R4-06として再掲する。 |
| R3-07 | 未解消 | dispute件数の累積と完了・中止報告への必須文言が追加されていない。R4-07として再掲する。 |

## 指摘

## R4-01 — major — `agent/author-core.md:522` / `agent/reviewer-core.md:115`

1. **指摘内容**: 文法解説へ明記する教員版項目名が、生成側では `kyoinban.name_ja`、レビュー側の正では教師向け表示名の `kyoinban.name_simple_ja` とされ、同じ候補の合否を一意に決められない。reviewer coreも「教員版項目名」とだけ記し、どちらを照合するか特定していない。正規化データでは両値が異なるtarget_eligible項目が多数あるため、生成仕様どおりの解説をCHK-10が不合格にし得る。R3-03から未解消である。

2. **根拠となる設計文書の規則ID**: `docs/question-generation-spec.md` GEN-22、`docs/subagent-review-spec.md` CHK-10手順2、`docs/cefrj-validation-spec.md` NRM-24、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5成果物1。

3. **具体的な修正案**: PLN-05で解説に必須とするフィールドを `name_ja` / `name_simple_ja` の一方へ決定し、GEN-22、CHK-10、author/reviewer coreを同じフィールド名へ揃える。両値が異なる実在 `gp:` 項目を使い、選択した名称を含む場合だけCHK-10が合格することを確認する。

## R4-02 — major — `agent/reviewer-core.md:83`

1. **指摘内容**: CHK-04の集約指示はmachine reportのトークン表と個別Wordlist語の語義・用法だけを対象にし、個々の語は上限内でも語連鎖として上限超になる複数語表現・イディオム・句動詞を検査しない。lexicon未収録表現を `reviewer_estimate` で判定する手順も、`grammar_rewrite` の元文・目標文双方へ適用する範囲も欠けている。このため機械検査で補えない上位表現をreview passにできる。R3-04から未解消である。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-04手順1〜3、`docs/cefrj-validation-spec.md` MAT-02、`IMPLEMENTATION_PLAN.md` M5成果物1・M5 DoD 2。

3. **具体的な修正案**: reviewer coreへ、対象語以外の全語と語連鎖を通読し、複数語見出しは `lex:` ID、未収録表現は推定レベルと根拠で判定する手順を追加する。⑧は元文・目標文の双方が対象であることを明記し、個別語だけなら上限内のイディオム、未収録句動詞、元文だけに上限超表現がある各負例を確認する。

## R4-03 — major — `agent/reviewer-core.md:137`

1. **指摘内容**: CHK-16の集約指示は、書き換え指示、元文・目標文の対象構造、意味関係を確認する一方、目標文の空欄が書き換えの核心部分を覆う必須条件を含まない。対象構造が完成目標文の別位置に存在し、空欄が無関係な名詞だけを問う候補でも、現行指示ではCHK-01/16を通過し得る。MC-26は両完成文の非同一しか検査しないため補完できない。R3-05から未解消である。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-16手順4、`docs/question-generation-spec.md` GEN-26、`docs/cefrj-validation-spec.md` MC-26・MAT-01 #19・MAT-02、`IMPLEMENTATION_PLAN.md` M5成果物1・M5 DoD 2。

3. **具体的な修正案**: reviewer coreのCHK-16へ、`target_sentence_with_blank` の空欄が対象文法操作の実現スパンを覆い、無関係な要素だけを問う場合はfailとする手順を追加する。対象構造は目標文にあるが空欄が無関係な負例と、核心部分を空欄にした正例の判定差を確認する。

## R4-04 — major — `scripts/set_support.py:884`

1. **指摘内容**: `build_set_report` は `machine.stats.texts[]` の全レコードを例文再利用検査へ投入しており、MC-07の「語として検査」対象も `V-SET-02` の例文として数える。`machine_check.py` は `vocab_mcq_ja2en` の `body.choices[*].text` と `grammar_cloze` の `body.answer_equivalents[*]` も同配列へ記録するため、異なる例文を持つ2問が同じ誤答語を2回使っただけでセットが不合格になる。`distractor_reuse_max=2` の条件で、異なる完成文を持つ2問に同じ誤答entryを各1回置く入力を `build_set_report` へ与えると、許容上限内で `V-SET-03` は出ない一方、語形を根拠とする `V-SET-02` が再現した。これは有効なセットを確定不能にし、CI-SET-02/03の担当分界を壊す。

2. **根拠となる設計文書の規則ID**: `docs/cefrj-validation-spec.md` MC-07・MC-27（`V-SET-02` / `V-SET-03`）、`docs/testing-and-acceptance.md` CI-SET-02・CI-SET-03、`IMPLEMENTATION_PLAN.md` M5成果物4・M5 DoD 4。

3. **具体的な修正案**: candidateのformatと `stats.texts[].field` をMC-07の表へ照合し、「文として検査」するフィールドだけを `sentence_to_questions` へ追加する。少なくとも、同じ誤答entryを上限内で2問に使う例と、同じ `answer_equivalents` を2問に含む例が `V-SET-02` にならず、実際の完成文・例文が一致する例だけが `V-SET-02` になる回帰確認を追加する。

## R4-05 — major — `agent/reviewer-core.md:79`

1. **指摘内容**: CHK-03の集約指示は「全文法構造」を列挙するとだけ記し、`grammar_rewrite` では元文と目標文の双方へ列挙・レベル比較を適用するという必須手順を欠いている。目標文は上限内でも元文だけに上限超構造がある候補で、元文を「例文」の対象外と解釈するとCHK-03をpassにできる。機械検査は文法構造の同定・レベル判定を行わないため、この欠落を補えない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-03手順1〜4（特に手順4）、`docs/cefrj-validation-spec.md` MC-25・MAT-02、`IMPLEMENTATION_PLAN.md` M5成果物1・M5 DoD 2。

3. **具体的な修正案**: reviewer coreのCHK-03へ、⑧では元文と目標完成文の双方について全構造を列挙し、同じ `grammar_intro_level_max` と比較し、どちらか一方でも超過すればfailとする手順を明記する。元文だけ、目標文だけ、両方に上限超構造がある3負例で、インベントリとviolationの対象スパンが正しく記録されることを確認する。

## R4-06 — minor — `agent/author-core.md:410` / `docs/interaction-flow.md:550`

1. **指摘内容**: `generation_max` は1〜3の設定値であり、1または2なら対応する最終世代で打ち切る一方、開始表示、FMT-80b事象9/10、DLG-81/DLG-82は常に「最大3世代」「3世代不合格」「gen1〜gen3」と表示する。author coreはこれらを固定文言どおり使うため、正常な `generation_max=1|2` セッションで未実行世代と存在しない監査ファイルを教師へ案内する。R3-06から未解消である。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RG-01、`docs/interaction-flow.md` IF-41・IF-42（FMT-80b事象9・10）・DLG-81・DLG-82、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5成果物3。

3. **具体的な修正案**: PLN-05でテンプレートの世代数・最終世代・監査範囲を検証済み `generation_max` からパラメータ化する設計を決定し、interaction-flowとauthor coreを同期する。上限1・2・3の各条件で表示上の世代数、列挙するgen行、監査ファイル範囲、代替対象の最大世代が実績と一致することを確認する。

## R4-07 — minor — `agent/author-core.md:547` / `docs/interaction-flow.md:626`

1. **指摘内容**: `machine_check_disputes[]` は直後の再生成入力へ渡す指示しかなく、セット全体で件数を累積し、完了または中止報告へFP-04の固定文言を含める指示がない。FMT-90とIF-52にも当該行がないため、誤検出疑いが監査に残っても教師へ件数を通知せず終了する。R3-07から未解消である。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` FP-04、`docs/interaction-flow.md` IF-50（FMT-90）・IF-52、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5成果物1・3。

3. **具体的な修正案**: PLN-05でFP-04の行をFMT-90/IF-52のどこへ追加するか決定し、author coreへ全受理review_resultのdispute件数を累積する指示を加える。完了・中止の双方で「機械検査誤検出疑い n件（詳細は監査ファイル参照）」を出し、複数問題・複数世代の合算と0件時の表示規則を確認する。

## 確認できた正常系・DoD再確認

- `git diff --check b146041bb3a78c2eb62f9fcafe1294bb63e22c4e` はpassした。
- `.venv/bin/python -m py_compile scripts/*.py` はpassした。
- `.venv/bin/python scripts/doctor.py` は12 pass / 0 fail、終了コード0だった。
- R3-01対応のレベル上限・limits・allowlist・topic照合と、R3-02対応の `2 * requested_count` 上限検査が実差分に存在し、前回の欠落を塞いでいることを確認した。
- machine/review/set_checkの三者がpassした世代だけを採用する合成規則、監査上書き防止、最終setの内部再検査とスキーマ検証は維持されている。
- 新規負例では、異なる完成文を持つ2問で同じ誤答entryを許容上限2回使っただけで、`build_set_report` が `V-SET-02` を返すことを再現した。

## 要約

- blocker: 0件
- major: 5件
- minor: 2件
- **判定**: `$m-fix` で修正が必要。
