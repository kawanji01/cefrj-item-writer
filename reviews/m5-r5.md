# M5 コードレビュー R5

- **対象差分**: `b146041bb3a78c2eb62f9fcafe1294bb63e22c4e`（M4確定コミット）からレビュー時点の作業ツリーまで。作業ツリーのHEADは `686922f9d1c128d9d348d5c6aede6d2e1b7a2a54`（M5実装コミット）で、R1〜R4対応の未コミット差分と `reviews/m5-r1*.md`〜`reviews/m5-r4*.md` を含む。
- **レビュー日**: 2026-08-18
- **仕様の正**: `docs/`、`schemas/`
- **M5完了条件**: `IMPLEMENTATION_PLAN.md` M5 DoD
- **前回レビュー**: `reviews/m5-r4.md`
- **対応記録**: `reviews/m5-r4-resolutions.md`
- **前回指摘の解消判定サマリー**: R4-01〜R4-07の7件は、対応記録と実差分を突き合わせ、全件の解消を確認した。
- **レビュー結果**: 前回未解消は0件。独立検査で新規major 3件を検出した。

## 前回指摘の解消判定

| 指摘 | 判定 | 確認内容 |
|---|---|---|
| R4-01 | 解消 | 承認済みM5D-06が `docs/question-generation-spec.md` GEN-22、`docs/subagent-review-spec.md` CHK-10、author/reviewer coreへ反映され、解説の必須名が `kyoinban.name_ja` に統一された。 |
| R4-02 | 解消 | reviewer coreのCHK-04が、全語・全語連鎖、複数語見出し、未収録表現の推定、および⑧の元文・目標完成文双方を検査するようになった。 |
| R4-03 | 解消 | reviewer coreのCHK-16が、目標完成文の対象文法操作の実現スパンを特定し、空欄がその核心部分を覆わない候補をfailにするようになった。 |
| R4-04 | 解消 | `build_set_report` がMC-07で「文として検査」するformat別フィールドだけを `V-SET-02` へ取り込むようになった。異なる完成文と共通誤答語はpass、正規化後に同一の完成文は `V-SET-02` となることをインメモリ入力で確認した。 |
| R4-05 | 解消 | reviewer coreのCHK-03が、⑧の元文と目標完成文を別々に全構造列挙・上限比較し、超過した文とスパンを記録するようになった。 |
| R4-06 | 解消 | 承認済みM5D-07がinteraction-flowとauthor coreへ反映され、開始表示、不成立表示、教師照会の世代数・最終世代・監査範囲が検証済み `generation_max` から展開されるようになった。 |
| R4-07 | 解消 | 承認済みM5D-08がinteraction-flow、subagent review spec、author coreへ反映され、保存成功したreview_resultのdispute要素を一度だけ累積し、S80開始後の完了・中止報告へ0件時も固定行を出す契約になった。 |

## 指摘

## R5-01 — major — `agent/author-core.md:547` / `agent/author-core.md:618`

1. **指摘内容**: PRM-12は `gen2/gen3` の生成入力へ、無条件に直前世代のcandidate・machine_report・review_resultを含めるよう指示している。一方、candidate受理検証が同一世代で2回失敗したT3では、仕様どおり `candidate.invalid1/2.txt` だけが残り、正規のcandidate・machine・request・review監査は存在しない。author core自身も618行でT3後に次世代へ進ませるため、例えばgen1がcandidate不正2回、gen2が妥当というRPL-06経路では、gen2の入力構築時に存在しない3監査を要求する。これにより、正規のT3遷移をインフラ停止にするか、PRM-12/AU-07のどちらかを破らなければ先へ進めない。DLG-81の監査案内も全世代の `review.json` が存在する前提であり、T3だけで消費された世代では存在しないパスを教師へ示す。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` T3・RG-05・RG-09・AU-07、`docs/testing-and-acceptance.md` RPL-06、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5 DoD 1。

3. **具体的な修正案**: PRM-12をT8/T11後の再生成だけへ明示的に限定し、T3後の次世代入力を別分岐にする。T3後に渡す診断内容が設計上未定義ならPLN-05で決定したうえで、少なくとも元の対象・確定条件・生成制約と2回目のcandidate受理診断から新しいcandidateを生成し、存在しない正規監査を要求しない。進捗行とDLG-81の世代別要約・参照先もT3では `candidate.invalid1/2.txt` を示す。gen1のcandidateが2回不正、gen2が妥当で合格するRPL-06リプレイを実際の入力組み立てまで確認する。

## R5-02 — major — `agent/author-core.md:673` / `docs/interaction-flow.md:606`

1. **指摘内容**: 提案モードのDLG-82は、補充プール枯渇または試行上限到達時に「確定済みのm問でセットを完成させる」を選ぶと、残りの初期対象を処理せず最終set_checkへ進ませる。しかし処理は逐次で補充対象をキュー先頭へ入れるため、補充枯渇時点で後続の初期スロットが未処理のまま残り得る。例えばN=3でq01を採用し、q02とその補充対象が全て不成立になってプールが尽きると、q03はまだ未着手である。ここで教師が1問での完成を選ぶと、author coreが書くのは当該スロットq02のreduced終端監査だけで、q03には世代監査も終端監査もないため、`finalize_set.py` は要求3スロット不足を `E-CONTRACT-04` で拒否する。m=0では続行選択肢自体が消えるため、未着手の後続初期対象が合格し得てもセット中止しか選べない。

2. **根拠となる設計文書の規則ID**: `docs/interaction-flow.md` DLG-82、`docs/subagent-review-spec.md` RG-02・S2・S6・RG-08・RG-12〜RG-14、`docs/json-output-spec.md` AUD-11、`docs/architecture.md` CLI-21手順5、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5 DoD 1。

3. **具体的な修正案**: DLG-82の続行を「現在の論理スロットを教師承認済み減数として終端し、補充なしで残りの初期対象を順番に処理する」に変更し、現在スロットのAUD-11を保存した後も未処理キューを維持する。全初期スロットがaccepted/reducedで終端し、確定問題が1件以上になった時点だけ最終set_checkへ進む。未処理スロットを一括放棄して早期完成させる設計を採るなら、各未処理スロットの教師承認と終端証跡を表せる新契約をPLN-05で先に確定する。N=3以上で中間スロットの補充が枯渇する正負例を追加する。

## R5-03 — major — `scripts/set_support.py:454` / `scripts/set_support.py:671`

1. **指摘内容**: `load_audit_state` はcandidate/reviewのinvalid監査をファイル名だけで分類し、内容を一度も読み取らずに連番集合へ追加する。`generation_terminal_status` は `candidate.invalid2` という名前の存在だけで当該世代をT3消費済みとするため、空ファイル、非UTF-8、AUD-09の区切り・診断を持たない任意バイト列の `candidate.invalid1/2.txt` でも正当な世代消費の証跡になる。その後の世代がT10採用されるか教師が減数を承認すれば、`finalize_set.py` はAUD-09不適合の監査を含むセットを確定できる。正規JSON監査とAUD-11は内容検証している一方、T3を立証する唯一の監査だけが無検証であり、終端検査が部分書込み・破損監査をfail-closedにできていない。

2. **根拠となる設計文書の規則ID**: `docs/json-output-spec.md` AUD-09・AUD-11、`docs/subagent-review-spec.md` AU-03・AU-07、`docs/architecture.md` CLI-21手順5・E-CONTRACT-03、`docs/testing-and-acceptance.md` RPL-06、`IMPLEMENTATION_PLAN.md` M5成果物6・M5 DoD 1。

3. **具体的な修正案**: `load_audit_state` で全invalid監査を読み、AUD-09の通常形とUTF-8化不能・プロセス失敗時の例外形を区別して、UTF-8・非空診断・区切り/必要情報を検証する。内容形式に反するファイルはT3/T6/T7の証跡へ数えず、対象ファイル名を列挙した `E-CONTRACT-03` で停止する。空・非UTF-8・区切り欠落・診断欠落を拒否し、正規のcandidate.invalid1/2だけで次世代消費を立証できる回帰確認を追加する。

## 確認できた正常系・DoD再確認

- `git diff --check b146041bb3a78c2eb62f9fcafe1294bb63e22c4e` はpassした。
- `scripts/*.py` は全件を `ast.parse` し、構文解析がpassした。
- R4-01〜R4-07は対応記録と実差分が一致し、前回指摘の対象箇所はいずれも解消されている。
- R4-04の回帰入力では、異なる完成文を持つ2問が同じ誤答語を上限内で共有してもset reportはpassし、正規化後に同一の完成文を持つ2問だけが `V-SET-02` になった。
- machine/review/set_checkの三者がpassした世代だけを採用する合成、監査の上限超過世代拒否、最終set reportと内部再検査の一致、上書き不能なset.json公開は維持されている。
- 一方、RPL-06のT3→次世代入力、提案モードで後続初期スロットを残した補充枯渇、AUD-09不適合invalid監査の拒否は、現状の指示・決定的検査では成立しない。

## 要約

- blocker: 0件
- major: 3件
- minor: 0件
- **判定**: `$m-fix` で修正が必要。
