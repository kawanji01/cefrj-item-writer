# M5 コードレビュー R6

- **対象差分**: `b146041bb3a78c2eb62f9fcafe1294bb63e22c4e`（M4確定コミット）からレビュー時点の作業ツリーまで。作業ツリーのHEADは `686922f9d1c128d9d348d5c6aede6d2e1b7a2a54`（M5実装コミット）で、R1〜R5対応の未コミット差分と `reviews/m5-r1*.md`〜`reviews/m5-r5*.md` を含む。
- **レビュー日**: 2026-08-18
- **仕様の正**: `docs/`、`schemas/`
- **M5完了条件**: `IMPLEMENTATION_PLAN.md` M5 DoD
- **前回レビュー**: `reviews/m5-r5.md`
- **対応記録**: `reviews/m5-r5-resolutions.md`
- **前回指摘の解消判定サマリー**: R5-01〜R5-03の3件は、対応記録と実差分を突き合わせ、指摘された主経路の解消を確認した。
- **レビュー結果**: 前回未解消は0件。独立検査で新規major 4件、minor 2件を検出した。

## 前回指摘の解消判定

| 指摘 | 判定 | 確認内容 |
|---|---|---|
| R5-01 | 解消 | PRM-12とRG-09/RG-09aがT3とT8/T11を分岐し、T3後は2回目のcandidate受理診断だけを次世代へ渡す。DLG-81もT3では`candidate.invalid1/2.txt`だけを案内し、存在しない正規4監査を要求しない。 |
| R5-02 | 解消 | DLG-82の続行は現在スロットだけを`reduced`で終端し、未処理の初期スロットを補充なしで元の順序に処理する。author coreの最終確定条件も、全Nスロットの終端監査と1問以上の確定を要求する。 |
| R5-03 | 解消 | `load_audit_state`は全candidate/review invalid監査をstrict UTF-8で読み、AUD-09の標準・UTF-8符号化失敗・プロセス失敗の各形を検査してから連番集合へ加える。空・非UTF-8・区切り/診断欠落は`E-CONTRACT-03`となり、名前だけでは世代消費を立証できない。 |

## 指摘

## R6-01 — major — `scripts/finalize_set.py:223` / `scripts/finalize_set.py:234` / `scripts/finalize_set.py:478`

1. **指摘内容**: FIN-01の`topic`と`model`は型・非空だけを検査するため、UTF-8の入力JSON内でエスケープされた孤立サロゲート（例: `"topic":"\ud800"`）を受理する。`validate_metadata`が成功した同じ値を`canonical_bytes`へ渡すと、strict UTF-8化で`UnicodeEncodeError`になる。この例外は一時ファイル作成後の書込み中に発生し、`CliFailure`/`OSError`のどちらにも捕捉されないため、CLIは定義済み`E-CONTRACT-01`・終了コード1ではなくスタックトレース・終了コード2となり、作成済みのランダム一時ファイルも残る。FIN-01の内容不正が未定義内部エラーと確定処理の残留物へ変わる。

2. **根拠となる設計文書の規則ID**: `docs/json-output-spec.md` JS-01・FIN-01・FIN-02、`docs/architecture.md` CLI-02・CLI-04・CLI-21手順1/8、`IMPLEMENTATION_PLAN.md` PLN-08・M5成果物5・M5 DoD 4。

3. **具体的な修正案**: FIN-01の全文字列を再帰的にstrict UTF-8へ符号化可能か、ファイル操作より前に検証し、不成立を内容不正の`E-CONTRACT-01`で返す。加えて、完成した`set_document`の正準バイト列を一時ファイル作成前に構築し、ファイル作成後に起きる例外では自プロセスの一時ファイルを必ず回収する。`topic`・`model`へescaped lone surrogateを入れたCLI回帰で、終了コード1、CLI-05エラーJSON、`set.json`も一時ファイルも0件を確認する。

## R6-02 — major — `scripts/set_support.py:353`

1. **指摘内容**: AUD-09の3形式の判別が先頭行だけに依存するため、正規の標準形と例外形が衝突する。例えば生成生出力の全文が先頭行`kind: utf8_encode_failure`または`kind: process_failure`で始まり、その後に正規の区切り行と非空診断を連結したファイルはAUD-09の標準形の文言を満たすが、`validate_invalid_audit`は先頭行を例外形の識別子として先に解釈し、「固定3行ではない」「kind/exit_code/stderr固定形ではない」と拒否する。同様に、生出力または診断自身が区切り行と同じ1行を含む場合も、全文保存と「区切り行ちょうど1個」を両立できない。LLM生出力は任意テキストなので、この衝突はcandidate/reviewの再試行が後で成功しても残存invalid監査を`E-CONTRACT-03`にし、正常な確定を妨げる。

2. **根拠となる設計文書の規則ID**: `docs/json-output-spec.md` AUD-09・AUD-10、`docs/subagent-review-spec.md` T2/T3・T6/T7・INF-04・AU-03・AU-07、`docs/testing-and-acceptance.md` RPL-05・RPL-06、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5 DoD 1/5。

3. **具体的な修正案**: PLN-05で、生出力の任意内容と衝突しない機械判別形式を確定する。例えば全3形に固定ヘッダーを持つ長さ付き封筒を採用するか、生出力・診断を長さ付きまたはbase64のフィールドとして保持し、予約行による区切りを廃止する。writerとreaderを同時に更新し、予約`kind`行始まり、区切り行を含む生出力、区切り行を含む診断、通常3形の正負例を追加する。生出力全文を欠落・改変させないこともバイト比較する。

## R6-03 — major — `docs/interaction-flow.md:610` / `agent/author-core.md:678`

1. **指摘内容**: M5D-09/RG-16は全ての不成立照会で世代ごとの実際の終端経路と実在監査を示す契約になったが、反映されたのはDLG-81だけである。提案モードのDLG-82は依然として各対象を「主な指摘: `violations[].code`」だけで表示し、author coreも経路別のT3/T8/T11要約をDLG-81にだけ指示する。candidate受理失敗だけで全世代をT3消費した対象には`review_result.violations[]`も正規4監査も存在しないため、RPL-06を含む提案モードが補充枯渇・2N到達後にDLG-82へ入ると、固定テンプレートを埋められないか、存在しないレビュー指摘を捏造しなければならない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` T3・RG-16・AU-07、`docs/interaction-flow.md` DLG-82、`DECISIONS.md` M5D-09、`docs/testing-and-acceptance.md` RPL-03・RPL-06・RPL-10、`IMPLEMENTATION_PLAN.md` PLN-03・M5 DoD 1。

3. **具体的な修正案**: DLG-82もDLG-81と同じく、対象ごと・実行世代ごとにT3/T8/T11、診断または違反要約、実在監査パスを表示するテンプレートへ変更し、author coreの経路別案内を両モードへ適用する。提案モードでT3のみ、T3/T8混在、T11混在の対象が補充枯渇または2N到達へ進むリプレイを追加し、存在しない`review.json`を一度も案内しないことを確認する。

## R6-04 — major — `scripts/finalize_set.py:481` / `scripts/finalize_set.py:503` / `agent/author-core.md:700`

1. **指摘内容**: `os.link`で`set.json`を公開した後の一時リンク削除が失敗すると、`remove_temp`は`E-ENV-05`を送出するが、公開済み`set.json`は上書き禁止のためロールバックされない。CLIは終了コード1を返し、author coreは全ての定義済み停止をS99へ送り、IF-52の「set.jsonは書き込まれていない」と報告する。一方、実体は有効な`set.json`が既に存在し、再実行も`E-CONTRACT-05`になる。これにより、確定済み正本とCLI/教師向け状態が食い違い、利用者が成功・失敗を回復不能な形で誤認する。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-07・CLI-02・CLI-21手順8・E-ENV-05・E-CONTRACT-05、`docs/subagent-review-spec.md` RG-07・S9/S10、`docs/interaction-flow.md` IF-52、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M5成果物5・M5 DoD 4。

3. **具体的な修正案**: PLN-05で公開成功後のcleanup失敗を表す回復契約を確定し、少なくとも公開済み正本を「未作成」のS99へ送らない。実装は公開前失敗と公開後失敗を区別し、後者では`set.json`を削除・上書きせず、正本が完成済みであることと残留一時リンクの対処を明示できる状態/エラーにするか、cleanup失敗を外部エラーにしない安全な確定プロトコルへ改める。`os.link`成功後の`unlink`だけを失敗させた回帰で、CLI結果、`set.json`の存在・内容、教師向け報告、再実行時の案内が矛盾しないことを確認する。

## R6-05 — minor — `agent/author-core.md:404` / `scripts/finalize_set.py:227`

1. **指摘内容**: SET-01は`created_at`をset_id採番と同時に取得する値と定めるが、author coreのS80開始処理はset_idだけを採番し、`created_at`の同時取得・保持を指示していない。さらに`validate_metadata`は日時を正規表現でしか検査せず、set_idのローカル日時部分との一致も実在日時・UTC offsetとしての妥当性も検査しない。実際に`set_id=20260818-120000-abcd`と`created_at=2026-08-17T01:02:03+09:00`の組は受理され、相互矛盾する作成日時を正本へ保存できる。

2. **根拠となる設計文書の規則ID**: `docs/json-output-spec.md` ID-01・SET-01・FIN-01・FIN-02、`docs/architecture.md` CLI-21手順1、`IMPLEMENTATION_PLAN.md` PLN-03・M5成果物5。

3. **具体的な修正案**: author coreでset_id採番時に同じローカル日時から秒精度・timezone付き`created_at`を同時取得し、FIN-01まで不変保持する。finalize側でもISO 8601値を実日時として解析し、そのローカル年月日時がset_id先頭14桁と一致することを`E-CONTRACT-01`境界で検証する。日付不一致、不可能日付、不正offsetの負例を追加する。

## R6-06 — minor — `scripts/set_support.py:178`

1. **指摘内容**: `contract_failure`は問題を先頭50件へ切り詰め、`detail.problems`にも50件しか格納しない。E-CONTRACT-03のエラー目録は不整合または欠落した期待ファイル名を全件列挙することを要求するため、最大20問×3世代の監査で51件以上の配置・対応不整合があると、利用者は残りの対象ファイル名をエラーJSONから取得できない。51件を渡した直接確認でも`total=51`に対し列挙名は50件だった。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` CLI-05・E-CONTRACT-03、`IMPLEMENTATION_PLAN.md` PLN-08・M5成果物4/5。

3. **具体的な修正案**: 少なくともE-CONTRACT-03では`detail.problems`へ全件を決定的順序で保持し、エラー目録どおり全ファイル名を取得可能にする。messageを要約する場合も、全件がdetailにあることを明示する。51件および最大監査数の負例で、全対象名・総数・順序が欠落しないことを確認する。

## 確認できた正常系・DoD再確認

- `git diff --check b146041bb3a78c2eb62f9fcafe1294bb63e22c4e` はpassした。
- `.venv/bin/python -m py_compile scripts/*.py` はpassした。
- `.venv/bin/python scripts/doctor.py` は12 pass / 0 failだった。
- R5-01〜R5-03の主修正は対応記録と実差分が一致し、T3入力分岐、未処理初期スロット継続、invalid内容検証が追加されている。
- AUD-09の予約kind行を生成生出力として標準形へ入れた直接確認では、2種類とも例外形として誤分類され拒否された。
- FIN-01の`topic`へescaped lone surrogateを入れた直接確認では、`validate_metadata`通過後の正準UTF-8化が`UnicodeEncodeError`になった。
- set_idと異なる有効書式の`created_at`は`validate_metadata`に受理され、E-CONTRACT-03の51問題はdetailへ50件しか保持されないことを確認した。

## 要約

- blocker: 0件
- major: 4件
- minor: 2件
- **判定**: `$m-fix` で修正が必要。
