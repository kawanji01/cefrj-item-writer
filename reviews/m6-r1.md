# M6 コードレビュー R1

- **対象差分**: `4b4e1da07895897d8ef3cc87ae74eb424811179d`（M5確定コミット）から `626d411c34ed153f0f9a428c141ce5c163586a53`（M6実装コミット）まで。レビュー開始時の作業ツリーはcleanだった。
- **レビュー日**: 2026-08-18
- **仕様の正**: `docs/`、`schemas/`
- **M6完了条件**: `IMPLEMENTATION_PLAN.md` M6 DoD
- **前回レビュー**: なし（M6初回レビュー）
- **レビュー結果**: 独立検査でmajor 4件、minor 1件を検出した。未解消指摘の合計はblocker 0件、major 4件、minor 1件。

## 独立検査の新規指摘

## R1-01 — major — `scripts/build_html.py:217-240`

1. **指摘内容**: `pos_from_ref` が語彙ID末尾のハイフンを全て空白へ置換するため、Wordlistの正規品詞値自体にハイフンを含む4種（`be-verb`、`do-verb`、`have-verb`、`infinitive-to`）を `POS_LABELS` から引けず、形式①のHTML生成が `KeyError`・終了コード2で落ちる。`lex:to:infinitive-to`、`lex:be:be-verb`、`lex:do:do-verb`、`lex:have:have-verb` をそれぞれ対象にしたsetスキーマ適合入力で、`validate.py --schema set` は終了コード0だが `build_html.py` は順に `infinitive to`、`be verb`、`do verb`、`have verb` の `KeyError` になった。IDで空白からハイフンへ変換されるのは `modal auxiliary` の空白だけであり、元からあるハイフンまで逆変換してはならない。

2. **根拠となる設計文書の規則ID**: `docs/json-output-spec.md` ID-04、`docs/html-output-spec.md` UI-04・CON-03、`IMPLEMENTATION_PLAN.md` M6 DoD 3。

3. **具体的な修正案**: 末尾文字列への一括 `replace` をやめ、ID表記15種から品詞15種への固定対応表を用意する（少なくとも `modal-auxiliary` だけを `modal auxiliary` へ戻し、上記4種のリテラルハイフンは保持する）。形式①について品詞15種を全て通す回帰確認を追加し、setスキーマ適合入力がHTML生成・品詞ラベル表示まで終了コード0になることを確認する。

## R1-02 — major — `scripts/build_html.py:261-267`

1. **指摘内容**: 対象語強調が `text.partition(target_surface)` による最初の部分文字列一致なので、対象語より前に同じ文字列を含む別語があると誤った範囲を強調する。例えば対象を `lex:he:pronoun`、`stem` を `The boy says he is happy.`、`target_surface` を `he` としたsetスキーマ適合入力は正常生成されるが、出力は `T<strong class="target">he</strong> boy says he is happy.` となり、対象トークン `he` ではなく `The` の一部を強調した。生成・機械検査が保証する「対象語ちょうど1回」はトークン区間の回数であり、同じ表層文字列が他語の部分文字列として先行しないことは保証していない。この関数は形式①・③・④の画面と印刷の両方で使われるため、短い見出し語（`he`、`a`、`in` など）で教材表示を誤る。

2. **根拠となる設計文書の規則ID**: `docs/question-generation-spec.md` GEN-08、`docs/cefrj-validation-spec.md` MC-19、`docs/html-output-spec.md` LAY-08、`IMPLEMENTATION_PLAN.md` M6 DoD 3。

3. **具体的な修正案**: 現行の `target_surface` 文字列だけでは対象区間と偶然の部分文字列を常に識別できないため、まずPLN-05で位置特定契約を確定する。保守的には、候補・setへ検証済みの文字オフセットを保存してHTMLがその範囲だけを強調するか、対象区間の原文スライスが本文中の部分文字列としても一意になることを生成・機械検査で必須化する。`The ... he ...` のような先行部分文字列、複数語、活用形、アポストロフィ・ハイフンを含む対象を回帰ケースにする。

## R1-03 — major — `templates/index.html.j2:134`

1. **指摘内容**: 全てのdisabledボタンへ `opacity: 0.72` を適用しており、4択判定後にdisabledとなる正解・不正解ボタンのコントラストが必須の4.5:1を下回る。指定色をsRGBのアルファ合成で白背景へ合成すると、正解の `#0f5132` / `#d1e7dd` は約3.67:1、不正解の `#842029` / `#f8d7da` は約3.85:1になる。UI-02/UI-03どおり、判定時には先に全選択肢をdisabledにしてから `is-correct` / `is-wrong` を付けるため、実際の終端表示でこの低コントラスト状態が必ず発生する。

2. **根拠となる設計文書の規則ID**: `docs/html-output-spec.md` UI-02・UI-03・STY-01・ACC-02・ACC-05、`IMPLEMENTATION_PLAN.md` M6 DoD 3。

3. **具体的な修正案**: 判定済みの `.choice.is-correct:disabled` と `.choice.is-wrong:disabled` は `opacity: 1` に上書きするか、disabled全体の減光を廃止し、操作不能は `cursor` とネイティブのdisabled属性で表す。初期・正解・不正解・disabledを組み合わせた全テキスト/背景について、最終合成色のコントラストが4.5:1以上であることを検査する。

## R1-04 — major — `templates/index.html.j2:120`

1. **指摘内容**: 操作要素には `min-height: 44px` しかなく、最小幅44pxが保証されていない。特に整序の `.token` は共通ボタンの左右padding 14pxと1px枠だけなので、許可される1文字トークン `i` や `a` は通常の16pxシステムフォントで幅44px未満になる。bank側だけでなくJavaScriptが生成するanswer-row側のトークンも同じclassであり、問題作成で頻出する1文字語の選択・除去がMOB-02の44×44 CSS pxを満たさない。

2. **根拠となる設計文書の規則ID**: `docs/html-output-spec.md` UI-16・UI-17・MOB-02・ACC-03、`IMPLEMENTATION_PLAN.md` M6 DoD 3。

3. **具体的な修正案**: 少なくとも全 `.token`（bankとanswer-rowの双方）へ `min-width: 44px` を指定する。可能なら全buttonへ共通指定し、375px表示で初期状態・動的生成後を含む全可視button/input/summaryの `getBoundingClientRect()` が幅・高さとも44px以上であることを検査する。

## R1-05 — minor — `templates/index.html.j2:520`

1. **指摘内容**: 穴埋めの印刷解答を段落全体 `lang="en"` にしているため、同値表記がある場合の日本語ラベル `（別解: ...）` まで英語としてマークされる。英語コンテンツを最小要素単位で囲むACC-01/LAY-02の指定に反し、支援技術が日本語ラベルを英語音声で読み上げ得る。

2. **根拠となる設計文書の規則ID**: `docs/html-output-spec.md` LAY-02・PRN-11・ACC-01。

3. **具体的な修正案**: 段落は既定の日本語のままにし、`answer` と各 `answer_equivalents` だけを個別の `<span lang="en">` で囲む。日本語ラベルと区切り記号はその外側へ置く。

## 確認できた正常系・DoD再確認

- `git diff --check 4b4e1da07895897d8ef3cc87ae74eb424811179d..626d411c34ed153f0f9a428c141ce5c163586a53` はpassした。
- `.venv/bin/python -m py_compile scripts/*.py` はpassした。`doctor.py` は12 pass / 0 failで、D04はテンプレートの存在・読取り・Jinja2構文を確認した。
- 9形式それぞれのsetスキーマ適合1問フィクスチャを独立に生成し、R1-01の品詞例外を含まない通常例では全9件が `validate.py`、`build_html.py`、抽出したインラインJavaScriptの `node --check` を通過した。選択肢・トークン・問題の保存順もテンプレート上で維持されている。
- `docs/json-output-spec.md` のコミット済みset実例2件をそれぞれ2回生成し、各HTMLのバイト一致、BOMなしUTF-8、CRLF 0件、末尾改行1個を確認した。
- schema majorを2へ変更した入力は `E-CONTRACT-02`・終了コード1となり、事前配置した出力HTMLを保持した。通常のsetスキーマ不通過、入力不存在・不正JSON、入出力同一実体、出力不能も定義済みエラーへ変換する経路が実装されている。
- HTMLはCSS/JavaScriptをインライン化し、外部リソース属性・fetch/XHR・状態永続化・表示時再シャッフルを含まない。4択、フラッシュカード、穴埋め/書き換え、整序、例文自己採点の主要状態遷移と印刷分岐は、上記指摘を除いて仕様のDOM・固定文言・判定規則に対応している。

## 要約

- blocker: 0件
- major: 4件
- minor: 1件
- **判定**: `$m-fix` で修正が必要。
