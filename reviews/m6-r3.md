# M6 コードレビュー R3

- **対象差分**: `4b4e1da07895897d8ef3cc87ae74eb424811179d`（M5確定コミット）から作業ツリーまで。作業ツリーの基点は `626d411c34ed153f0f9a428c141ce5c163586a53`（M6実装コミット）で、M6 R1・R2対応の未コミット差分を含む。
- **レビュー日**: 2026-08-18
- **仕様の正**: `docs/`、`schemas/`
- **M6完了条件**: `IMPLEMENTATION_PLAN.md` M6 DoD
- **前回レビュー**: `reviews/m6-r2.md`
- **前回対応記録**: `reviews/m6-r2-resolutions.md`
- **前回指摘の解消判定**: 3件中3件を解消と判定した。
- **レビュー結果**: 独立検査でminor 1件を新規検出した。未解消指摘の合計はblocker 0件、major 0件、minor 1件。

## 前回指摘の解消判定

| 指摘ID | 判定 | 確認内容 |
|---|---|---|
| R2-01 | 解消 | `render_html()` がレンダリング全文のCRLFをLFへ変換し、続いて単独CRもLFへ変換してから末尾をLF1個へ正規化している。setスキーマに適合する解説へ単独CRとCRLFを混在させた入力でも、出力のCRは0件、末尾LFは1個となることを確認した。 |
| R2-02 | 解消 | 形式②・⑤の英語選択肢へ動的に追加する `.judgement-label` の「（正解）」「（あなたの選択）」の両方に `lang="ja"` が設定され、日本語ラベルが英語buttonの言語指定を継承しなくなった。 |
| R2-03 | 解消 | フラッシュカードの「まだ」一覧生成時、`vocab_flashcard_en2ja` の英例文だけを格納する `li` に `lang="en"` を設定し、`vocab_flashcard_ja2en` の日本語表面は文書既定の日本語を維持している。 |

## 独立検査の新規指摘

## R3-01 — minor — `scripts/build_html.py:291`

1. **指摘内容**: `blank_display()` が生成する空欄要素の日本語アクセシブル名 `aria-label="空欄"` と、`blank_input()` が生成する視覚非表示ラベル `空欄に入る語句` に `lang="ja"` がない。これらは形式②・⑤の空欄表示、および形式⑥・⑧の入力欄で `lang="en"` の祖先要素内に挿入されるため、支援技術上は日本語ラベルが英語として扱われる。印刷DOMの空欄表示でも同じ `blank_display()` を使う。R2-02で動的な日本語判定ラベルの言語継承は解消されたが、静的な支援技術向けラベルには同系統の問題が残っている。

2. **根拠となる設計文書の規則ID**: `docs/html-output-spec.md` LAY-02・LAY-07・UI-13・UI-21・ACC-01・ACC-07。

3. **具体的な修正案**: `blank_display()` の `.blank` と `blank_input()` の `<label>` に `lang="ja"` を設定し、日本語アクセシブル名だけ文書既定言語へ戻す。形式②・⑤・⑥・⑧について画面DOMと印刷DOMを検査し、英語祖先内の `aria-label="空欄"` と `空欄に入る語句` の実効言語が日本語になることを回帰確認する。

## 確認できた正常系・DoD再確認

- `git diff --check 4b4e1da07895897d8ef3cc87ae74eb424811179d` はpassした。
- `PYTHONPATH=scripts .venv/bin/python -m py_compile scripts/*.py` はpassした。`doctor.py` は12 pass / 0 failだった。
- `docs/question-generation-spec.md` の公式candidate 9形式をsetスキーマ形へ包み、全9形式でsetスキーマ検証、同一入力2回のHTMLバイト一致、UTF-8/BOMなし・CR 0件・末尾LF 1個、外部URL属性0件、インラインJavaScriptの `node --check` を確認した。全件がpassした。
- setスキーマ適合入力の解説文字列へ単独CRとCRLFを混在させた回帰確認では、画面用・印刷用の両転記箇所がLFへ正規化され、出力全体のCRが0件となった。
- R2の3修正と、9形式の主要DOM、保存順、判定用data属性、自己完結性、CLIのメジャー不一致・入力出力同一実体拒否の実装経路を再確認した。上記minorを除き、M6 DoDに新たな未充足は確認しなかった。

## 要約

- blocker: 0件
- major: 0件
- minor: 1件
- **判定**: 収束。コミット可（$m-commit）
