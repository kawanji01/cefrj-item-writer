# M6 コードレビュー R4

- **対象差分**: `4b4e1da07895897d8ef3cc87ae74eb424811179d`（M5確定コミット）から作業ツリーまで。作業ツリーの基点は `626d411c34ed153f0f9a428c141ce5c163586a53`（M6実装コミット）で、M6 R1〜R3対応の未コミット差分を含む。
- **レビュー日**: 2026-08-18
- **仕様の正**: `docs/`、`schemas/`
- **M6完了条件**: `IMPLEMENTATION_PLAN.md` M6 DoD
- **前回レビュー**: `reviews/m6-r3.md`
- **前回対応記録**: `reviews/m6-r3-resolutions.md`
- **前回指摘の解消判定**: 1件中1件を解消と判定した。
- **レビュー結果**: 独立検査でminor 1件を新規検出した。未解消指摘の合計はblocker 0件、major 0件、minor 1件。

## 前回指摘の解消判定

| 指摘ID | 判定 | 確認内容 |
|---|---|---|
| R3-01 | 解消 | `blank_display()` が生成する空欄要素と `blank_input()` が生成する視覚非表示ラベルの双方に `lang="ja"` が追加されている。正本文書の公式candidate 9形式をsetへ包んだ実生成でも、形式②・⑤・⑥・⑧の画面・印刷DOMに現れる `aria-label="空欄"` 7件と、形式⑥・⑧の入力ラベル2件が全て実効言語を日本語へ戻していることを確認した。 |

## 独立検査の新規指摘

## R4-01 — minor — `templates/index.html.j2:330`

1. **指摘内容**: 形式⑤ `grammar_mcq` の画面DOMが `.feedback`、`.translation-line`、`.explanation` の順で生成されており、UI-05が固定する「`.feedback` の直後に解説を置く」順序と一致しない。公式candidateから生成したHTMLでも各要素の開始順はこの並びになった。判定時の日本語訳表示と解説の自動展開自体は動作するため機能停止ではないが、M6 DoD 3が要求する形式別DOM構造から逸脱している。

2. **根拠となる設計文書の規則ID**: `docs/html-output-spec.md` UI-05・UI-06、`IMPLEMENTATION_PLAN.md` M6 DoD 3。

3. **具体的な修正案**: `grammar_mcq` の分岐だけ、`<details class="explanation">` を `.feedback` の直後へ移し、その後に `.translation-line` を置く。判定前は両要素が `hidden`、判定後は日本語訳を表示しつつ解説へ `open` を付ける現行状態遷移を維持する。生成DOMで `.feedback + .explanation` が成立し、その次に `.translation-line` が続くことを回帰確認する。

## 確認できた正常系・DoD再確認

- `git diff --check 4b4e1da07895897d8ef3cc87ae74eb424811179d` はpassした。
- `PYTHONPATH=scripts .venv/bin/python -m py_compile scripts/*.py` はpassした。`doctor.py` は12 pass / 0 failだった。
- `docs/question-generation-spec.md` の公式candidate 9形式をsetスキーマ形へ包み、全9形式でsetスキーマ検証、同一入力2回のHTMLバイト一致、UTF-8/BOMなし・CR 0件・末尾LF 1個、LAY-01骨格、外部リソース参照0件、インラインJavaScriptの `node --check` を確認した。全件がpassした。
- `grammar_cloze` のスキーマ適合する解説へ単独CRとCRLFを混在させた回帰でも、出力全体はCR 0件・末尾LF 1個となった。
- R1〜R3の修正経路、保存済み選択肢・整序トークン順、判定用 `data-*`、メジャー不一致拒否、入出力同一実体拒否、9形式の主要状態遷移を再確認した。上記minorを除き、新たなM6 DoD未充足は確認しなかった。

## 要約

- blocker: 0件
- major: 0件
- minor: 1件
- **判定**: 収束。コミット可（$m-commit）
