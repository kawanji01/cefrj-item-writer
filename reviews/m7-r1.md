# M7 コードレビュー R1

- **対象差分**: `46bf790f27059550850b8d4fc74720c62316578e`（M6確定コミット）から `19dc64c73c4f0ffb0cfe9f21eecf158204815da9`（M7実装コミット）まで。レビュー開始時の作業ツリーはクリーン。
- **レビュー日**: 2026-08-18
- **仕様の正**: `docs/`、`schemas/`
- **M7完了条件**: `IMPLEMENTATION_PLAN.md` M7 DoD
- **前回指摘の解消判定**: R1のため対象なし。
- **レビュー結果**: blocker 0件、major 5件、minor 2件を検出した。

## 独立検査の新規指摘

## R1-01 — major — `AGENTS.md:6`

1. **指摘内容**: 固定 `codex exec` はリポジトリルートを `--cd` にするため、子レビュアーのモデル入力にこの `AGENTS.md` が自動的に読み込まれる。`codex debug prompt-input` で確認すると、子には3要素の起動プロンプト以外に、プロジェクト `AGENTS.md`、個人 `AGENTS.md`、利用可能スキル等が渡る。特に本行は子自身にも「レビューは新しい `codex exec` を起動し、自分で判定しない」と指示するため、再帰起動・read-only下での起動失敗・指示競合がモデル判断に依存する。現存のM7実走例が合格していても、封筒と許可リソースだけに入力を限定したことにはならない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RC-01・RC-02・RC-10、`docs/cross-agent-compatibility.md` COR-05・COR-06・CDX-09、`IMPLEMENTATION_PLAN.md` M7 DoD 2。

3. **具体的な修正案**: PLN-05でCodex子プロセスのクリーン入力方式を確定する。少なくともプロジェクト/ユーザー `AGENTS.md`、個人スキル、追加ツール設定を子のモデル入力から除外する専用起動プロファイル/サンドボックスを用意し、起動プロンプト自体はCOR-07の3要素のままにする。修正後は `codex debug prompt-input` 相当の診断で、封筒パスとRC-10の許可リソース以外のユーザ固有コンテキストが入力されないことを回帰確認する。

## R1-02 — major — `.claude/settings.json:8`

1. **指摘内容**: CLI許可の末尾 `*` は引数全体を無制限に許可するが、Claude Codeの `Edit(./output/**)` パス制限はBash子プロセスのファイルアクセスを制限しない。そのため例えば本行は `build_normalized.py --out-dir <リポジトリ外>`、同様に13行目は `build_html.py --out <リポジトリ外>` を追加確認なしで起動できる。実装側の両CLIも絶対パスの出力を受理するため、「リポジトリ外への書き込み許可を含めない」という権限境界が強制されていない。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` CCW-11・CCW-12、`IMPLEMENTATION_PLAN.md` M7 DoD 1。

3. **具体的な修正案**: 末尾ワイルドカードだけのCLI許可を廃止し、標準フローで必要な引数形と出力先をリポジトリ内（実行時成果物は `output/`）へ固定した許可に絞る。併せてClaude CodeのBashサンドボックスでファイルシステム境界を強制し、`--out-dir /tmp/...`、`--out ../...`、シンボリックリンク経由のリポジトリ外書き込みが `dontAsk` で拒否されることを回帰検査する。

## R1-03 — major — `.claude/settings.json:11`

1. **指摘内容**: `Bash(python scripts/finalize_set.py *)` という汎用許可が、12行目のFIN-01専用ヒアドキュメント許可より先に同じ `finalize_set.py` 起動を広く許可する。これにより `--set-dir` を `output/` 以外にする呼出しや、任意ファイルからのstdinリダイレクトを専用規則で禁止できない。「引用付き `FIN01` ヒアドキュメントだけ」とするM7D-05とCCW-11の権限縮小が実質的に無効である。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` CCW-11・CCW-12、`DECISIONS.md` M7D-05、`IMPLEMENTATION_PLAN.md` M7 DoD 1。

3. **具体的な修正案**: 11行目の汎用 `finalize_set.py *` 許可を削除し、標準フローの確定は12行目の専用許可だけで実行する。`dontAsk` の新規セッションで、正規ヒアドキュメントは成功し、`< metadata.json`、非 `output/` の `--set-dir`、コマンド置換、追加コマンドはすべて拒否されることを個別に確認する。

## R1-04 — major — `docs/setup-claude-code.md:62`

1. **指摘内容**: 両セットアップ手順書は `en_core_web_sm` 取得を「唯一許可されるネットワーク取得」とするが、直前に実行する `scripts/setup.py` は新規venvで先に `pip install -r requirements.txt` を行い、リポジトリにwheelhouse等は同梱されていない。クリーン環境では依存パッケージ取得にもネットワークが必要である。さらに `docs/architecture.md` OPS-07は「依存導入とモデル取得はセットアップ処理でオフライン要件の対象外」とする一方、`docs/cross-agent-compatibility.md` SUP-02はモデル取得を唯一のネットワーク許可点としており、正本文書同士の未解消矛盾である。M7はPLN-05でこれを確定せず、両手順書で一方の解釈を断定している。

2. **根拠となる設計文書の規則ID**: `IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M7 DoD 4、`docs/cross-agent-compatibility.md` SUP-02・SUP-03、`docs/architecture.md` ARC-05・OPS-07。

3. **具体的な修正案**: PLN-05でセットアップ時の通信境界を確定する。依存導入もネットワーク許可とするならSUP-02と手順書の「唯一」を修正し、パッケージインデックへの通信と成功時期待結果を明記する。モデル取得だけを許可するなら固定wheelをリポジトリまたは検証済みオフラインwheelhouseから導入し、外部キャッシュなしのクリーン環境でDoD 4を再検証する。

## R1-05 — major — `NOTICE:23`

1. **指摘内容**: 本文はWordlistとGrammar Profileの両方を一括し、「引用を伴う研究・教育・商用利用と改変が許容」と断定し、27行目で再配布にも引用だけを求める。しかし、この明示的な利用・改変条件が確認できるのは同梱WordlistのREADMEであり、同梱Grammar Profile workbookに同等のライセンス文はない。2026-08-18時点の入手先サイトはGrammar Profileに引用方法と免責を示すが、商用・改変・再配布を引用のみで許容する条文は確認できない。またCEFR-Jの利用案内は商用利用に事前許諾を求めている（`https://www.cefr-j.org/contact.html`）。この状態でGrammar ProfileまでWordlist固有の条件で利用・改変・再配布できると案内するのは、NOTICEとして必要な正確な利用条件を満たさない。これはOPS-06の前提自体に要確認の法務上の穴があることを示す。

2. **根拠となる設計文書の規則ID**: `docs/requirements.md` FR-41、`docs/architecture.md` OPS-06、`IMPLEMENTATION_PLAN.md` PLN-05・M7成果物4（NOTICE）、`DECISIONS.md` D-22。

3. **具体的な修正案**: Grammar Profileに適用される商用・改変・再配布条件について著作権者の許諾または一次資料を確認し、PLN-05で証跡と適用範囲を確定する。確認できるまではWordlist固有条件とGrammar Profileの条件を分離し、後者を引用だけで商用・改変・再配布可能と案内しない。確定結果に合わせてOPS-06、D-22、NOTICE、READMEの利用案内を同時に改訂する。

## R1-06 — minor — `docs/setup-codex.md:135`

1. **指摘内容**: Codexトラブルシュートは「公式手順に従って更新」と外部リンクへ委ねるだけで、手順書内に更新コマンド文字列も成功時の期待結果もない。必要フラグを持たない旧版環境で、このリポジトリの手順書だけから更新を完了できない。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` CDX-10・SUP-02・SUP-03、`IMPLEMENTATION_PLAN.md` M7 DoD 4。

3. **具体的な修正案**: 対応版で利用できる `codex update`（または公式の導入方式別の更新コマンド）をコードブロックで示し、終了コード0・新バージョン表示・ `codex exec --help` の必須フラグ表示を成功条件として明記する。

## R1-07 — minor — `DECISIONS.md:371`

1. **指摘内容**: M7D-01は「AD-28の起動コマンドラインを `--ephemeral` の点に限って更新する」と明記するが、AD-28本文の既定値は依然として `--ephemeral` のない旧コマンドのままである。後続決定を追えば解決できるが、同じ決定ログ内に現行値と旧値が併存し、起動手順の参照先によって異なるコマンドを導く。

2. **根拠となる設計文書の規則ID**: `DECISIONS.md` AD-28・M7D-01、`docs/cross-agent-compatibility.md` CDX-03、`IMPLEMENTATION_PLAN.md` PLN-03。

3. **具体的な修正案**: AD-28の既定値を現行の `--ephemeral` 付きコマンドへ更新するか、AD-28に「M7D-01で置換済み」の明示と現行コマンドへの参照を追加する。

## 確認できた正常系・DoD再確認

- `git diff --check 46bf790f27059550850b8d4fc74720c62316578e..19dc64c73c4f0ffb0cfe9f21eecf158204815da9` はpassした。レビュー開始時の `git status --short` は空だった。
- `.venv/bin/python scripts/doctor.py` は12 pass / 0 failだった。
- M7実走成果物 `output/20260818-153251-cd28`（Codex）と `output/20260818-162750-wisc`（Claude Code）は、`validate.py --set-dir` でともに `status=complete`・setスキーマ適合、`set_check.py` でともにverdict `pass`だった。
- 上記2セットのset/HTML SHA-256は `CHANGELOG.md` の記録と一致した。不完全例 `output/20260818-160020-f25z` は `validate.py --set-dir` が `status=incomplete`・`set_json_path=null` を返した。
- Codex実走の `*.codex-prompt.txt` 2件はいずれもCOR-07/CDX-05の3行だけで構成され、`*.codex-last.txt` は対応する正規review監査へ取り込まれていた。
- `.claude/agents/cefrj-reviewer.md` の名称・説明・ `Read, Grep, Glob` 制限、`.claude/skills/cefrj-author/SKILL.md` のフロントマター、`CLAUDE.md` / `AGENTS.md` のコア参照とCLI判定非上書き配線は、上記指摘を除きCCW/CDXの構成要件に一致した。
- 両セットアップ手順書は日本語の11節を指定順で持ち、READMEはLLM送信、ツール間保証範囲、教師の最終確認責任を明示していた。

## 要約

- blocker: 0件
- major: 5件
- minor: 2件
- **判定**: `$m-fix` で修正が必要
