# M7 コードレビュー R4

- **対象差分**: `46bf7906d68264852b5b0c34a54ea1c38b44c0fe`（M6確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `19dc64c144005b3c0019f5e7653bd1e3546987ac`（M7実装コミット）で、R1〜R3対応の追跡済み変更、未追跡の `.claude/cleanup_staging.py`・`.claude/run_reviewer.py`・`.claude/hooks/`、R1〜R3のレビュー記録を含む。
- **レビュー日**: 2026-08-19
- **仕様の正**: `docs/`、`schemas/`
- **M7完了条件**: `IMPLEMENTATION_PLAN.md` M7 DoD
- **前回指摘の解消判定サマリー**: R3の3件は3件とも解消した。
- **レビュー結果**: blocker 0件、major 1件、minor 1件を検出した。

## 前回指摘の解消判定

- **R3-01: 解消** — candidate/review_request用の固定 `.staging` 名、既存衝突を拒否するPreToolUseガード、スキーマとパス種別を固定する検証許可、限定削除ヘルパーが実装された。許可名・不許可世代・存在しない検証対象・追加引数をガード単体で再確認し、Claude完走セット `output/20260819-092134-jk41` に `.staging` が残らないこと、set検証と集合検査がpassすることを確認した。
- **R3-02: 解消** — `.claude/run_reviewer.py` は `--safe-mode`、COR-07の3行、`Read,Grep,Glob`、空MCP、非永続セッションで子 `claude -p` を起動し、`review_timeout_seconds` をプロセスグループ全体へ適用してSIGTERM/SIGKILLで停止する。固定コマンド構築、プロンプト3行、ガードの正常許可・追加引数拒否、擬似子プロセスの期限超過停止を再確認した。記録済み異常系 `output/20260819-095749-1tg9` は3件の `process_failure` を残し、`.staging` と `set.json` がない。
- **R3-03: 解消** — `agent/author-core.md` の未activate時フォールバックは `.venv/bin/python` だけとなり、ネイティブWindows形式は削除された。

## 独立検査の新規指摘

## R4-01 — major — `docs/cross-agent-compatibility.md:37`

1. **指摘内容**: 承認済みM7D-13はClaude Codeのレビュー方式を同期Taskから `.claude/run_reviewer.py` が監視する新規 `claude -p` へ置き換え、実装の `CLAUDE.md:6` と同文書のCOR-06・CCW-09/10もその方式になっている。しかしCCW-01の本行は依然として、`CLAUDE.md` が専用サブエージェント `cefrj-reviewer` をTaskで起動するよう指示することをMUSTとしている。さらに `docs/subagent-review-spec.md:48` は実行方式をTaskと定義し、同589行のINF-01はClaude側の異常終了をTask失敗と例示し、`docs/requirements.md:100` のNFR-01aもTask起動を現行方式としている。同じ規範文書群がTaskと監視 `claude -p` の両方を要求するため、現行 `CLAUDE.md` はCCW-09には適合する一方でCCW-01には適合できず、仕様を正としてM7 DoDを一意に判定できない。

2. **根拠となる設計文書の規則ID**: `DECISIONS.md` M7D-13、`docs/cross-agent-compatibility.md` COR-06・CCW-01・CCW-09・CCW-10、`docs/subagent-review-spec.md` 1.3節・INF-01、`docs/requirements.md` NFR-01a、`IMPLEMENTATION_PLAN.md` PLN-03・M7 DoD 1・2。

3. **具体的な修正案**: M7D-13を現行決定として、CCW-01第3項を固定ラッパー起動へ更新し、`docs/subagent-review-spec.md` 1.3節とINF-01、`docs/requirements.md` NFR-01aのTask表記を監視 `claude -p` サブプロセスへ統一する。`DECISIONS.md` D-15の旧Task記述もM7D-13で置換済みと明示するか現行値へ更新し、Taskが標準フローとして残っていないことを `rg` と文書間照合で確認する。

## R4-02 — minor — `IMPLEMENTATION_PLAN.md:27`

1. **指摘内容**: R1-04対応のM7D-07により、ネットワーク例外は固定版依存パッケージとspaCyモデルを取得するセットアップ処理へ統一され、`docs/architecture.md` ARC-05、`docs/requirements.md` NFR-04、両セットアップ手順書とREADMEはその契約になった。しかしPLN-07は現在も「セットアップ時のspaCyモデル取得を唯一の例外」としている。クリーン環境の `scripts/setup.py` はモデルより先にパッケージインデックスから固定依存を取得するため、現行実装・設計とマイルストーン総則の文言が一致していない。現行手順と実装は正しいため実走を妨げないが、後続M8がPLN-07だけを基準にすると、許可済みセットアップ通信を違反と誤判定する。

2. **根拠となる設計文書の規則ID**: `DECISIONS.md` M7D-07、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-07、`docs/architecture.md` ARC-05、`docs/requirements.md` NFR-04、`docs/cross-agent-compatibility.md` SUP-02。

3. **具体的な修正案**: PLN-07を、固定版依存パッケージとspaCyモデルの取得を行う `scripts/setup.py` だけがネットワーク例外であり、セットアップ完了後の決定的CLIは完全オフライン、というM7D-07/ARC-05の現行表現へ揃える。

## 確認できた正常系・DoD再確認

- `.venv/bin/python scripts/doctor.py` は12 pass / 0 failだった。
- Claude完走セット `output/20260819-092134-jk41` とCodex完走セット `output/20260818-153251-cd28` は、`validate.py --set-dir` でcomplete、setスキーマでvalid、`set_check.py` でpassだった。Claude完成セットと3回タイムアウト異常系の双方に `.staging` は残っていない。
- Codex CLI 0.147.0の `codex debug prompt-input` を手順書の専用 `CODEX_HOME`・4件のdisable・2件のコンテキスト無効化設定で再実行し、`user_message_count=1`、`user_prompt_exact_match=true`を確認した。
- `.claude/settings.json` はJSONとして妥当で、`.claude/cleanup_staging.py`・`.claude/run_reviewer.py`・`.claude/hooks/*.py` はPython構文検査に合格した。`run_with_timeout` は期限超過した擬似子プロセスを停止した。
- `git diff --check 46bf7906d68264852b5b0c34a54ea1c38b44c0fe` はpassし、`schemas/` と `scripts/` の差分は空だった。

## 要約

- blocker: 0件
- major: 1件
- minor: 1件
- **判定**: `$m-fix` で修正が必要
