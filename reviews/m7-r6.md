# M7 コードレビュー R6

- **対象差分**: `46bf7906d68264852b5b0c34a54ea1c38b44c0fe`（M6確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `19dc64c144005b3c0019f5e7653bd1e3546987ac`（M7実装コミット）で、R1〜R5対応の追跡済み変更、未追跡の `.claude/cleanup_staging.py`・`.claude/run_reviewer.py`・`.claude/hooks/`、R1〜R5のレビュー記録を含む。
- **レビュー日**: 2026-08-19
- **仕様の正**: `docs/`、`schemas/`
- **M7完了条件**: `IMPLEMENTATION_PLAN.md` M7 DoD
- **前回指摘の解消判定サマリー**: R5の2件中、解消1件、未解消1件。未解消のR5-02はR6-01として継続する。
- **レビュー結果**: blocker 0件、major 2件、minor 0件を検出した。

## 前回指摘の解消判定

- **R5-01: 解消** — `.claude/hooks/guard_build_html.py` が追加され、`.claude/settings.json` の全Bash PreToolUseへ接続されている。ガードは `python scripts/build_html.py --set output/<set_id>/set.json` のコマンド全文だけを許可し、別setの`set.json`および`docs/set.json`を`--out`に追加した呼出しを終了コード2で拒否した。正常形は終了コード0で許可された。
- **R5-02: 未解消** — `reviews/m7-r5-resolutions.md`にR5-02の対応記録がなく、`AGENTS.md`とCDX-03は依然として監視なしで`codex exec`を直接起動する。リポジトリ内で`review_timeout_seconds`を読み、プロセスグループを停止する実装はClaude用`.claude/run_reviewer.py`だけである。詳細はR6-01。

## 独立検査の新規指摘

## R6-01 — major — `AGENTS.md:9`

1. **指摘内容**: R5-02のCodex側タイムアウト欠落は未解消である。固定コマンドは`codex exec`を直接起動するだけで、セッション設定スナップショットの`review_timeout_seconds`を壁時計期限として待機し、超過時に子プロセスを停止するラッパー・ホスト手順・実装がない。現行`codex exec --help`にもタイムアウト指定はなく、リポジトリ検索でもCodexプロセスを期限付きで停止するコードは存在しない。このため、子が出力を返さず停止した場合にINF-07をINF-01のインフラ障害へ変換できず、同一requestの最大3実行とセット中止へ進めない。正常系のCodex完走記録はこの異常系を実証しない。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` COR-09・CDX-03、`docs/subagent-review-spec.md` INF-01・INF-02・INF-07、`agent/author-core.md` 第7.2節、`IMPLEMENTATION_PLAN.md` PLN-02・M7成果物2・M7 DoD 2。

3. **具体的な修正案**: PLN-05で、CDX-03の専用`CODEX_HOME`、COR-07の3行入力、read-only子サンドボックス、個別承認境界を維持したまま、検証済みセッション値で固定`codex exec`のプロセスグループを壁時計監視するホスト機構を確定する。専用ラッパーを採用する場合は、承認対象をその固定呼出し1件へ限定するガードとともに規範文書、`AGENTS.md`、Codexセットアップ手順書へ反映する。停止しない擬似子を使い、各実行が設定秒数で停止し、同一requestの3件がAUD-09 `process_failure`となり、`set.json`なしで中止することを受け入れ確認する。

## R6-02 — major — `.claude/settings.json:9`

1. **指摘内容**: Claude Codeアダプタが`machine_check.py`の`--requested-count`を1から20まで20本の許可規則として列挙し、問題数の数値制約を`.claude/`配下へ重複保持している。`set_question_max`は`limits.json`だけを実行時の正とする運用パラメータであり、値域判定は`machine_check.py`自身のCLI契約で行う。権限規則が安全な引数形を固定することは必要だが、現在の列挙はCOR-02が明示的に禁止する「問題数」の数値制約をアダプタへ転記しており、M7 DoD 1のアダプタ純度を満たさない。設定値・スキーマが将来改訂された場合にも、CLIとは別にこの列挙の追随が必要となり、COR-04が禁止するホスト別の挙動差の入口になる。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` COR-01・COR-02・COR-04・CCW-11、`docs/cefrj-validation-spec.md` VAL-CFG-01、`docs/architecture.md` CLI-16、`IMPLEMENTATION_PLAN.md` M7 DoD 1。

3. **具体的な修正案**: 20本の列挙を、`--requested-count`が単一の10進整数トークンであることまでを配線として許可する1規則へ置き換え、PreToolUseガードでコマンド全文、固定引数順、監査パス、追加引数・リダイレクト・パイプ・複合コマンドの不在を検査する。問題数が1〜現在の`set_question_max`かという意味上の判定は、現行どおり`machine_check.py`にだけ行わせる。正常な設定内値が`dontAsk`で通り、追加引数付き呼出しがガードで拒否され、設定外値がCLIの定義済み`E-INPUT-04`で停止することを確認する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python scripts/doctor.py` は12 pass / 0 failだった。
- Claude完走セット `output/20260819-092134-jk41` とCodex完走セット `output/20260818-153251-cd28` は、`validate.py --set-dir`でcomplete、setスキーマでvalid、`set_check.py`でpassだった。
- `.claude/settings.json` はJSONとして妥当で、`.claude/cleanup_staging.py`・`.claude/run_reviewer.py`・`.claude/hooks/*.py` はPython構文解析に合格した。
- R5-01のbuild_htmlガードは正常形を許可し、別setの`set.json`と`docs/set.json`への`--out`追加を拒否した。
- `git diff --check 46bf7906d68264852b5b0c34a54ea1c38b44c0fe`はpassし、`schemas/`と`scripts/`の差分は空だった。
- 現行Claude CLIの`--safe-mode`、`--system-prompt-file`、`--tools`、`--permission-mode`、`--no-session-persistence`、`--no-chrome`、`--strict-mcp-config`と、Codex固定コマンドの各必須オプションがhelpに存在することを確認した。ただしCodex側に壁時計タイムアウト指定はない。

## 要約

- blocker: 0件
- major: 2件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
