# M7 コードレビュー R7

- **対象差分**: `46bf7906d68264852b5b0c34a54ea1c38b44c0fe`（M6確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `19dc64c144005b3c0019f5e7653bd1e3546987ac`（M7実装コミット）で、R1〜R6対応の追跡済み変更、未追跡の `.claude/cleanup_staging.py`・`.claude/run_reviewer.py`・`.claude/hooks/`、R1〜R6のレビュー記録を含む。
- **レビュー日**: 2026-08-19
- **仕様の正**: `docs/`、`schemas/`
- **M7完了条件**: `IMPLEMENTATION_PLAN.md` M7 DoD
- **前回指摘の解消判定サマリー**: R6の2件中、解消1件、未解消1件。未解消のR6-01はR7-01として継続する。
- **レビュー結果**: blocker 0件、major 1件、minor 0件を検出した。

## 前回指摘の解消判定

- **R6-01: 未解消** — `reviews/m7-r6-resolutions.md`はM7D-16のPLN-05提案が承認待ちで未実装と記録しており、実差分にもCodex側の壁時計監視・超過時停止機構はない。詳細はR7-01。
- **R6-02: 解消** — `.claude/settings.json`の問題数別20規則は単一の構文規則へ統合され、`.claude/hooks/guard_machine_check.py`は`requested_count`を10進整数トークンとしてだけ受理する。ガード単体で20と21を許可し、追加引数、set_id不一致、generation不一致を終了コード2で拒否した。設定外の21は実`machine_check.py`が終了コード1・`E-INPUT-04`で停止し、意味上の上限判定がCLIに留まることも確認した。

## 独立検査の新規指摘

新規指摘はない。R6から継続する未解消指摘は次の1件である。

## R7-01 — major — `AGENTS.md:9`

1. **指摘内容**: Codex側の壁時計タイムアウト欠落は未解消である。`AGENTS.md`とCDX-03は固定`codex exec`を直接起動するだけで、セッション設定スナップショットの`review_timeout_seconds`を壁時計期限として待機し、超過時に実行中のプロセスグループを停止するホスト機構を持たない。現行Codex CLI 0.147.0の`codex exec --help`にもタイムアウト指定はなく、リポジトリ内でCodex子プロセスを監視する実装も存在しない。`docs/subagent-review-spec.md` INF-07は「Codexではホスト側のサブプロセス待機が停止する」と記すが、その待機・停止の起動手順、実装、強制境界が配線されていないため、子が応答しない場合にINF-01へ変換して同一requestの最大3実行とセット中止へ進めない。正常完走記録はこの異常系を実証しない。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` COR-09・CDX-03、`docs/subagent-review-spec.md` INF-01・INF-02・INF-07、`agent/author-core.md` 第2.3節・第7.2節、`IMPLEMENTATION_PLAN.md` PLN-02・M7成果物2・M7 DoD 2。

3. **具体的な修正案**: 承認待ちのPLN-05を先に確定し、専用`CODEX_HOME`、COR-07の3行入力、read-only子サンドボックス、モデル無指定、固定コマンドだけの個別承認というCDX-03の境界を維持したまま、検証済みセッション値でCodex子プロセス全体を壁時計監視するホスト機構を実装する。専用ラッパーを採用する場合は、固定argv以外を拒否し、超過時に子プロセスグループを停止して非0終了し、生出力なしをAUD-09 `process_failure`へ返す責務だけに限定する。停止しない擬似子で、各実行が設定秒数で停止し、同一requestの3失敗後に`set.json`を作らず中止するCodex側受け入れ記録を残す。

## 確認できた正常系・DoD再確認

- `.venv/bin/python scripts/doctor.py` は12 pass / 0 failだった。
- Claude完走セット `output/20260819-092134-jk41` とCodex完走セット `output/20260818-153251-cd28` は、`validate.py --set-dir`でcomplete、setスキーマでvalid、`set_check.py`でpassだった。
- Claudeタイムアウト異常系 `output/20260819-095749-1tg9` は`set.json`がなく、上記2完成セットを含め検査対象3セットに`.staging/`の残留ディレクトリはなかった。
- `.claude/settings.json`はJSONとして妥当で、`.claude/cleanup_staging.py`・`.claude/run_reviewer.py`・`.claude/hooks/*.py`の8ファイルはPython構文解析に合格した。
- R6-02のmachine_checkガードは正常形と構文上の設定外整数を許可し、追加引数・set/generation不一致を拒否した。実CLIは設定外値を`E-INPUT-04`で拒否した。
- `git diff --check 46bf7906d68264852b5b0c34a54ea1c38b44c0fe`はpassし、`schemas/`と`scripts/`の差分は空だった。

## 要約

- blocker: 0件
- major: 1件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
