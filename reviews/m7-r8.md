# M7 コードレビュー R8

- **対象差分**: `46bf7906d68264852b5b0c34a54ea1c38b44c0fe`（M6確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `19dc64c144005b3c0019f5e7653bd1e3546987ac`（M7実装コミット）で、R1〜R7対応の追跡済み変更、未追跡の `.claude/cleanup_staging.py`・`.claude/run_reviewer.py`・`.claude/hooks/`・`.codex/run_reviewer.py`、R1〜R7のレビュー記録を含む。
- **レビュー日**: 2026-08-19
- **仕様の正**: `docs/`、`schemas/`
- **M7完了条件**: `IMPLEMENTATION_PLAN.md` M7 DoD
- **前回指摘の解消判定サマリー**: R7の1件は解消した。未解消0件。
- **レビュー結果**: blocker 0件、major 0件、minor 0件。新規指摘はない。

## 前回指摘の解消判定

- **R7-01: 解消** — `reviews/m7-r7-resolutions.md`の対応記録どおり、`.codex/run_reviewer.py`が追加され、固定request監査パス1件だけを受理し、専用`CODEX_HOME`、全コンテキスト無効化引数、`--ephemeral`、`--sandbox read-only`、モデル無指定、COR-07の3行stdin、導出`codex-last`作業ファイルからなる固定argvをシェル不使用で構築する。純粋関数の固定argv・3行プロンプトを仕様の列と完全一致で照合し、停止しない擬似子を新しいプロセスグループで実行した試験では、1秒の期限に対して約1.006秒で`ReviewerTimeoutError`となり、プロセスグループの消滅と終了124への対応付けを確認した。異常系`output/20260819-130400-tm16`にも同一requestのAUD-09 `process_failure`・終了124が3件あり、`set.json`と`.staging/`は存在しない。実効入力診断は`{"additional_user_context_count": 0, "input_text_count": 1, "user_message_count": 1, "user_prompt_exact_match": true}`で、追加userコンテキストなしを再確認した。

## 独立検査の新規指摘

指摘はない。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py`はpassし、`.claude/settings.json`もJSONとして妥当だった。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `codex-cli 0.147.0`の`codex exec --help`に固定argvの全必須オプションが存在し、`python .codex/run_reviewer.py --help`は終了コード0で必須`--request`だけを公開した。
- Codex完成セット`output/20260818-153251-cd28`とClaude完成セット`output/20260819-092134-jk41`は、`validate.py --set-dir`でcompleteかつsetスキーマvalid、`set_check.py`でpassだった。
- `find output -type d -name .staging`は0件だった。Codexタイムアウト異常系には3件の終了124監査があり、`set.json`はない。
- 両セットアップ手順書はSUP-02の11節を指定順で持つ。現行文書群に旧来の直接Codexレビュー起動またはClaude Task起動を標準フローとして残す記述はなく、監視ラッパー、壁時計停止、WSL2境界、セットアップ時だけのネットワーク例外は相互に一致した。
- `scripts/`・`schemas/`・`data/source/`・`data/normalized/`・`data/config/`はM6確定コミットから差分がなく、`build_normalized.py --diff`は語彙・文法とも追加・削除・レベル変更0件、`written=[]`だった。
- `git diff --check 46bf7906d68264852b5b0c34a54ea1c38b44c0fe`はpassした。

## 要約

- blocker: 0件
- major: 0件
- minor: 0件
- **判定**: 収束。コミット可（$m-commit）
