# M8 コードレビュー R1

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8実装コミット、annotated tag `v1.0.0`）で、未追跡の `.agents/skills/cefrj-author/SKILL.md`・`docs/assets/cefrj-material-workflow.png`・`docs/assets/cefrj-material-workflow-ai-vs-machine.png`を含む。
- **レビュー日**: 2026-08-20
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: なし（M8初回レビュー）
- **レビュー結果**: blocker 0件、major 3件、minor 1件を検出した。

## 独立検査の新規指摘

## R1-01 — major — `tests/replay/harness.py:125`

1. **指摘内容**: リプレイハーネスが、フィクスチャへ置換してよい候補生成・独立レビューの境界だけでなく、世代管理・補充対象の所属決定・再生成指摘の組み立て・合否集計・教師照会への遷移までテスト側で再実装している。`for step in scenario["steps"]` はシナリオに列挙済みの試行をそのまま全件処理し、実装側が試行を開始・停止する経路を呼ばない。したがってRPL-10は製品実装が`2N×3`で停止することではなく、fixtureに18 stepしか書かれていないことを数えており、上限超過stepを追加してもハーネス自身は拒否しない。RPL-03の補充順・上限もscenario側の`q02`に依存する。さらに`request["mode"]`を遷移判定に使わず、未完成かつ非abortedなら一律`teacher_consult`とするため、RPL-04は明示モード固有の自動補充禁止と全3世代理由サマリーを検証していない。150件のpytestが通過しても、実際の作問フローがRG-03/RG-15/RG-16/RG-19に違反する回帰を検出できない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` RPL-R-01・RPL-R-02・RPL-R-03・RPL-03・RPL-04・RPL-10、`docs/subagent-review-spec.md` RG-03・RG-12〜RG-16・RG-19、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 世代管理・補充・教師照会・監査保存を担う決定的な状態遷移実装を製品側の共通モジュールへ分離し、通常フローとリプレイの双方がその同じ実装を呼ぶようにする。リプレイでは候補生成providerとレビュー実行providerの2点だけをfixture providerへ差し替える。上限外stepをfixtureに含めても共通実装が消費しないこと、明示モードでは補充候補があっても教師照会へ進むこと、照会データに全世代の終端理由・確定数・試行数が残ることを負のケースでも確認する。新しい製品モジュールの配置やアダプタ責務が現行設計で未定義なら、実装前にPLN-05で決定する。

## R1-02 — major — `tests/support.py:73`

1. **指摘内容**: リプレイの一時出力を`ROOT / "output" / set_id`へ作成しており、RPL-R-04の「テスト一時ディレクトリ」「`output/`を汚してはならない」に直接反する。実際にpytest実行中は`output/20990101-000000-rpl1`などの合成監査ディレクトリが実リポジトリの`output/`へ現れ、正常終了時だけ`finally`で削除された。テスト中断・プロセス強制終了では残留し、同一予約IDとの衝突で次回テストを失敗させるほか、実教材出力とテスト監査を同じ名前空間へ混在させる。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` RPL-R-04、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: `replay_scenario`へpytestの`tmp_path`を渡し、その配下にsetディレクトリを作成する。`machine_check.py`・`set_check.py`・`finalize_set.py`・`validate.py`へは一時ディレクトリの絶対パス、または専用一時リポジトリ内の相対パスを渡せるようテストヘルパーを調整し、リプレイ実行中・異常終了後のどちらも実リポジトリの`output/`へ書き込まないことを検査する。

## R1-03 — major — `tests/support.py:127`

1. **指摘内容**: `review_request()`、`write_slot_outcome()`、`finalize_metadata()`がJSON文書をテストコード内の辞書リテラルで組み立て、RPL群とCI-SET群の監査入力・`finalize_set.py` stdinとして使用している。CI-R-03はテスト入力を第4節のfixture/goldenへ限定し、コード内JSON生成をCI-SCH-03の必須欠落・型不正だけに限定しているため、この3系統は許可範囲外である。また`tests/unit`・`tests/replay`の関数名/docstringには`CI-R-03`が1件もなく、TST-08が要求するテストID対応もない。入力契約をfixture indexと独立レビューから外したままテスト実装と期待値を同じコードで作れるため、M8 DoD 1の「全テストID実装」を満たさない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` TST-08・CI-R-03・FIX-05、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: R1-01の共通状態遷移実装が生成すべきreview_request・slot outcome・finalize metadataはテスト側で再構築せず、その製品実装の出力を使用する。CI-SETの独立入力として必要な監査文書は`tests/fixtures/`へ正準JSONで保存してindex登録し、テストは読み込むだけにする。あわせてCI-R-03のfixture限定とコード内JSON不在を検査する、IDを明記したメタテストを追加する。

## R1-04 — minor — `tests/unit/test_html.py:13`

1. **指摘内容**: 9形式のテスト対象を固定目録ではなく、現存する`tests/golden/sets/*.set.json`のglob結果から作っている。set/HTMLの1組を誤って削除するとpytestのparameter数も8へ縮み、CI-HTM-02/03は全件passできる。同じ縮退が`test_ci_sch_02_valid_examples()`のset glob、`test_ci_sch_04_format_union()`の`official_*.json` glob、GLD-05のgolden case globにもあり、9形式または必須2ケースの欠落自体をCIが検出しない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-SCH-02・CI-SCH-04・CI-HTM-02・CI-HTM-03・FIX-01・GLD-05、`IMPLEMENTATION_PLAN.md` M8 DoD 1・2。

3. **具体的な修正案**: 9形式コードを固定tupleとしてテストへ定義し、golden set・golden HTML・公式candidateの実ファイル名集合がその固定集合と完全一致することを先にassertする。golden caseも`these_relative_clause.candidate.json`と`estimate_label.candidate.json`の固定2件との完全一致を検査し、その固定列を各parameterizeへ渡す。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は150件全てpassした（262.55秒）。ただしR1-01〜R1-04のため、このpassだけではM8 DoD 1の充足証拠にならない。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙・文法とも追加・削除・レベル変更0件、`written=[]`だった。
- A-02〜A-06の9セットとA-15のClaudeセット、計10セットは`validate.py --set-dir`で全てcompleteかつsetスキーマvalidだった。A-07の独立レビュー2件はCHK-03でfailし、A-08は`kyoinban`と`reviewer_estimate`を区別していた。
- `v1.0.0`は`c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`を指すannotated tagで、注釈にM8自動テストとA-01〜A-15完了が記録されていた。
- `git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。pytest正常終了後、`output/2099*`の残留はなかった。

## 要約

- blocker: 0件
- major: 3件
- minor: 1件
- **判定**: `$m-fix` で修正が必要
