# M8 コードレビュー R10

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R9対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-22
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r9.md`
- **前回指摘の解消判定サマリー**: R9-01〜R9-04は解消。固定review 41件へ教員版・推定の既知全文法目録を照合する検査、`dict(...)`・段階的代入・`update()`・merge/unpackの自己負検査、POSIX shellによる3秒timeout試験、`.agents/`・`.tmp-slide-*/`の追跡除外を実差分で確認した。CI-R-03メタ検査には前回とは別の構築経路が残るため、新規問題R10-02として記録する。
- **レビュー結果**: blocker 0件、major 2件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R9-01 — 解消**: `PASS_GRAMMAR_ESTIMATES`が主動詞`has`を`reviewer_estimate`として定義し、q02のpass/fail reviewへA1.1・日本語根拠付きで反映されている。メタ検査はpass 23件とfail 18件を既知のID/source/span/level集合へ照合し、全要素のstructure/evidence非空、`has`の完全なstructure/evidence、製品`review_semantic_problems()`適合を検査する。
- **R9-02 — 解消**: `static_dict_keys()`とbuilder追跡が`dict(...)`、文字列キーの段階的代入、`update()`、`|`、`**` unpackを扱い、各構築形の自己負ケースが違反を検出する。前回の具体的再現ケースは通らない。別経路の未検出はR10-02に記録する。
- **R9-03 — 解消**: timeout用偽レビュアーは低オーバーヘッドのPOSIX shellとなり、3秒以内にNUL・非UTF-8バイトを含むstderrを書いてからTERMを無視する。今回のマージ条件と同じ全239テスト実行を含めて安定通過した。
- **R9-04 — 解消**: `.gitignore`の限定規則により、`.agents/`と`.tmp-slide-*/`がGit追跡対象外になることを`git check-ignore -v`で確認した。

## 独立検査の新規指摘

## R10-01 — major — `scripts/flow_control.py:1191` / `tests/unit/test_flow_control.py:1255`

1. **指摘内容**: CHK-03の複数上限超過構造とviolationsの対応が一対一になっておらず、1件のviolationを複数の独立構造へ使い回せる。`review_semantic_problems()`は各上限超過inventory itemについて`any(chk03_violation_matches_inventory_item(...))`を独立に評価するだけで、一度対応に使ったviolationを消費しない。`chk03_violation_matches_inventory_item()`もstructureを対応キーに含めないため、同じ英文スパン・導入レベル・level_source根拠を持つ別構造が複数あれば同じ1件が全要素に一致する。実際に既存の`chk03_review_for_candidate()`が作る上限超過itemを複製し、`structure`だけを別構造名へ変えてinventoryへ追加したところ、上限超過item 2件・CHK-03 violation 1件でも戻り値が`[]`となった。現行自己負テストは2件目のspanとevidenceを両方変えるため、この使い回しを検出しない。これによりRR-05が要求する「超過構造ごとにviolation 1件」を満たさないreview_resultが正規監査へ受理され得る。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-03手順1〜3・CHK-03記録規則・RR-02・RR-05・INF-05、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 上限超過inventory itemとCHK-03 violationsを、violationの配列indexを一度だけ使える一対一対応として照合する。二部マッチングまたは決定的な未使用index消費で全超過itemが別々のviolationへ対応することを必須化し、対応しない余剰CHK-03 violationも拒否する。同じspan・level・evidenceだが異なるstructureの上限超過itemを2件持つreviewについて、violation 1件を拒否し2件なら受理する自己負・正常ケースを追加する。

## R10-02 — major — `tests/unit/test_fixtures.py:95`

1. **指摘内容**: CI-R-03の必須メタ検査は前回追加された辞書構築形以外なら、製品JSONのテストコード内直書きを依然として検出できない。`contract_dict_violations()`が追跡するのは静的な`ast.Dict` / `dict(...)` / `|`と、同一変数への定数文字列subscript代入・`update()`だけである。反例として、(a) `machine = json.loads('{"violations": []}')`、(b) 空辞書をaliasへ代入して`alias["violations"] = []`、(c) `for key in ("violations",): machine[key] = []`をそれぞれ`TEST_PRODUCT_CONTRACTS`へ検査すると、全て違反リスト`[]`になった。いずれもCI-R-03が禁止するテスト入力JSONをfixture/product出力を介さず構築できるため、「テストコード内JSON不在を検査するメタテスト」という必須条件を構文表現だけで回避できる。現在の自己負ケースにはJSON文字列、alias、計算・反復されるキーがない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 任意の辞書データフローを列挙する方式ではなく、テスト入力の取得口を正準fixture loaderと検査対象製品の直前段出力APIへ限定し、その許可済み取得口から対象CLI・製品関数へ届く呼出しだけをASTでallowlist検査する方式を優先する。現方式を維持する場合は少なくともJSON object文字列を`json.loads`等で復元する経路、alias伝播、反復・計算キーによる代入を追跡し、上記3反例を自己負ケースへ追加する。スキーマ不当例CI-SCH-03とfixture生成器の明示的例外は、許可された関数・ファイル単位で限定する。

## 確認できた正常系・DoD再確認

- R9-01について、q02のpass/fail review双方に`has`の`reviewer_estimate`記録があり、固定review 41件を既知集合と製品意味検査へ照合することを確認した。
- R9-02について、`dict(...)`、段階的代入、`update()`、merge/unpackの各自己負ケースが違反を検出することを確認した。
- R9-03について、偽レビュアーがPOSIX shell・3秒期限・TERM無視・NUL/非UTF-8 stderrを使用し、両ラッパーの停止後drainを検査することを確認した。
- `.venv/bin/python -m pytest tests/unit tests/replay -q`は239件全てpassした（605.63秒）。
- ゴールデン候補・machine互換の対象2件は2件passした（3.80秒）。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/unit/*.py tests/replay/*.py tests/generate_assets.py`、`jq empty .claude/settings.json`、`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- 上限超過inventory item 2件をCHK-03 violation 1件で受理する反例、およびJSON文字列・alias・反復キーによる製品辞書構築をCI-R-03メタ検査が見逃す3反例を再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。

## 要約

- blocker: 0件
- major: 2件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
