# M8 コードレビュー R11

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R10対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-22
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r10.md`
- **前回指摘の解消判定サマリー**: R10-01は解消。R10-02は前回の具体的3反例と追加された同一スコープ内の構築形については解消したが、必須メタ検査を同じ意味の製品JSON構築で迂回できる別経路が残るため一部解消と判定した。未解消部分はR11-02として記録する。
- **レビュー結果**: blocker 0件、major 2件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R10-01 — 解消**: CHK-03の上限超過inventory itemとviolationsを配列index単位の最大二部マッチングで一対一対応させている。同一span・level・evidenceを持つ別構造2件にviolation 1件しかない場合、2件ある場合、inventoryを1件へ戻して余剰violationが生じる場合の回帰検査を確認し、対象テストもpassした。
- **R10-02 — 一部解消・一部未解消**: 前回示したJSON文字列の直接復元、同一スコープ内の辞書alias、反復キーによる代入は検出される。文字列結合、辞書内包、`zip`、`setdefault`、`operator.setitem`相当の追加自己負ケースもpassした。一方、モジュール定数を関数内で使うJSON復元、import alias、`dict.fromkeys`では違反リストが空になり、CI-R-03の「テストコード内JSON不在」を保証できない。詳細はR11-02に記録する。

## 独立検査の新規指摘

## R11-01 — major — `.claude/run_reviewer.py:76` / `.codex/run_reviewer.py:95`

1. **指摘内容**: 両固定レビュアーラッパーは、現在の`limits.json`を単独で読み、`review_timeout_seconds`が1以上の整数であることだけを確認して、その値で子レビュアーを起動する。現在設定全体のスキーマ検証も、`flow-state.json`に保存されたセッション設定スナップショットとの一致確認も起動前には行わない。設定不一致は、子レビュアー終了後にラッパーが生出力を`flow_control.py review`へ渡した時点で初めて`ensure_config_snapshot()`に検出される。隔離フローでrequest確定後に`review_timeout_seconds`を300からスキーマ適合値1へ変更し、起動時にmarkerを作る偽Claudeで固定ラッパーを実行したところ、markerが作成されて子レビュアーが実際に起動した後、ラッパーは`E-DATA-08`・終了1を返した。したがって最終的にはセット中止になるものの、「検証済みでセッションスナップショットと一致する現在のlimitsから期限を読み、各レビュー実行前に不一致を停止する」という必須順序を満たさない。スキーマ上限外の大きな正整数へ改変された場合は、C12が不一致を検出する前にその不当な値だけ子プロセスの壁時計期限として適用され得る。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` CCW-09・CDX-03・COR-09、`docs/architecture.md` ARC-03・CLI-35・E-DATA-05・E-DATA-08、`docs/subagent-review-spec.md` INF-07、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: ラッパーが子レビュアーを起動する前に、C12の単一実装で現在設定の検証、セッションスナップショットとの完全一致、直前action/requestの対応、および適用するスナップショット由来timeoutを一括確定できる固定preflight境界を設ける。アダプタ内へ設定検証を複製せず、preflightがE-DATA-05/E-DATA-08を返した場合は子を一度も起動せず、そのCLI-05を非改変で返す。Claude/Codex両方について、request確定後に(1)スキーマ適合のtimeoutだけを変更、(2)スキーマ不適合の大値へ変更するケースを追加し、偽レビュアーのmarkerが作られないこと、定義済みコード、state削除、監査保持を検査する。

## R11-02 — major — `tests/unit/test_fixtures.py:224` / `tests/unit/test_fixtures.py:260`

1. **指摘内容**: CI-R-03の必須メタ検査は、R10で追加された構築形以外なら製品JSONのテストコード内直書きを依然として検出できない。`contract_dict_violations()`は文字列定数と辞書builderをスコープごとに空で初期化するため、モジュール定数を関数スコープへ伝播しない。また、呼出し名はAST上の末尾名が`loads` / `literal_eval` / `dict`と完全一致する場合だけ解釈し、import aliasや`dict.fromkeys`を追跡しない。実際に同関数へ、(a)モジュール定数`PAYLOAD='{"violations": []}'`をテスト関数内の`json.loads(PAYLOAD)`で復元、(b)`from json import loads as parse_json`後に`parse_json('{"violations": []}')`、(c)`dict.fromkeys(('violations',), [])`をそれぞれ入力すると、全て違反リスト`[]`になった。いずれもR8〜R10で禁止対象とされた部分machine reportと同じ製品入力をfixture/product出力なしで作れるため、「テストコード内JSON不在を検査するメタテスト」という必須条件が構文表現だけで再び迂回される。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 任意の辞書・文字列データフローを構築形ごとに追記する方式をやめ、製品入力を取得できる入口を正準fixture loaderと検査対象製品の直前段出力APIへ限定し、対象CLI・製品関数へ届く値の取得元をallowlist検査する方式へ置き換える。現方式を維持する場合でも、モジュールから関数への定数参照、import/import-asの名前解決、`fromkeys`を含む辞書factoryを追跡し、上記3反例を自己負ケースへ追加する。CI-SCH-03の許可例は既存どおり関数・ファイル単位で狭く限定する。

## 確認できた正常系・DoD再確認

- R10-01の一対一対応について、上限超過inventory 2件/violation 1件の拒否、2件/2件の受理、余剰violationの拒否を確認した。
- R10-02の前回具体例であるJSON文字列直接復元、辞書alias、反復キーと、追加された文字列結合・辞書内包・`zip`・`setdefault`・setitemの自己負ケースが違反を検出することを確認した。
- `.venv/bin/python -m pytest tests/unit tests/replay -q`は239件全てpassした（569.68秒）。
- ゴールデン候補・machine互換の対象2件は2件passした（3.33秒）。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/unit/*.py tests/replay/*.py tests/generate_assets.py`、`jq empty .claude/settings.json`、`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- スナップショット不一致後も子レビュアーが先に起動する反例、およびモジュール定数・import alias・`dict.fromkeys`でCI-R-03メタ検査を迂回する3反例を再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。

## 要約

- blocker: 0件
- major: 2件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
