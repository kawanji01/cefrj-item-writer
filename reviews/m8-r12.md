# M8 コードレビュー R12

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R11対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-22
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r11.md`
- **前回指摘の解消判定サマリー**: R11-01は設定全体・セッションsnapshot・直前actionのパスを子起動前に検査してtimeoutを確定する部分は解消したが、実際のrequest内容を検証済み封筒および現在stateへ照合しないため一部解消。R11-02は前回の3反例を検出する部分は解消したが、同義の製品JSON復元をメタ検査が見逃す別経路が残るため一部解消。未解消部分はR12-01・R12-03として記録する。
- **レビュー結果**: blocker 0件、major 3件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R11-01 — 一部解消・一部未解消**: 両固定ラッパーはC12の`review-preflight`を子起動前に呼び、現在設定の検証、セッションsnapshotとの一致、直前`run_review` actionのrequestパス、適用timeoutを確認する。設定不一致・スキーマ不当の両ツール回帰では子markerが作られず、定義済みCLI-05とstate削除を確認した。一方、preflightはrequestファイルを読まずパス文字列だけを照合するため、実際に子へ渡す封筒の検証済み性と現在stateとの内容一致を保証しない（R12-01）。
- **R11-02 — 一部解消・一部未解消**: モジュール文字列定数、import alias、`dict.fromkeys`による前回3反例は検出され、自己負ケースもpassした。一方、検出器は`str`定数と`loads` / `literal_eval`という末尾名だけを追跡するため、別の標準JSON復元APIとbytes入力で同じ製品JSONを構築しても違反0件になる（R12-03）。

## 独立検査の新規指摘

## R12-01 — major — `scripts/flow_control.py:367` / `.claude/run_reviewer.py:76` / `.codex/run_reviewer.py:95`

1. **指摘内容**: 子起動前の`review_preflight()`は、flow-stateの`last_action.request_path`と引数文字列の一致しか確認せず、当該requestファイルを読み取りも検証も再構築もしない。正常な`candidate`処理で`run_review` actionを得た後、`review/q01.gen1.request.json`の内容をスキーマ不当な`{}`へ置き換えて同じパスで`flow_control.py review-preflight`を実行したところ、終了コード0で`request_path`とtimeoutが返った。両ラッパーの事前検査も通常ファイルであることしか確認しないため、この結果では独立レビュアーを起動する。したがって、実際に渡す封筒が`review_request.schema.json`を通過済みであるというRC-08の境界と、R11-01が求めた現在state・直前requestの対応を保証できない。スキーマ有効だがcandidate・machine_report・制約snapshotだけを改変した封筒も同じパスなら通過し、レビュー対象とC12が後で意味検証・確定する対象が分離し得る。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RC-02・RC-06・RC-08、`docs/cross-agent-compatibility.md` CCW-09・CDX-03、`docs/architecture.md` ARC-06・CLI-08、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: C12のpreflightで実requestをstrict UTF-8・標準JSON・`review_request`スキーマへ再検証し、flow-stateの現在candidate・machine report・generation・session・設定snapshotから`build_review_request()`で再構築したJS-01正準バイト列と完全一致させる。相違時は子を一度も起動せず`E-CONTRACT-01`で監査保持・state削除する。Claude/Codex両方について、(a)requestを`{}`へ変更、(b)スキーマ有効なままcandidate、machine_report、constraints_snapshotの各1箇所を変更、の回帰を追加し、偽レビュアーmarkerが作られないこととCLI-05の非改変伝播を検査する。

## R12-02 — major — `scripts/flow_control.py:140` / `docs/architecture.md:135`

1. **指摘内容**: R11対応で`flow_control.py review-preflight --set-dir ... --request ...`という新しいサブコマンドを公開parserと両固定ラッパーの実行経路へ追加したが、CLI契約の正は依然として`init` / `candidate` / `review` / `decide` / `status`の5コマンドだけを列挙し、CLI-32〜CLI-35にもpreflightの入力・stdout・エラー時のstate削除・ラッパー専用境界が定義されていない。実装の`--help`には6番目のコマンドとして現れ、製品フローが実際に依存する一方、仕様の正からは呼出し契約を導出できない。これはC12の挙動規則とCLI入出力形を設計文書に集約する規則、およびM8のCLI契約テスト前提に反する。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-02・ARC-08・CLI-06・CLI-32〜CLI-35、`docs/cross-agent-compatibility.md` COR-08・CCW-09〜CCW-10・CDX-03、`IMPLEMENTATION_PLAN.md` PLN-03・M8 DoD 1。

3. **具体的な修正案**: PLN-05の手順でpreflight境界を正式に決定し、`docs/architecture.md`のCLI一覧と5.10節、両ラッパーの配線仕様へ、固定引数、正常stdoutの2フィールド、設定/request不一致時のコード、state寿命、ホストから直接呼ばせない範囲を一意に記載する。その契約に対する`--help`、必須引数欠落、存在しないrequest、直前action不一致、正常出力のCLI回帰を追加する。新サブコマンドを公開契約へ加えない方針なら、既存の文書化済みCLI境界だけで同じpreflightを実現し、parserから未定義コマンドを除去する。

## R12-03 — major — `tests/unit/test_fixtures.py:121` / `tests/unit/test_fixtures.py:239`

1. **指摘内容**: CI-R-03の必須メタ検査はR11の3構築形を追加追跡したが、構文別blacklistのままで、テストコード内JSON不在を保証できない。`static_call_name()`は`loads` / `literal_eval`だけをJSON復元として扱い、`static_string_values()`は文字列定数だけを扱う。実際に`machine = json.JSONDecoder().decode('{"violations": []}')`と`machine = json.loads(b'{"violations": []}')`をそれぞれ`contract_dict_violations(..., TEST_PRODUCT_CONTRACTS)`へ渡すと、どちらも違反リスト`[]`になった。両方ともR8〜R11で禁止対象となった部分machine reportと同じ製品入力をfixtureまたは製品直前段出力なしで作れるため、前回反例だけを塞いでも必須メタ検査は別の標準API表現で直ちに迂回できる。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 構築APIを列挙し続ける方式をやめ、製品入力を受け取る検査対象関数・CLI呼出しをsinkとして、到達する値の由来が正準fixture loaderまたは検査対象製品の直前段出力だけであることをallowlist検査する。少なくとも上記`JSONDecoder.decode`とbytes版`json.loads`を自己負ケースへ追加するが、それだけを最終修正とせず、未知のdecoder・helper・データフローは許可済み由来を立証できなければfail-closedにする。CI-SCH-03の許可例はファイル・関数・生成箇所を限定して別扱いする。

## 確認できた正常系・DoD再確認

- R11-01の設定検査について、現在設定全体のスキーマ不当とセッションsnapshot不一致の双方でClaude/Codex子markerを作らず、`E-DATA-05` / `E-DATA-08`、監査保持、state削除となる回帰を確認した。
- R11-02の前回具体例であるモジュール文字列定数、import alias、`dict.fromkeys`の自己負ケースが違反を検出することを確認した。
- `.venv/bin/python -m pytest tests/unit tests/replay -q`は243件全てpassした（626.44秒）。対象を絞った`tests/unit/test_flow_control.py tests/unit/test_fixtures.py`も95件全てpassした（266.63秒）。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/unit/*.py tests/replay/*.py tests/generate_assets.py`、`jq empty .claude/settings.json`、`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- 内容を`{}`へ改変したrequestがpreflight終了0になる反例と、`JSONDecoder.decode`・bytes版`json.loads`でCI-R-03メタ検査を迂回する2反例を再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。

## 要約

- blocker: 0件
- major: 3件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
