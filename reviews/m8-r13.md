# M8 コードレビュー R13

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R12対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-22
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r12.md`
- **前回指摘の解消判定サマリー**: R12-01は解消。R12-02とR12-03は対応記録がなく、実装・仕様にも前回指摘の状態が残るため未解消。未解消分はR13-01・R13-02として記録する。
- **レビュー結果**: blocker 0件、major 2件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R12-01 — 解消**: `review_preflight()`は実requestを読み、`review_request`スキーマで再検証したうえで、現在のflow-stateにあるcandidate・machine report・generation・session・設定snapshotから再構築したrequestのJS-01正準バイト列と完全一致を要求する。両固定ラッパーについて、スキーマ不当、candidate改変、machine_report改変、constraints_snapshot改変の計8ケースが、子markerを作らず`E-CONTRACT-01`で停止し、flow-stateを削除して監査を保持することを確認した。
- **R12-02 — 未解消**: `reviews/m8-r12-resolutions.md`に対応記録がない。実装の`--help`には依然として第6コマンド`review-preflight`が現れる一方、`docs/architecture.md`のCLI一覧とCLI-32〜CLI-35は5コマンドだけを定義し、`docs/cross-agent-compatibility.md`にもこの内部呼出しの固定argv・正常出力・エラー・state寿命がない。R13-01として再記録する。
- **R12-03 — 未解消**: `reviews/m8-r12-resolutions.md`に対応記録がなく、検出器と自己負ケースも前回時点から変更されていない。`json.JSONDecoder().decode('{"violations": []}')`と`json.loads(b'{"violations": []}')`を再投入すると、どちらも違反リスト`[]`となった。R13-02として再記録する。

## 独立検査の指摘

## R13-01 — major — `scripts/flow_control.py:140` / `docs/architecture.md:135` / `docs/architecture.md:224`

1. **指摘内容**: 子レビュアー起動の必須経路となった`flow_control.py review-preflight --set-dir ... --request ...`が、CLI契約の正に未定義のままである。実装の公開parserと`--help`は`init` / `candidate` / `status` / `review-preflight` / `review` / `decide`の6コマンドを公開し、両固定ラッパーもこのサブコマンドへ依存する。しかし`docs/architecture.md`のCLI一覧は`init` / `candidate` / `review` / `decide` / `status`の5コマンドだけを列挙し、CLI-32〜CLI-35にもpreflightの固定引数、正常stdout、request再検証失敗時の詳細、flow-state削除、ラッパー専用境界を定義していない。`CHANGELOG.md`の実装説明は仕様の正ではないため、製品フローの呼出し契約とCLI回帰の期待値を正本文書から導出できない。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-02・ARC-08・CLI-06・CLI-32〜CLI-35、`docs/cross-agent-compatibility.md` COR-08・CCW-09〜CCW-10・CDX-03、`IMPLEMENTATION_PLAN.md` PLN-03・PLN-05・M8 DoD 1。

3. **具体的な修正案**: PLN-05の手順でpreflight境界を正式決定し、`docs/architecture.md`のCLI一覧と5.10節、両ラッパーの配線仕様へ、固定argv、正常時の`request_path` / `review_timeout_seconds`、設定・state・request不一致時のコードとdetail、異常時の監査保持・state削除、ホストから直接呼ばせない範囲を一意に記載する。その契約に対する`--help`、必須引数欠落、存在しないrequest、直前action不一致、スキーマ不当・内容不一致、正常出力のCLI回帰を追加する。公開サブコマンドにしない方針なら、既存の文書化済みCLI境界だけで同じpreflightを実現し、parserから未定義コマンドを除去する。

## R13-02 — major — `tests/unit/test_fixtures.py:121` / `tests/unit/test_fixtures.py:131` / `tests/unit/test_fixtures.py:239` / `tests/unit/test_fixtures.py:436`

1. **指摘内容**: CI-R-03の必須メタ検査は製品入力の由来を検査せず、既知のAST構築形から製品契約らしいキー集合を復元するblacklistに留まっている。`static_call_name()`は属性呼出し`JSONDecoder().decode(...)`を`decode`としか認識せず、`static_string_values()`はbytes定数を扱わないため、`machine = json.JSONDecoder().decode('{"violations": []}')`と`machine = json.loads(b'{"violations": []}')`はいずれも`contract_dict_violations(..., TEST_PRODUCT_CONTRACTS)`で違反ゼロになる。どちらも正準fixtureまたは検査対象製品の直前段出力を経ずに部分machine reportをテストへ投入でき、CI-R-03が要求する入力由来の限定と「テストコード内JSON不在」を保証しない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 構築構文を追加列挙する方式をやめ、製品入力を受け取るCLI・製品関数をsinkとして、到達する値が正準fixture loaderまたは検査対象製品の直前段出力から来たことをallowlistで立証するメタ検査へ置き換える。未知のdecoder・helper・データフローは、許可済み由来を立証できなければfail-closedにする。最低限の回帰として上記`JSONDecoder.decode`とbytes版`json.loads`を自己負ケースへ追加し、CI-SCH-03の許可例はファイル・関数・生成箇所を限定する。

## 確認できた正常系・DoD再確認

- R12-01のrequest再検証について、両ラッパー×4改変の対象回帰8件は全てpassした。
- `.venv/bin/python -m pytest tests/unit tests/replay -q`は251件全てpassした（675.84秒）。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- `flow_control.py --help`に6番目の未文書化コマンドが現れることと、`JSONDecoder.decode`・bytes版`json.loads`の2反例がメタ検査を通過することを再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。
- R12から引き継いだ上記2件以外に、新たなblocker / major / minorは検出しなかった。

## 要約

- blocker: 0件
- major: 2件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
