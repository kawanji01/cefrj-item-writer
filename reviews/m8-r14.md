# M8 コードレビュー R14

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R13対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-22
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r13.md`
- **前回指摘の解消判定サマリー**: R13-01は解消。R13-02は旧反例への対応は確認できたが、同じCI-R-03の入力由来制限をキーワード引数・sink aliasなどで迂回できるため未解消。未解消分はR14-01として記録する。
- **レビュー結果**: blocker 0件、major 1件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R13-01 — 解消**: 承認済みM8D-13、`docs/architecture.md` CLI一覧・CLI-33a・CLI-35、`docs/cross-agent-compatibility.md` COR-08a・CCW-09/10・CDX-03に、ラッパー内部専用`review-preflight`の固定argv、検査順、正常2フィールド、定義済みエラー、監査保持、flow-state寿命が記載された。`scripts/flow_control.py`は現在設定、snapshot、直前action、request通常ファイル性、スキーマ、現在stateからの正準再構築を検査し、両ラッパーは終了0のpreflight後だけ子を起動する。help、必須引数欠落、request欠落、直前action不一致、4種のrequest改変、正常系の回帰が実装され、全テストでも通過した。
- **R13-02 — 未解消**: `JSONDecoder.decode`、bytes版`json.loads`、未知decoder/helper、偽stdout、直接の`write_json(candidate_path, unknown_helper())`という対応記録の自己負ケースは検出するようになった。しかしsink検査は終端名と位置引数indexだけに依存し、キーワード引数と呼出しaliasを追跡しない。実行した3反例は全て違反リスト`[]`となったため、CI-R-03が要求するfail-closedな入力由来制限はまだ保証されない。R14-01として再記録する。

## 独立検査の指摘

## R14-01 — major — `tests/unit/test_fixtures.py:126` / `tests/unit/test_fixtures.py:143` / `tests/unit/test_fixtures.py:386` / `tests/unit/test_fixtures.py:536`

1. **指摘内容**: CI-R-03のメタ検査は製品契約sinkへの値の由来をfail-closedに検査できていない。`contract_sink_positions`はsinkごとに位置引数indexだけを列挙し、検査ループも`node.args[position]`しか確認しないため、`write_json(candidate_path, data=unknown_helper())`と、全引数をキーワード指定した`review_semantic_problems(..., machine_report=unknown_helper(), ...)`を許す。またsinkの識別は`static_call_name()`が返す終端名だけで、`alias = write_json; alias(candidate_path, unknown_helper())`も許す。実際に`contract_dict_violations(..., TEST_PRODUCT_CONTRACTS)`へこの3件を投入すると、結果は順に`[]`, `[]`, `[]`だった。さらに`tests.support.load_json`は引数パスを立証せず一律`approved`となるため、正準fixture外の手書き一時JSONも同じ由来として扱える。テストコードが正準fixtureまたは検査対象製品の直前段出力を経ずにcandidate・machine report等を製品sinkへ入力でき、メタテスト自身はpassする。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: sinkを終端名だけでなくimport元を含む完全なcall identityで定義し、代入・import aliasを追跡する。各sinkについて位置引数indexと対応するキーワード名の両方を検査し、未知の呼出し形・`**kwargs`・解決不能aliasは由来を立証できなければfail-closedにする。`load_json`を一律承認せず、candidate/review等の製品契約値については引数パスが`tests/fixtures/`・`tests/golden/`の正準資産、または製品直前段の実出力であることまで追跡する。最低限、上記3反例と正準fixture外の一時JSONを読む反例を自己負ケースへ追加する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は254件全てpassした（727.61秒）。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `jq empty .claude/settings.json`と`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`は終了コード0だった。
- R13-01のpreflight契約、実装、両ラッパー配線、および異常時の監査保持・flow-state削除に追加の不一致は検出しなかった。
- R13-02の旧反例と現在のCI-R-03自己負ケースは通過したが、R14-01の3反例が全て違反ゼロになることを再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。
- R14-01以外に新たなblocker / major / minorは検出しなかった。

## 要約

- blocker: 0件
- major: 1件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
