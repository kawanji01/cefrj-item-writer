# M8 コードレビュー R16

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R15対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-22
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r15.md`
- **前回指摘の解消判定サマリー**: R15-01で示されたloaderの直接上書きとリテラル名の`getattr` sinkは拒否されるようになり、具体的な2反例は解消した。しかし、同じ入力由来制限をプロセスhelper、別の制御フロー、反射的call、計算キー、正準ディレクトリからの遡りで迂回でき、対応記録が主張するfail-closedなscope別symbol tableには到達していないため、指摘全体は一部解消と判定した。未解消分をR16-01・R16-02として記録する。
- **レビュー結果**: blocker 0件、major 2件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R15-01 — 一部解消**: `load_json = lambda ...`による直接上書きは`contract-call-identity` / `contract-origin`として、`getattr(support, "write_json")(..., unknown_helper())`は`contract-origin`として検出されることを再現した。分割代入・属性代入・ネストscope・単純な`if`分岐の自己負ケースも追加されている。一方、実際の製品CLI入出力を経由する境界が由来検査のsinkになっておらず、`try` / `global`等のscope効果と反射的calleeもfail-openである。R15-01が要求した「全ての再束縛」と「解決不能な間接呼出し」の保守的拒否は未達である。

## 独立検査の指摘

## R16-01 — major — `tests/unit/test_cli.py:72` / `tests/unit/test_flow_control.py:1211` / `tests/unit/test_flow_control.py:1214` / `tests/unit/test_flow_control.py:1226` / `tests/unit/test_fixtures.py:233` / `tests/unit/test_fixtures.py:601` / `tests/unit/test_fixtures.py:617`

1. **指摘内容**: CI-R-03が禁止する手書きJSONが現在のテスト入力に残り、製品CLIのプロセス境界も入力由来を検査していない。`test_ci_cli_01_missing_paths`は`finalize_set.py`へ`b"{}\n"`を直接stdin送信し、`test_m8_flow_cleanup_warning_is_propagated`は成功した`finalize_set.py` / `validate.py`のstdoutとして`b"{}\n"`を3か所で手組みしている。これらはCI-SCH-03の必須欠落・型不正例ではなく、正準fixtureでも製品実装の直前段出力でもない。後者はCLI-22 / CLI-27が定める正常stdoutの形にも適合しない。メタ検査側では`run_cli` / `machine_for_path`を引数検査なしで`process`由来にし、`contract_sinks`にもプロセスhelperのstdin・入力パスがない。このため、`machine_for_path(tmp_path / "candidate.json", ...)`を`stdout_json`で復号して`write_json`へ渡す反例、および任意の一時JSONを`run_cli(..., stdin=...)`へ直接渡す反例がともに違反リスト`[]`になる。既存のCI-R-03メタテスト3件と全255テストはpassするため、現行ゲートは実在する直書きと同じ迂回を検知しない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08、`docs/architecture.md` CLI-03・CLI-22・CLI-27、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: `b"{}\n"`を、用途に対応する正準fixtureまたは製品CLIを実行して得た直前段出力へ置換する。`run_cli` / `machine_for_path`を出力sourceであると同時に入力sinkとして扱い、CLIごとの`--file` / `--candidate` / `--set` / stdin引数が正準fixture・goldenまたは検証済み製品出力に由来することを検査する。テストからの直接`subprocess.run` / `Popen`は明示allowlist外で拒否し、プロセスhelperへ非正準一時パス、手書きstdin、手組み成功stdoutを渡す自己負ケースを追加する。

## R16-02 — major — `tests/unit/test_fixtures.py:142` / `tests/unit/test_fixtures.py:324` / `tests/unit/test_fixtures.py:430` / `tests/unit/test_fixtures.py:1031` / `tests/unit/test_fixtures.py:1138` / `tests/unit/test_fixtures.py:1368`

1. **指摘内容**: R15で追加したcall identity・scope・path・dict追跡は、同義の通常のPython構文を由来不明として拒否せず、依然fail-openである。scopeの合流処理は`ast.If`だけを特別扱いするため、`try`本体で`load_json`を任意loaderへ置換し`except`側で元へ戻すコードは、実行される正常経路が任意loaderであるにもかかわらず違反リスト`[]`となる。`global`経由の再束縛、`globals()["write_json"](...)`、`support.__dict__["write_json"](...)`、計算名の`getattr`も同様に通る。さらに`static_path_origin()`は任意の属性参照を親と同じ由来にし、`canonical-path`へ続く`/`を無条件に同じ由来とするため、`load_json(FIXTURES.parent.parent / "data" / "config" / "limits.json")`を正準fixtureとして承認する。`static_dict_keys()`は解決不能キーを単に捨てるため、`{"".join(("violations",)): []}`を`canonical_bytes`でCLI stdinへ渡す製品JSON直書きも違反ゼロとなる。R15の個別構文を追加しただけでは、CI-R-03のfail-closed保証になっていない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 任意のPythonデータフローを個別構文追加で近似する方式をやめ、テストで許可する入力APIとcall形を狭く固定して、それ以外を保守的に拒否する。現方式を維持するなら、少なくとも`Try` / `Match` / loopの全出口を合流し、`Global` / `Nonlocal`の外側scopeへの効果を伝播し、`globals` / `locals` / `vars` / `__dict__` / 動的`getattr`・`setattr`を敏感symbolが関与する場合に拒否する。pathは定数成分を正規化して`tests/fixtures/`または`tests/golden/`への`relative_to`成立を確認し、`.parent` / `.parents` / `..` / 絶対成分 / 動的成分をfail-closedにする。製品入力へ到達するdictの未解決キーも承認せず、上記反例を自己負ケースへ追加する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は255件全てpassした（726.36秒）。
- CI-R-03メタ回帰3件は3件pass（0.25秒）、`tests/unit/test_fixtures.py`全体は11件pass（3.29秒）だった。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/unit/*.py tests/replay/*.py tests/generate_assets.py`、`jq empty .claude/settings.json`、`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`は終了コード0だった。
- R15-01の元の2反例は非空の違反リストになった一方、R16-01 / R16-02の`process_tmp_path`、`try_branch_rebind`、`globals_sink`、`escaped_fixture_root`、`computed_contract_key`は全て違反リスト`[]`になることを独立に再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。公開済み`v1.0.0`は移動されていない。
- R16-01・R16-02以外に新たなblocker / major / minorは検出しなかった。

## 要約

- blocker: 0件
- major: 2件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
