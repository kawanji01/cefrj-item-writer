# M8 コードレビュー R2

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8実装コミット、annotated tag `v1.0.0`）で、R1対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-20
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r1.md`
- **前回指摘の解消判定サマリー**: R1-01 解消、R1-02 解消、R1-03 未解消、R1-04 解消（3件解消、1件未解消）。R1-01は製品側の`flow_control.py`と2 provider境界へ移され、R1-02はpytest一時リポジトリへ分離され、R1-04は固定目録との完全一致検査になった。R1-03は通常テスト用ヘルパーでは製品構築関数へ移されたが、`tests/generate_assets.py`に同じ監査入力の手組みが残り、追加されたCI-R-03メタテストの検査対象外である（R2-07）。
- **レビュー結果**: blocker 1件、major 6件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R1-01 — 解消**: `tests/replay/harness.py`は`flow_control.py`のactionに応じてcandidate/review providerだけを返す構成になり、世代停止・補充・照会・確定は製品CLIへ移された。ただし新しい製品実装自体の契約違反をR2-02〜R2-06で指摘する。
- **R1-02 — 解消**: 全リプレイがpytestの`tmp_path`配下に一時リポジトリを作り、実リポジトリの`output/20990101-000000-rpl1`を作らない回帰検査も追加された。
- **R1-03 — 未解消**: `tests/support.py`とリプレイは製品構築関数を使うようになったが、資産生成元の`tests/generate_assets.py`がreview_request・AUD-11・FIN-01を引き続き辞書リテラルで再構築する。メタテストは`tests/support.py`と`tests/replay/harness.py`しか走査しないため検出しない（R2-07）。
- **R1-04 — 解消**: 公式9形式と必須ゴールデン2件が固定tupleになり、set・HTML・candidate・golden caseの実ファイル集合との完全一致をCIで検査する。

## 独立検査の新規指摘

## R2-01 — blocker — `.claude/hooks/guard_flow_control.py:140`

1. **指摘内容**: stdinを使う標準`flow_control.py`呼出しがClaude Codeでは必ずPreToolUseで拒否される。`main()`は`guard_heredoc(command)`が固定`FLOW01` / `REV02` / `ERR02`を認識して`allow()`を出力した後も、無条件に142行目の`deny()`を実行する。実際に固定`FLOW01`形をガードへ投入すると、allow JSONを出力した直後に「ヒアドキュメント形式が固定形と一致しません」と終了コード2になった。したがってClaude Codeは`init`を開始できず、review受理・process_failure受理にも到達できない。さらにこのフォールスルーだけを直しても、`REV02`本文を`parse_object()`で事前拒否するため、JSONパース不能なレビュアー生stdoutをC12へ渡して`validation_failure`監査と再実行actionを作るCOR-08/INF-01経路を実行できない。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` COR-08・CCW-11・CCW-12、`docs/subagent-review-spec.md` INF-01〜INF-04、`IMPLEMENTATION_PLAN.md` M7 DoD 2・M8 DoD 1。

3. **具体的な修正案**: ガードの許可処理を成功終了または明示的なmatched返却にして、許可後にdenyへ落ちないようにする。review生stdoutは不正JSONもC12へバイト完全に渡せる安全な固定データ経路を設計し、ガード自身がreview_resultの妥当性を判定しないようにする。固定`FLOW01`・正常`REV02`・不正JSONの`REV02`・`ERR02`が期待どおり許可され、区切り注入・追加コマンド・別引数だけが拒否されるPreToolUse単体テストと、新規`dontAsk`セッションの実コマンド確認を追加する。

## R2-02 — major — `scripts/flow_control.py:1226`

1. **指摘内容**: init時に保存した`config_snapshot`と現在の`limits.json` / `proper_nouns.json`を、各candidate・review・decide・status処理前に比較していない。非init経路は`require_basic_environment()`だけで状態を読み、candidate処理では`machine_check.py`が現在設定を読む一方、review_requestは古いstateスナップショットから作られるため、セッション中の設定変更後に1世代の機械判定とレビュー制約が異なる設定を使い得る。reviewがfailした経路ではデータ前提検査を行う子CLIへ到達せず、そのまま次世代へ進む。最終的に`finalize_set.py`がE-DATA-08を返しても、843〜847行目が一律E-CONTRACT-04へ置き換えるため、目録どおりのコード・差分・対処手順が呼出し元へ返らない。

2. **根拠となる設計文書の規則ID**: `docs/interaction-flow.md` IF-04a、`docs/json-output-spec.md` SET-02、`docs/architecture.md` ARC-06・CLI-05・CLI-08・CLI 9本契約表の`flow_control.py`前提検査・E-DATA-08、`agent/author-core.md` 第6節（C12一元管理）。

3. **具体的な修正案**: state読込み直後、statusを含む各処理前に現在の検証済み設定を取得し、stateの`config_snapshot`とJSON値で完全一致比較する。不一致はE-DATA-08の規定detailとremedyでセット中止にし、監査を保持してflow-stateを削除する。子CLI終了1はCLI-05 JSONを厳格に解析して、E-DATA-08・E-CONTRACT-03等の元コード、message、remedy、detailを誤分類せず伝播する。init後のlimits変更・proper_nouns変更、およびfinalize子CLIの各定義済みエラーを一時リポジトリで検査する。

## R2-03 — major — `scripts/flow_control.py:782`

1. **指摘内容**: 提案モードの対象不成立時、`finish_or_next()`は`unresolved_slots`の自動補充より`pending_assignments`を常に先に処理する。N>1では、例えばq01が3世代不成立になってもS2の補充q{N+1}へ直ちに進まず、未処理のq02〜qNNを先に生成する。補充を「現在の問題が不成立」の直後にキュー先頭へ入れる規範遷移と異なり、増分set_checkの確定済み集合と後続の採否・再生成経路が変わる。現行RPL-03はN=1、RPL-10は要求ID上限しか検査せずprovider要求順を検査しないため、この差を検出できない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` S2・RG-12〜RG-14・5.7節規範疑似コード、`docs/testing-and-acceptance.md` RPL-03・RPL-R-02、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 現在スロットの不成立時は、提案モード・上限未満・補充ありならそのスロットへの補充割当を直ちに開始し、未処理初期assignmentは後ろに保持する。補充不能時だけ教師照会または次の規範遷移へ進む。N=3でq01不成立・q04補充・q02未処理となるシナリオを追加し、provider要求順、slot所属、増分set_checkの比較集合を固定期待値で検査する。

## R2-04 — major — `scripts/flow_control.py:1108`

1. **指摘内容**: T8/T11後の`generate_candidate.regeneration`が、直前candidateと機械・レビュー・set_checkの区分を保持せず、`violations`を1配列へ連結した値だけを返す。`review_result.machine_check_disputes[]`と「疑いがあっても機械違反は回避対象」の指示も欠落する。RG-09が必須とする直前candidate全体、machine_report全違反、review violations全件、T11のset_check違反、disputes全件をホストへ渡せず、ARC-11aによりホストは会話履歴や監査から独自再構築することも禁止されている。RPL-02はレビューviolation 1件の6フィールド、RPL-07はコードの存在しか検査せず、candidate・区分・disputesの欠落を見逃す。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RG-09〜RG-11・FP-03、`docs/architecture.md` ARC-11a・CLI-35、`docs/testing-and-acceptance.md` RPL-R-03・RPL-02・RPL-07、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: regeneration actionへ直前candidate、machine violations、review violations、set_check violations（該当時）、machine_check_disputesを別フィールドで非改変に格納し、disputeがある場合の固定注意文も製品側で構築する。T3用diagnosticだけの形とは明確に分離する。各要素の完全一致、旧世代非累積、candidate同一性、disputeありmachine failを負ケースで検査する。

## R2-05 — major — `scripts/flow_control.py:1106`

1. **指摘内容**: 正準review監査の保存成功後に`machine_check_disputes[]`を加算するstateが存在せず、`terminal_payload()`にも合計件数がない。M8D-09後はホストが監査を再読込み・再集計してはならないため、S80開始後の完了・中止報告で必須の「機械検査誤検出疑い n件」を正しく表示する決定的な情報源がなくなった。全review fixtureのdisputesが空のため、現行リプレイはこの退行を検出しない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` FP-04、`docs/interaction-flow.md` IF-50・IF-52、`docs/architecture.md` ARC-11a、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: stateを0で初期化し、スキーマ検証・正準監査の排他保存に成功したreviewごとにdisputes件数をちょうど1回加算する。再実行invalidや状態再読込みでは加算しない。completed・aborted・teacher_consultの必要なactionへ累計を含め、ホストはその値だけを固定報告へ展開する。dispute 1件を持つmachine failレビューと、その後の完了／中止を含むリプレイを追加する。

## R2-06 — major — `scripts/flow_control.py:1070`

1. **指摘内容**: review_result受理は`validate.py`のスキーマ合格と識別子一致だけで、スキーマ間では表現できないRR記入規則を検査していない。実際に妥当なpass fixtureのCHK-03だけを`result: "fail"`へ変え、`verdict: "pass"`・`violations: []`のまま`validate.py`へ渡すと終了コード0・`valid=true`になった。`submit_review()`はこの文書を正準review監査へ保存し、machineがpassならセット横断検査・確定へ進めるため、failチェックを持つ問題を合格扱いできる。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RC-13（failの各チェックにつきviolation 1件以上）・RR-01〜RR-05・INF-05、`docs/architecture.md` CLI-33、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: C12にreview_resultの決定的な意味検査を追加し、少なくともchecksのfail集合、violations.code集合、verdictの整合、レベル系codeとexpected/actualのnull可否、not_applicable理由の機械化可能な条件を検査する。不一致は正規review監査へ保存せずINF-01のvalidation_failureとして最大3実行へ送る。各不整合をスキーマ有効fixtureで追加し、3回失敗時に世代を消費せず中止することを検査する。

## R2-07 — major — `tests/generate_assets.py:243`

1. **指摘内容**: R1-03対応後も、テスト資産の生成元が`review_request()`を独自実装し、同じ`build_golden_set()`内でAUD-11（358〜368行）とFIN-01（383〜399行）を辞書リテラルから構築している。これは製品`flow_control.py`の構築・状態遷移を通らず、将来`tests/generate_assets.py`を再実行すると、製品実装と異なる監査入力からゴールデンを再生成できる。追加されたCI-R-03メタテストは`tests/support.py`と`tests/replay/harness.py`だけを走査し、この残存再実装を常に見逃すため、R1-03の「テスト実装と期待値を同じコードで作れる」問題が資産更新経路に残っている。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・FIX-03・GLD-03、`docs/subagent-review-spec.md` RG-00、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: `tests/generate_assets.py`のreview_request・AUD-11・FIN-01手組みを削除し、製品`flow_control.py`の構築関数または実CLI actionから得た出力だけを使う。初回ゴールデン生成もcandidate/review provider以外は製品フローへ通す。CI-R-03メタテストの走査対象へ資産生成スクリプトを含め、別名変数を介したdict、helper返却、直接`write_bytes`も含めて禁止入力の生成を検出する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は155件全てpassした（330.86秒）。ただしR2-01〜R2-07の負経路がテスト対象外または不十分なため、このpassだけではM8 DoD 1の充足証拠にならない。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙・文法とも追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile`による新規・変更Pythonファイルの構文検査はpassした。
- 固定`FLOW01`形のPreToolUse再現はallow出力後に終了コード2で拒否され、R2-01を再現した。
- CHK-03=`fail`・verdict=`pass`・violations空のreview_resultは`validate.py`で終了コード0・`valid=true`となり、R2-06のスキーマ外意味検査欠落を再現した。
- `git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。

## 要約

- blocker: 1件
- major: 6件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
