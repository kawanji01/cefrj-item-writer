# M8 コードレビュー R3

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1/R2対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-20
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r2.md`
- **前回指摘の解消判定サマリー**: R2-01 未解消、R2-02〜R2-07 解消（6件解消、1件未解消）。R2-01のallow後フォールスルーと不正JSON受理は修正されたが、終端語を本文中へ先置きする区切り注入をガードが許可するため、同指摘が要求した複合コマンド拒否は未達である（R3-01）。
- **レビュー結果**: blocker 1件、major 7件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R2-01 — 未解消**: `FLOW01` / `REV02` / `ERR02` の一致後は成功終了し、不正JSONのreview生stdoutもC12へ渡るようになった。一方、`REV02`または`ERR02`の終端行を本文の先頭にも置くと、ガードの貪欲な本文正規表現は末尾側を終端とみなしてallowするが、実シェルは先頭側でヒアドキュメントを終了する。後続行がコマンドとして実行可能であり、R2-01の修正条件だった区切り注入・複合コマンド拒否を満たさない（R3-01）。
- **R2-02 — 解消**: `status`を含む全非init処理が開始時設定と現在値を比較し、不一致時はE-DATA-08の差分・remedyを返してstateを削除する。構造化された子CLI終了1も元のcode/detail/remedyを伝播する。
- **R2-03 — 解消**: 不成立スロットを`unresolved_slots`の先頭で処理し、自動補充または教師照会を未処理初期assignmentより優先する。N=3のq01→q04→q02順も固定検査されている。
- **R2-04 — 解消**: T8/T11再生成actionは直前candidate、machine/review/set_check違反、disputesを別フィールドで保持し、dispute時の固定注意文を含む。T3診断専用形とも分離されている。
- **R2-05 — 解消**: 正準review監査の排他保存後だけdispute数を加算し、完了・中止・教師照会actionへ累計を含める。
- **R2-06 — 解消**: failチェックとviolation、verdict、適用外条件、レベル値null規則を意味検証し、不一致を正準review監査へ保存せずINF再実行へ送る。
- **R2-07 — 解消**: ゴールデンset生成は製品`flow_control.py`を通り、review_requestは製品構築関数を使用する。資産生成器内のAUD-11/FIN-01手組みも除去され、CI-R-03走査へ生成器が追加された。

## 独立検査の新規指摘

## R3-01 — blocker — `.claude/hooks/guard_flow_control.py:66`

1. **指摘内容**: reviewer生stdoutとプロセスstderrを展開する`REV02` / `ERR02`ヒアドキュメントは、本文中の早い終端行によるシェルコマンド注入を防げない。たとえば本文を`REV02`行、任意コマンド行、末尾の`REV02`行とすると、`REVIEW_RE`の`[\s\S]*`は末尾の終端までを本文として`fullmatch`し、ガードは終了コード0・allowを返す。しかし実シェルは最初の`REV02`行でstdinを閉じ、その次の行をコマンドとして実行する。`ERR02`も同じである。実際にこの2形をPreToolUseガードへ投入すると、どちらもallowになった。現行単体テストは「末尾終端の後にコマンドを置く」形だけを拒否確認しており、先行終端を検査しない。独立レビュアー出力は候補文からのプロンプト注入も受け得る非信頼入力なので、Claude Code標準フローからリポジトリ内の任意コマンドを実行できる。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` COR-08・CCW-10・CCW-11・CCW-12、`docs/subagent-review-spec.md` INF-01・AUD-09、`IMPLEMENTATION_PLAN.md` M7 DoD 2・M8 DoD 1。

3. **具体的な修正案**: reviewer生bytes・stderrをシェルコマンド文字列へ補間しない。固定argvのシェル不使用ブリッジから`flow_control.py review`のstdinへ直接渡すか、ラッパーが排他的な固定rawファイルへ保存し、ガード済み固定パスを`--file`で受ける配線へ変更する。任意bytesをAUD-09へ完全保存できる経路を維持し、本文に`REV02` / `ERR02`の単独行、改行、NUL以外の制御文字、シェルメタ文字、追加終端がある負ケースで「コマンド非実行かつ生bytesがC12へ到達」を検査する。

## R3-02 — major — `scripts/flow_control.py:1278`

1. **指摘内容**: エラーによる終端actionが、IF-52に必要な定義済みエラーコードと日本語remedyを返さない。review受理が3実行連続で失敗すると`{"action":"aborted","reason":"review_invalid",...}`だけを終了コード0で返し、E-CONTRACT-01・message・remedy・detailを欠く。再現でも最終actionは`review_invalid`だけだった。同様に最終`set_check`がfailしたS10経路も、1009〜1010行目で`reason: "final_set_check"`だけを返し、規定のE-CONTRACT-04を失う。C1はC12 action以外から中止理由を再構築できないため、教師へ`エラーコード{code}`と対処手順を提示できない。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-09・ARC-11a・CLI-05・CLI-35・E-CONTRACT-01・E-CONTRACT-04、`docs/subagent-review-spec.md` T7・S8・S10・INF-03、`docs/interaction-flow.md` IF-52。

3. **具体的な修正案**: T7のaborted actionへE-CONTRACT-01のCLI-05完全形を、S10へE-CONTRACT-04の完全形を構造化して含め、ホストが値を非改変でIF-52へ展開できるようにする。教師選択によるabortとはフィールドで区別する。3種のreview invalid、process_failure 3回、最終set_check failについて、code/message/remedy/detailとIF-52入力が揃うことを回帰検査する。

## R3-03 — major — `scripts/flow_control.py:1422`

1. **指摘内容**: `flow-state.json`の削除は正常な`completed` / `aborted` actionにしか実行されず、定義済みCLIエラーでS99へ停止する経路はstateを残す。たとえば正準review監査名を先に衝突させると、`review`はE-DATA-07・終了1を返すが、`.staging/flow-state.json`は残った。ホストはこのエラーを中止として報告する一方、C12状態は直前actionのままで、`status`や再投入による再開が可能に見える。これは「教師照会中だけ保持」「完成・中止時はC12が削除」「クラッシュ後も再開しない」という一時状態契約に反する。設定不一致だけは`ensure_config_snapshot()`が個別削除しているが、他のE-DATA/E-CONTRACT/E-ENV停止に一般化されていない。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-14・CLI-35・E-DATA-07、`docs/subagent-review-spec.md` RG-07・S8・AU-02a、`docs/interaction-flow.md` IF-52。

3. **具体的な修正案**: S80後に定義済みエラーで停止する全経路をC12内で終端化し、監査を保持したままstateとC12所有の許可済み一時ファイルだけを削除する共通処理を設ける。既存監査や衝突対象は変更しない。E-DATA-07、子CLIのE-DATA-08/E-CONTRACT-03/04、E-ENV-05、入力監査保存後の失敗について、終了1後にstateがなく、監査が不変で、再開できないことを一時リポジトリで検査する。

## R3-04 — major — `scripts/flow_control.py:1022`

1. **指摘内容**: `finalize_set.py`が終了0でstderrへ返す`W-CLEANUP-01`をC12が捨てる。`require_child_success(finalized, ...)`は終了0なら即returnし、その後`finalized.stderr`を解析せず、`completed` actionにも警告を含めない。set.json公開後の一時リンク削除失敗では、C1はC12 actionだけに従うため、IF-51aの`temp_path`・remedyを教師へ提示できず、残留リンクの存在と削除対象が失われる。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-11a・CLI-21手順8・CLI-22a・CLI-35、`docs/interaction-flow.md` IF-51a、`agent/author-core.md` 第11.5節。

3. **具体的な修正案**: finalize終了0のstderrが非空ならCLI-22aの正準警告JSONとして厳格検証し、`completed` actionへ`warning_code`・`detail.temp_path`・`message`・`remedy`を非改変で含める。C1がIF-51aを表示し、再finalizeせずset.json成功を維持できるようにする。公開後unlink失敗を注入するテストで、set.json存在・completed維持・警告完全伝播を検査する。

## R3-05 — major — `scripts/flow_control.py:1246`

1. **指摘内容**: reviewの意味検証が`machine_check_disputes[]`を対応するmachine違反へ照合しない。machine verdict=`pass`・`violations=[]`でも、スキーマenum内の任意Vコードと架空locationを持つdisputeを追加したreviewが受理される。実際にpass machineへ存在しない`V-LEN-01` disputeを付けると、フローは`completed`となり、誤検出疑い件数1を報告した。現行`test_m8_flow_regeneration_separates_sources_and_counts_disputes`もmachine violationsが空なのに`V-LEX-01` disputeを付け、この不正状態を正常系として固定している。機械違反の引用ではない報告が運用上の誤検出件数と再生成指示を汚染する。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` FP-01・FP-02・FP-03・FP-04・INF-05、`docs/cefrj-validation-spec.md` MC-03、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: `machine_check_disputes[]`の各要素について、`machine_violation_code`と引用`location`が現在の`machine_report.violations[]`の同一要素に存在することを決定的に照合する。machine passまたは対応違反なしは意味契約不通過として正準review監査・件数加算を行わず、INF再実行へ送る。実在V違反の正常dispute、存在しないVコード、別location、W系相当、machine passの負ケースを追加する。

## R3-06 — major — `scripts/flow_control.py:808`

1. **指摘内容**: 外部candidate/reviewの検証に使う`validate.py`の非0終了を、終了コードやCLI-05 error_codeに関係なくprovider出力不通過へ読み替える。`validate.py`が未定義内部エラーの終了2を返しても、`validation_failure`監査を作り、candidateなら再指示・世代消費、reviewなら最大3回再実行・セット中止へ進む。スキーマ欠落等のE-ENV/E-DATA系終了1も同様である。これによりCLI内部バグや環境障害が問題品質の試行として数えられ、全CLI共通の終了2契約とRG-04の世代消費条件が失われる。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` CLI-02・CLI-05・CLI-08、`docs/subagent-review-spec.md` RG-04・INF-01・INF-06、`agent/author-core.md` 第6節・第11.5節。

3. **具体的な修正案**: `validate_external()`でreturncodeを分岐し、外部入力に帰属するE-CONTRACT-01/E-INPUT-03だけをcandidate T2/T3またはreview INFへ送る。他の定義済み終了1はCLI-05完全形を伝播し、終了2や終了1の非CLI-05出力は内部バグとしてflow_control自身も終了2にする。candidate/reviewそれぞれでvalidateの終了0、対象入力不当の終了1、環境系終了1、終了2を注入して遷移・世代数・監査有無を固定検査する。

## R3-07 — major — `tests/replay/test_replay.py:189`

1. **指摘内容**: RPL-10が規定の最悪コスト境界を実行せず、誤った小さい期待値をpassさせる。仕様はN=3なら試行ID`q01`〜`q06`の6対象・各3世代、合計18生成を上限として、上限外q07をfixtureに置いても要求しないことを求める。現行scenarioはq01と補充q04〜q06だけの4対象で教師照会に止まり、テストは`attempts_total == 4 * 3`、q02/q03/q07への要求なしを明示的にassertする。したがって未処理初期スロットを減数判断後に続行する経路、全6対象での2N到達、q07遮断を検証せず、M8 DoD 1のRPL-10実装になっていない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` RPL-10・RPL-R-02、`docs/requirements.md` NFR-02、`docs/subagent-review-spec.md` RG-03・RG-14・5.7節規範疑似コード、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: scenarioに教師判断入力を明示できるようにし、`teacher_consult`で現在スロットを`reduce`して未処理初期スロットへ進む操作を`flow_control.py decide`へ渡す。q01・q04〜q06・q02・q03の6対象を各3世代failさせ、上限外q07のstepも置いたうえで、生成/レビュー要求18回、対象集合q01〜q06、q07要求・監査なし、試行数6/6を検査する。教師判断はfixture providerの第3境界にせず、決定的CLIへの外部入力イベントとして扱う。

## R3-08 — major — `tests/acceptance/records/v1.0.0.md:69`

1. **指摘内容**: 手動受け入れ記録と`v1.0.0`タグが、現在レビューしているリリース内容を証明していない。記録は150 tests passとし、タグ`v1.0.0^{}`も`HEAD=c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`を指す。一方、現在の作業ツリーはその後に`flow_control.py`、Claudeガード、コア・設計文書、リプレイを追加・変更し、実テストは164件である。A-02〜A-15のset_idは新C12導入前に生成されており、特にClaude Codeの新しいstdin配線、終端action、教師判断、警告伝播を一度も完走確認していない。最終修正を含まないコミットへ既にタグを付けた状態なので、M8 DoD 3・4と「受け入れ後にリリース対象へタグ」の順序を満たさない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` ACC-01・ACC-02・ACC-07・ACC-11、`docs/architecture.md` VER-08・VER-09・OPS-03、`IMPLEMENTATION_PLAN.md` M8 DoD 3・4。

3. **具体的な修正案**: コードレビュー収束後の最終コミット候補で第1層・第2層とA-01〜A-15を再実施し、同じ受け入れ記録へ新ラウンド、実行件数、現行C12を使ったset_idと結果を追記する。その確定コミットをリリースタグが指すようにする。既に公開済みの`v1.0.0`を移動するか後続版を発行するかは現行文書に安全な手順がないため、必要ならPLN-05でタグ修復方針を承認してから実施する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は164件全てpassした（385.60秒）。ただしR3-01・R3-05・R3-07の負ケースが欠落または誤期待のため、このpassだけではM8 DoD 1を満たさない。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙・文法とも追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py`と`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- `REV02` / `ERR02`の先行終端を持つコマンド全文はPreToolUseガードでともに終了0・allowとなり、R3-01を再現した（実シェルで後続コマンドは実行していない）。
- review invalidを3回投入した最終actionは`reason=review_invalid`だけで、error_code/remedyを持たなかった。E-DATA-07監査衝突後も`flow-state.json`が残ることを確認した。
- machine pass・violations空に架空の`V-LEN-01` disputeを付けたreviewは受理され、セットがcompleted・誤検出疑い1件となることを確認した。
- `v1.0.0^{}`と`HEAD`はいずれも`c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`で、R1/R2対応はタグ外の作業ツリー差分である。

## 要約

- blocker: 1件
- major: 7件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
