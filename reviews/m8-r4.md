# M8 コードレビュー R4

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R3対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-20
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r3.md`
- **前回指摘の解消判定サマリー**: R3-01・R3-02・R3-04〜R3-08は解消、R3-03は未解消（7件解消、1件未解消）。R3-08は承認済みM8D-11の後続リリースゲートへ移行しており、現時点で受け入れ合格・`v1.0.1`リリース完了を主張していない点を含めて解消と判定した。R3-03は通常のC12処理内では修正されたが、共通事前検査が終端後始末の外に残る（R4-01）。
- **レビュー結果**: blocker 0件、major 5件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R3-01 — 解消**: 両ラッパーはreviewer bytesをシェル文字列へ展開せず、固定argv・バイトstdinでC12へ渡す。旧REV02/ERR02許可は撤去され、区切り語・NUL・制御文字を含む生bytesがAUD-09へ完全保存される負ケースも追加された。Codex作業ファイル異常時のcode/stderr置換は別の契約問題としてR4-03に記録する。
- **R3-02 — 解消**: review受理3回失敗と最終set_check failの`aborted` actionは、教師選択と区別するreasonおよびCLI-05完全形のerrorを返す。
- **R3-03 — 未解消**: C12の主要処理で定義済み停止・内部例外時にstateを削除する共通処理は追加された。しかし`require_basic_environment()`がその`try`より前にあり、S80中のE-ENV-04等ではstateが残って再開可能に見える（R4-01）。
- **R3-04 — 解消**: finalize終了0の正準`W-CLEANUP-01`は厳格検査され、completed actionへ4フィールドが非改変で伝播する。
- **R3-05 — 解消**: disputeのcode/location組は現在のmachine violationsと照合され、不在・別location・machine passは正準review監査と件数加算の前にINF再実行へ送られる。
- **R3-06 — 解消**: candidate/reviewの外部入力検証では、provider起因のE-CONTRACT-01/E-INPUT-03だけがinvalid監査へ変換され、他の定義済み終了1と終了2は試行失敗へ読み替えられない。ほかの子CLIに残る終了2の変換は別問題としてR4-02に記録する。
- **R3-07 — 解消**: RPL-10は製品`decide` CLIへの教師判断イベントを使い、q01・q04〜q06・q02・q03の6対象を各3世代、計18試行する。上限外q07はfixtureに存在しても要求・監査されない。
- **R3-08 — 解消**: 公開済み`v1.0.0`を移動せず、レビュー収束後の最終候補でA-01〜A-15を再実施し、合格時だけ`v1.0.1`記録・CHANGELOG・annotated tagを作るゲートがM8D-11として仕様化された。現CHANGELOGもDoD 3〜5を未実施と明記しており、旧受け入れを現差分の証拠に読み替えていない。

## 独立検査の新規指摘

## R4-01 — major — `scripts/flow_control.py:1562`

1. **指摘内容**: S80中の共通事前検査エラーが終端後始末を通らず、`flow-state.json`を残す。`run()`は`require_basic_environment(repo_root)`を1570行目の`try`より前に実行するため、進行中セットでスキーマ欠落などのE-ENV-04が発生すると、終了コード1を返しても1592行目の`cleanup_state()`へ到達しない。隔離リポジトリでreview待ち状態を作り`schemas`を欠落させて`status`を実行すると、E-ENV-04・終了1の後も`.staging/flow-state.json`が存在した。これはR3-03が要求した「S80後の終了1/2は再開不能」の全経路化を満たさない。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-14・CLI-35、`docs/interaction-flow.md` IF-04a・IF-52、`docs/subagent-review-spec.md` RG-07・AU-02a。

3. **具体的な修正案**: 非initコマンドではset-dirを確定した直後から共通終端処理の範囲に入れ、`require_basic_environment()`を含む全事前検査・状態読込み・処理・永続化を同じ`try`で囲む。E-ENV-01/02/04、E-DATA系、内部例外を注入し、終了1/2後に監査正本は不変、flow-stateは不存在、`status`で再開できないことをCLIプロセスで固定検査する。

## R4-02 — major — `scripts/flow_control.py:400`

1. **指摘内容**: provider用`validate.py`以外の子CLIが終了2で内部エラーになっても、C12が定義済み契約エラー・終了1へ変換する。`require_child_success()`はreturncode 1のCLI-05完全形以外を全て`contract_failure(fallback_code, ...)`にするため、`machine_check.py`の終了2はE-CONTRACT-01、最終`set_check.py`や`finalize_set.py`の終了2はE-CONTRACT-04として表面化する。実際にreturncode 2・stderr=`internal traceback`を渡すと`CliFailure E-CONTRACT-01`になった。未定義バグと定義済み入力/契約違反の区別が失われ、修正対象を教師入力や監査契約へ誤誘導する。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` CLI-02・ERR-03・CLI-33、`IMPLEMENTATION_PLAN.md` PLN-08。

3. **具体的な修正案**: `require_child_success()`も`validate_external()`と同じ分類にし、終了1かつCLI-05完全形だけを非改変伝播し、終了2、終了1の非CLI-05、その他の異常終了は`RuntimeError`等の未定義内部例外としてC12自身も終了2にする。machine_check、増分/最終set_check、finalize、完成後validateの各呼出しへ終了1完全形・不正終了1・終了2を注入し、終了コード、state削除、監査不変を検査する。

## R4-03 — major — `.codex/run_reviewer.py:324`

1. **指摘内容**: `codex exec`が終了0でも`--output-last-message`ファイルが欠落・空・非通常ファイルの場合、実終了コードと生stderrをAUD-09へ保存しない。`ReviewerOutputError`を捕捉するとreturncodeを70へ、stderrをラッパー生成文へ置換してからC12へ渡す。偽codexを終了0・生stderr=`raw-codex-stderr`・作業ファイル未作成で実行すると、invalid監査は`exit_code: 70`と「最終メッセージを開けません」という合成stderrになり、実値0と生stderrが失われた。作業パスがディレクトリの場合は続く`_remove_work_file()`が`fail()`してC12自体を呼ばず、INF再実行actionとinvalid監査も作られない。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` COR-08・CDX-07・CDX-08、`docs/subagent-review-spec.md` INF-01・INF-04、`docs/json-output-spec.md` AUD-09。

3. **具体的な修正案**: 子codex終了後の作業ファイル不成立では`ReviewerProcessResult`の実returncode（終了0なら0）と生stderrを保持したまま`--process-failure`へ渡す。作業ファイル後始末の失敗がC12送信を先取りしない構造にし、欠落・空・通常ファイルでない・symlinkの各ケースで`process_failure.exit_code`と`stderr_base64`が実値と完全一致し、同一requestの再実行actionが返ることを検査する。

## R4-04 — major — `scripts/flow_control.py:1234`

1. **指摘内容**: candidate provider rawを、対応する正規candidate監査またはinvalid監査の排他保存より前に削除する。`validate_external()`の直後にある`finally`が常に`cleanup_candidate_provider_input()`を実行し、その後で初めてcandidate識別子照合と`write_audit()`を行う。このため、正規監査名を事前衝突させた隔離実行ではE-DATA-07で停止し、衝突対象は保持された一方、新しい`candidate.raw1.json`は監査保存前に消失した。子validate内部エラーでも同じ順序でrawだけが失われる。

2. **根拠となる設計文書の規則ID**: `docs/json-output-spec.md` JS-02、`docs/subagent-review-spec.md` AU-02a・AU-05、`docs/architecture.md` ARC-14・CLI-33。

3. **具体的な修正案**: raw削除を分岐ごとの監査保存成功後へ移す。妥当candidateは正規`candidate.json`の排他保存後、不当candidateは対応するAUD-09 invalid監査の排他保存後にだけ削除する。監査衝突、validateの定義済み環境エラー、内部終了2、raw削除失敗を注入し、保存前の生入力を失わず、成功時だけ`.staging`が所定どおり空になることを検査する。

## R4-05 — major — `scripts/flow_control.py:922`

1. **指摘内容**: CHK-03のレベル違反が封筒の決定的なL_ctxと整合するかを意味検証せず、実際には上限内の構造を「超過」とするreviewを正準受理する。現実装はレベル系violationの2値が非nullであることしか見ない。現行RPL-02のA2語彙candidateに対する`fail_q01_gen1.json`は`expected_level: A1.3`、`actual_level: A2.1`だが、当該封筒のL_ctxは`ceiling(A2)=A2.2`なので、expectedはA2.2でなければならず、actual A2.1は超過でもない。それでも`review_semantic_problems()`は問題0件を返し、リプレイはこの偽CHK-03 failを世代消費の正常入力としている。誤った独立レビューが監査・再生成判断を汚染する。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RC-06・CHK-03・RR-03・RR-04・INF-05、`docs/cefrj-validation-spec.md` LVL-08・LVL-11、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: C12でcandidateから封筒と同じL_ctxを決定的に導出し、CHK-03 violationごとに`expected_level == L_ctx`、`actual_level`がCEFR-J 9段階でL_ctxを真に超えること、対応する文法構造インベントリ記録と根拠が存在することを検査する。文法/語彙形式の各上限、同値・下位・上位、スケール混在を負ケース化し、RPL用fail fixturesも実際に超過するcandidate/reviewの組へ修正する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は175件全てpassした（458.26秒）。ただしR4-01〜R4-05の負ケースが欠落し、RPL-02はR4-05の不整合fixtureを正常入力としているため、このpassだけではM8 DoD 1を満たさない。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙・文法とも追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/replay/*.py tests/unit/*.py`と`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- E-ENV-04事前検査後のstate残置、子CLI終了2のE-CONTRACT-01変換、Codex作業ファイル欠落時のexit code/stderr置換、candidate監査衝突時のraw先行削除、A2 candidateに対する偽CHK-03 fail受理をそれぞれ隔離実行または純関数呼出しで再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。

## 要約

- blocker: 0件
- major: 5件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
