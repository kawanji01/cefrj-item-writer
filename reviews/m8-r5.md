# M8 コードレビュー R5

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R4対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-20
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r4.md`
- **前回指摘の解消判定サマリー**: R4-01〜R4-04は解消、R4-05は一部解消・一部未解消（4件解消、1件未解消）。R4-05はL_ctx、真のレベル超過、CEFR-J 9段階の検査は追加されたが、violationと実candidate・インベントリ根拠の対応検査が不足する（R5-02）。
- **レビュー結果**: blocker 0件、major 4件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R4-01 — 解消**: 非init処理はset-dir検証後から共通例外処理内に入り、`require_basic_environment()`を含む事前検査の終了1/2でflow-stateを削除する。E-ENV-04注入後に監査正本を保持し、再度の`status`をE-CONTRACT-03で拒否する負ケースも追加された。
- **R4-02 — 解消**: 子CLIの終了1かつ認識可能なCLI-05 JSONは`CliFailure`として伝播し、非CLI-05終了1と終了2は内部例外になる。machine_check、増分/最終set_check、finalize、完成後validateの5境界で終了2後のstate削除を検査している。CLI-05認識範囲に残る別の不一致はR5-04に記録する。
- **R4-03 — 解消**: Codex作業ファイルが欠落・空・directory・symlinkでも、子`codex exec`の実returncodeと生stderrを変えずC12へ渡し、同一requestのINF再実行actionとAUD-09監査を作る。作業ファイル後始末失敗もC12送信を先取りしない。
- **R4-04 — 解消**: candidate rawは正規candidate監査またはinvalid監査の排他保存成功後だけ削除される。監査衝突、validateの定義済み/内部異常、raw削除失敗の各ケースで、保存前rawまたは保存済み正準監査を保持する負ケースが追加された。
- **R4-05 — 一部解消・一部未解消**: `expected_level == L_ctx`、`actual_level`がCEFR-J 9段階でL_ctxを真に超えること、および同じactual levelを持つインベントリ要素の存在は検査するようになり、RPL fail fixtureもA2.2→B1.1へ整合した。一方、インベントリspanがcandidate中の実在箇所であることと、violationの根拠が当該インベントリ根拠と対応することを検査しないため、R4-05が求めた対応確認は未完了である（R5-02）。

## 独立検査の新規指摘

## R5-01 — major — `scripts/flow_control.py:866`

1. **指摘内容**: 空のcandidate生出力が、AUD-09自身が不適合とする`validation_failure`監査を作る。`validate_external()`はpayloadが0バイトでも`validation_failure(payload, ...)`を返すため、`raw_output_base64`は空文字列になる。しかしAUD-09は`validation_failure`の復号結果を1バイト以上とし、空出力は`process_failure`へ分類する。実際に`validation_failure(b"", ...)`を正準保存して製品`validate_invalid_audit()`へ渡すと「raw_output_base64が空バイト列です」と拒否された。この空出力が同一世代の1回目で、2回目candidateが妥当でも、後続set_checkが先のinvalid監査をE-CONTRACT-03として拒否するため、仕様上認められたT2の再指示から正常復帰できない。review側も固定ラッパーを経ない空の`--file`入力では同じ不正監査を作る。

2. **根拠となる設計文書の規則ID**: `docs/json-output-spec.md` JS-02・AUD-09、`docs/subagent-review-spec.md` T2・T3・INF-04・AU-07、`IMPLEMENTATION_PLAN.md` M8 DoD 1（RPL-06）。

3. **具体的な修正案**: `validate_external()`または呼出し側で0バイトを先に判定し、生出力未取得/空出力としてAUD-09 `process_failure`（取得不能のexit codeはnull、stderr未取得なら空Base64）を構築する。candidateの1回目空出力→2回目妥当→review/set_check/finalize完了、およびreview空入力→同一request再実行の負/復帰ケースを追加し、全invalid監査を`validate_invalid_audit()`へ通す。

## R5-02 — major — `scripts/flow_control.py:891`

1. **指摘内容**: R4-05対応のCHK-03意味検証は、架空のspanと無関係な根拠を持つreviewを正準受理する。`chk03_inventory_matches()`はインベントリの`span`がviolationの自由記述`location`に部分文字列として含まれること、actual levelが一致すること、reviewer estimateではviolation側`evidence`に文字列`reviewer_estimate`があることしか確認しない。インベントリspanがcandidateの検査対象英文に実在するか、両evidenceが同じ推定根拠かは確認しない。`replay_q04_pass.json` / `fail_q04_gen1.json`を基に、インベントリspanとlocationをcandidateに存在しない`invented span not in candidate`へ揃え、インベントリとviolationへ互いに異なる`reviewer_estimate`根拠を入れても、`review_semantic_problems()`は問題0件を返した。このreviewはINF-05へ送られず、架空のレベル超過を理由に世代を消費できる。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-03・RR-02・RR-04・INF-05、`docs/cefrj-validation-spec.md` LVL-08・LVL-11、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 形式別にCHK-03の検査対象英文フィールドをcandidateから決定的に列挙し、各インベントリspanがいずれかの実英文に完全な語列として存在することを確認する。CHK-03 violationは、そのspanを完全な引用として含むlocation、同じ導入レベル、同じ`level_source`根拠を持つ一意なインベントリ要素へ対応付ける。`kyoinban`では同じgp ID・`level_raw`、`reviewer_estimate`では同じ推定根拠を要求し、架空span・部分span・別根拠・候補内の別フィールドを負ケース化する。

## R5-03 — major — `.claude/run_reviewer.py:157` / `.codex/run_reviewer.py:245`

1. **指摘内容**: 両固定ラッパーは期限超過前に子レビュアーが出力した生stderrを破棄し、ラッパー生成文へ置換する。`run_with_timeout()`は`TimeoutExpired`でプロセスグループを停止した後、pipeを`communicate()`で回収せず、stderrを持たない`ReviewerTimeoutError`だけを送出する。`main()`はそれを終了124と日本語の合成stderrへ置換してC12へ渡す。このため、期限前に子がstderrへ出した任意バイト（NUL・非UTF-8・診断を含む）はAUD-09に残らず、COR-08の生stderr完全転送とAUD-09のバイト完全監査を満たさない。既存テストは期限とプロセスグループ消滅だけを確認し、期限前stderrの保存を検査していない。

2. **根拠となる設計文書の規則ID**: `docs/cross-agent-compatibility.md` COR-08・COR-09・CDX-07、`docs/subagent-review-spec.md` INF-04・INF-07、`docs/json-output-spec.md` AUD-09。

3. **具体的な修正案**: TERM/KILL後に同じ`Popen`へ`communicate()`してstdout/stderr pipeを最後まで回収し、終了124・stdout空・回収した生stderrの`ReviewerProcessResult`を返す。タイムアウト理由は生stderrを置換せずexit code 124で表現する。Claude/Codex双方で、子がNUL等を含むstderrを書いてから停止する偽実行を使い、AUD-09の`stderr_base64`が書いた全バイトと一致し、同一requestの再実行actionになることを検査する。

## R5-04 — major — `scripts/flow_control.py:427`

1. **指摘内容**: 子CLIの定義済み終了1を識別する`child_cli_failure()`がCLI共通契約と一致しない。CLI-02はエラーJSONをstderrの最終行へ出す契約で、CLI-03はstderrへの診断ログを許可するが、実装はstderr全体を単一JSONとして`strict_json_loads()`する。このため、許可された診断行の後に正しいCLI-05 JSONを出す子を内部バグへ誤分類し、C12自身が終了2になる。再現上も`diagnostic\n<CLI-05 JSON>\n`は`child_cli_failure()`で認識されなかった。逆に、固定4キーさえあれば目録外の`error_code`も定義済み`CliFailure`として伝播するため、ERR-01にないコードを終了1で表面化できる。R4-02の主要な終了2分類は直ったが、CLI-05完全形の認識境界がまだ過不足を持つ。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` CLI-02・CLI-03・CLI-05・ERR-01・ERR-03、`IMPLEMENTATION_PLAN.md` PLN-08。

3. **具体的な修正案**: CLI-02どおりstderrの最終非空行だけをstrict UTF-8・標準JSONとして解析し、CLI-05の固定キー・型に加えて`error_code`が第6節の目録内であることを検査する。目録外コード、JSON後の余分な非空行、不正UTF-8、欠落キーは内部異常として終了2にする。診断前置行＋正しい最終JSON、目録外コード、非CLI-05終了1を、各子CLI境界の伝播/内部異常テストへ追加する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は201件全てpassした（520.93秒）。ただしR5-01〜R5-04の負経路が未検査であり、R5-02は既存意味検証を直接迂回できるため、このpassだけではM8 DoD 1を満たさない。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/replay/*.py tests/unit/*.py`、`.venv/bin/python -m json.tool .claude/settings.json`、`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- 空payloadの`validation_failure`を製品AUD-09検証へ渡すと空Base64違反になること、架空CHK-03 span・異なる推定根拠が意味検証を問題0件で通ること、診断前置行付きCLI-05最終行を`child_cli_failure()`が認識しないことを純関数経路で再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。

## 要約

- blocker: 0件
- major: 4件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
