# M8 コードレビュー R6

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R5対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-21
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r5.md`
- **前回指摘の解消判定サマリー**: R5-01・R5-03・R5-04は解消。R5-02はcandidate内のspan実在、完全語列、field引用、reviewer_estimate根拠一致までは解消したが、kyoinban根拠を正規化文法データへ結び付けず、gp IDも部分文字列で照合するため一部未解消（3件解消、1件一部未解消）。未解消部分はR6-01に記録する。
- **レビュー結果**: blocker 0件、major 3件、minor 0件を検出した。

## 前回指摘の個別解消判定

- **R5-01 — 解消**: 0バイトのcandidate/review生出力は、空Base64の`validation_failure`ではなく、`exit_code: null`・空stderrの`process_failure`として保存される。candidateとreviewの各初回空出力後に正常入力で復帰し、set_check・finalizeまで完走する回帰も追加された。
- **R5-02 — 一部解消・一部未解消**: 9形式別のCHK-03対象英文をcandidateから列挙し、インベントリspanの実在と完全語列、violation.locationのfield・完全引用、reviewer_estimateの根拠完全一致を検査するようになった。一方、kyoinban分岐はgp IDとレベル文字列がevidence中の部分文字列であることしか確認せず、正規化文法データの実在ID・教員版レベルと照合しないため、同一根拠への結び付けは未完了である（R6-01）。
- **R5-03 — 解消**: 両ラッパーはタイムアウト時にプロセスグループをTERM/KILLした後でpipeをdrainし、終了124と期限前からの生stderr全バイトをC12へ渡す。NUL・非UTF-8を含むstderrがAUD-09へ完全保存される回帰も追加された。
- **R5-04 — 解消**: `child_cli_failure()`は終了1のstrict UTF-8 stderrから末尾のCLI-05 JSON文書を抽出し、固定4キー・型と第6節の既知エラーコードを検査する。診断前置行付きの正しいエラーは伝播し、未知コード・不完全形・終了2は内部異常へ分類される。

## 独立検査の新規指摘

## R6-01 — major — `scripts/flow_control.py:1005`

1. **指摘内容**: CHK-03のkyoinban根拠が正規化文法データに存在するかを検査せず、さらにgp IDを部分文字列で照合するため、架空または別IDの原本根拠を正準受理する。`chk03_inventory_matches()`は`grammar_item_id`と`item["level"]`がインベントリとviolationの各evidenceに`in`で含まれることしか見ない。実際に`grammar_item_id: "gp:1"`へ`evidence: "教員版 gp:10、レベル B1.1"`を与えても`review_semantic_problems()`は問題0件を返した。また追加テスト自身も、`data/normalized/grammar.json`の501件に存在しない`gp:999`をkyoinban根拠として合格させている。この状態では、レビュアーが誤って作った架空の教員版項目・レベルでCHK-03 failを成立させ、INF-05へ送らず世代を消費できる。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RC-09・CHK-03・RR-02・RR-04・INF-05、`docs/cefrj-validation-spec.md` LVL-08・LVL-11、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 検証済み`data/normalized/grammar.json.entries`をID索引にし、kyoinbanインベントリの`grammar_item_id`が実在すること、直接付与または`inherited_from`先から導出した教員版`level_raw`が`item.level`と完全一致することを検査する。evidenceはgp IDを境界付きの完全トークンとして照合し、継承時は親IDと継承根拠も要求する。存在しない`gp:999`、`gp:1`対`gp:10`、実在IDだが別`level_raw`、継承元不一致をINF再実行へ送る負ケースを追加する。

## R6-02 — major — `.codex/run_reviewer.py:294` / `.claude/run_reviewer.py:200`

1. **指摘内容**: 子プロセス起動前のラッパー障害がC12を迂回し、INF再実行actionとAUD-09監査を作らない。Codexラッパーは既存`codex-last`作業パスの削除に失敗すると`fail(..., 70)`で直接終了し、両ラッパーともレビュアーコマンドがPATHにない場合も同様に直接終了する。隔離セットのreview待ち状態で`q01.gen1.codex-last.txt`位置へディレクトリを置いて固定ラッパーを実行すると、終了70になったが`review.invalid1.txt`は作られず、`flow-state.json`も残った。INF-01は固定ラッパー自身の非0終了もレビュー系インフラ障害とし、各失敗実行の監査と同一request再実行を要求しているため、標準フローはこの障害から規定どおり収束できない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` INF-01・INF-02・INF-04、`docs/cross-agent-compatibility.md` COR-08・CDX-07・CDX-08、`DECISIONS.md` M8D-10。

3. **具体的な修正案**: requestパスを安全に確定した後の起動前障害（コマンド欠落、専用CODEX_HOME不成立、Codex作業ファイル準備失敗、`Popen`失敗）を、終了70・空stdout・診断stderrの`ReviewerProcessResult`へ統一し、必ず`submit_to_flow_control()`へ1回渡す。C12のaction/CLIエラーだけをrelayする。両ラッパーのコマンド欠落と、Codexの起動前作業パスがdirectory・削除不能であるケースについて、invalid監査、retry番号、3回目中止、state寿命を固定検査する。

## R6-03 — major — `agent/author-core.md:619`

1. **指摘内容**: `run_review`のホスト手順に、現行のshell-free bridge導入前の直接C12送信手順が残り、同じ文書内の第7節およびCOR-08と矛盾する。第6節は固定ラッパーを実行して「得た生出力」をホストが`flow_control.py review --file -`へ渡し、ラッパー非0時もホストが`--process-failure`へ渡すよう指示する。一方、第7節と11.2.2節はラッパー自身が生バイト列をC12へ渡し、ホストはラッパーが返したaction/CLIエラーだけを取り込むよう指示する。前者に従うと、ラッパーが返したaction JSONをreview_resultとしてC12へ二重送信し、正常完了後のE-CONTRACT-03、または1回のレビュアー失敗に対するinvalid監査の二重加算を起こす。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-11a、`docs/subagent-review-spec.md` RG-00、`docs/cross-agent-compatibility.md` COR-08、`DECISIONS.md` M8D-10。

3. **具体的な修正案**: 第6節619〜625行の直接`flow_control.py review`呼出しとホスト再送指示を削除し、`run_review`ではホスト用固定ラッパーを1回実行して、そのstdoutのactionまたはstderrのCLIエラーだけを処理する一意な手順へ統一する。11.2.2節の旧「ホストが生bytesを取り込み、コードフェンスを再解析する」表現もC12内部の責務へ書き換え、コア指示書の標準ホスト経路に直接review呼出しが残らないことを静的テストで固定する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は215件全てpassした（564.76秒）。ただしR6-01〜R6-03の負経路・指示矛盾は未検査であるため、このpassだけではM8 DoD 1を満たさない。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/replay/*.py tests/unit/*.py`、`.venv/bin/python -m json.tool .claude/settings.json`、`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- `gp:1`を主張しながら`gp:10`だけを引用するCHK-03 kyoinban reviewが意味検証を問題0件で通ること、起動前`codex-last`位置のdirectoryによりラッパーが終了70となってもinvalid監査が0件・flow-state残置となることを隔離経路で再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。

## 要約

- blocker: 0件
- major: 3件
- minor: 0件
- **判定**: `$m-fix` で修正が必要
