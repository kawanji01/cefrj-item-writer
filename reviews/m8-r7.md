# M8 コードレビュー R7

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R6対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-21
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r6.md`
- **前回指摘の解消判定サマリー**: R6-02・R6-03は解消。R6-01は実在gp ID、正規化データの`level_raw`、継承元、境界付きgp IDの照合までは解消したが、evidence内のレベル引用を部分文字列で受理するため一部未解消（2件解消、1件一部未解消）。未解消部分はR7-02に記録する。
- **レビュー結果**: blocker 0件、major 3件、minor 1件を検出した。

## 前回指摘の個別解消判定

- **R6-01 — 一部解消・一部未解消**: `data/normalized/grammar.json`の実在ID、直接付与または継承元の`level_raw`、継承元IDと継承表記、gp IDの完全トークン境界を照合するようになり、架空ID・`gp:1`対`gp:10`・誤った`level`・継承根拠欠落はINF再実行へ送られる。一方、evidence中の`level_raw`は`level_raw in evidence`で照合するため、`B2.2`に対する`B2.20`のような別値を完全引用として受理する（R7-02）。
- **R6-02 — 解消**: requestパス確定後のコマンド欠落、専用`CODEX_HOME`不成立、Codex作業パス異常、`Popen`失敗は終了70のprocess failureとしてC12へ1回送られ、同一requestのinvalid監査と再実行actionを作る。両ラッパーの起動前障害とCodex作業パスdirectoryが3回で監査付き中止へ収束する回帰も追加された。
- **R6-03 — 解消**: 作問コアの標準`run_review`経路は固定ラッパーを1回実行してactionまたはCLIエラーだけを扱う形へ統一され、ホストによる直接`flow_control.py review`呼出し、生bytes再送、コードフェンス抽出は残っていない。静的回帰もこの一意性を検査する。

## 独立検査の新規指摘

## R7-01 — major — `scripts/flow_control.py:1079`

1. **指摘内容**: CHK-03の文法構造インベントリを空配列にしたreview_resultを正準受理し、全例文の文法構造列挙を丸ごと省略できる。`review_semantic_problems()`は`sentence_grammar_inventory`を反復するだけで、0件を問題にしない。スキーマも同配列に`minItems`を持たない。実際に英例文を持つ`replay_q01_pass.json`と`pass_q01_gen1.json`を渡すと意味検証は問題0件となった。また、18件ある`pass_q*.json`は全て空インベントリであり、これらを使うRPL正常系はCHK-03を実施していないreviewを合格入力としている。全9形式は完全な英例文を必ず持つため、「列挙対象となる構造が0件」はRR-02/RR-03を満たさない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-03・RR-02・RR-03・INF-05、`docs/question-generation-spec.md` GEN-05、`docs/cefrj-validation-spec.md` LVL-08・LVL-11・LS-01、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: C12の意味検証で全9形式の`sentence_grammar_inventory`を1件以上必須とし、空配列をINF再実行へ送る。18件のpass review fixtureを、candidate中の実在spanとRR-02の完全な根拠を持つインベントリへ更新し、index/generatorも同期する。空インベントリreviewが正準保存・世代消費されず、3回で監査付き中止になるCLI回帰と、RPL正常系の全reviewが非空である固定検査を追加する。

## R7-02 — major — `scripts/flow_control.py:954`

1. **指摘内容**: kyoinban根拠の照合がR6-01の要求した完全引用に達しておらず、さらにRR-02必須の構造名を正規化データへ照合しない。`kyoinban_evidence_matches()`はgp IDには境界を設けるが、教員版レベルは`level_raw not in evidence`という部分文字列判定である。また`review_semantic_problems()`は`structure`を一度も検査しない。`gp:84`（正規値`B2.2`）について、`structure: "教員版には存在しない偽の構造名"`、`evidence: "教員版 gp:84 の B2.20 を引用しました。"`としたreviewでも意味検証は問題0件を返した。この偽の原本引用はINF-05へ送られず、CHK-03 failの正準根拠として世代を消費できる。`reviewer_estimate`側も、CHK-03 violationに結び付かないインベントリ要素では推定レベルの引用と日本語の推定根拠を検査していない。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` RC-09・CHK-03・RR-02・RR-04・INF-05、`docs/cefrj-validation-spec.md` LVL-08・LS-01〜LS-05、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: evidenceからCEFR-J単一値・範囲値を境界付きトークンとして抽出し、正規化データの`level_raw`との完全一致を要求する。`structure`も直接付与・継承の別に正規化データ上の規範名へ完全一致させる。継承項目でどの名称を記録するかが既存文書から一意でない場合はPLN-05で先に確定する。`reviewer_estimate`は全インベントリ要素について推定レベルの完全引用と非空の日本語根拠を検査する。偽構造名、`B2.2`対`B2.20`、範囲値の前後付加、pass review中の根拠欠落を負ケースに追加する。

## R7-03 — major — `tests/unit/test_fixtures.py:72` / `tests/unit/test_flow_control.py:518`

1. **指摘内容**: CI-R-03のメタテストがテスト入力全体を検査せず、新設された`test_flow_control.py`内の有効な製品JSON手組みを常に見逃す。現在の走査対象は`tests/support.py`と`tests/replay/harness.py`の`canonical_bytes`/`write_json`直接引数、および`tests/generate_assets.py`の3種の固定キー集合だけである。`tests/unit/test_flow_control.py`は走査対象外で、たとえば518行目でCLI-05 error、989行目でset_check violation、1028行目で`W-CLEANUP-01`をdict literalから構築して子CLIの有効出力として使う。変数を介した`canonical_bytes(error)`も現行AST条件では検出できない。これはCI-SCH-03の必須欠落・型不正以外のJSON直書きを禁じるCI-R-03に反し、製品出力と期待値を同じテストコードから作れる状態を再導入している。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08・FIX-01〜FIX-05、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 有効なCLI-05、set_check report、`W-CLEANUP-01`を正準fixtureとしてindex登録するか、検査対象の実製品CLIが生成した直前段出力を使用する。CI-R-03メタテストは`tests/**/*.py`と資産生成器を対象にし、直接呼出しだけでなく変数代入を介した契約形dictも検出する。CI-SCH-03の許可例だけを明示的に限定し、現在の手組み3系統を検出する自己負ケースを追加する。

## R7-04 — minor — `tests/unit/test_cli.py:37`

1. **指摘内容**: 第2節のテストID `CI-R-01`が、`tests/unit/`・`tests/replay/`のどのテスト関数名・docstringにも存在しない。`test_ci_cli_01_argument_errors`は実質的に9本すべてをCLIプロセスで起動しているため機能的な網羅はあるが、docstringは`CI-CLI-01`だけであり、TST-08の追跡契約とM8 DoDの「全テストID実装」を満たさない。リポジトリ全体を検索しても`CI-R-01`は設計文書にしか存在しない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` TST-08・CI-R-01、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 9本の固定CLI目録をプロセス起動で検査するテストへ`CI-R-01`を関数名またはdocstringで明記する。第2・第3節の全テストID集合とテスト関数名/docstring中のID集合を比較し、テストIDの追加・削除時に追跡漏れを検出するメタテストも追加する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は228件全てpassした（587.60秒）。R6対応に直接関係する`author_core or prelaunch or kyoinban`の14件も全てpassした。ただしR7-01〜R7-04は既存テストの入力・メタ検査自体に含まれないため、このpassだけではM8 DoD 1を満たさない。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/unit/*.py tests/replay/*.py`と`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- 空の`sentence_grammar_inventory`を持つpass review、および`gp:84`を主張しながら偽構造名と`B2.20`だけを引用するkyoinban reviewが、どちらも`review_semantic_problems()`で問題0件となることを再現した。18件のpass review fixtureは全て空インベントリだった。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。

## 要約

- blocker: 0件
- major: 3件
- minor: 1件
- **判定**: `$m-fix` で修正が必要
