# M8 コードレビュー R8

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R7対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-22
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r7.md`
- **前回指摘の解消判定サマリー**: R7-02・R7-04は解消。R7-01は空インベントリ拒否と実在する単一spanの追加までは解消したが、全構造列挙とインベントリからの超過判定が未解消。R7-03は一部の製品契約をfixture・製品生成へ移したが、メタ検査が部分的な製品JSONを見逃し、実際の直書き入力が残るため未解消（2件解消、2件一部未解消）。未解消部分はR8-01・R8-02に記録する。
- **レビュー結果**: blocker 0件、major 2件、minor 1件を検出した。

## 前回指摘の個別解消判定

- **R7-01 — 一部解消・一部未解消**: `sentence_grammar_inventory`の空配列はINF再実行へ送られ、23件のpass review fixtureには正規化grammarへ解決できる要素が1件ずつ追加された。一方、生成器は常に単一要素だけを返し、CHK-03が要求する全文法構造の列挙になっていない。また、インベントリ自身がL_ctx超過を記録しているのにCHK-03・verdictをpassとしたreviewを意味検証が受理する（R8-01）。
- **R7-02 — 解消**: `kyoinban`の構造名、単一値・範囲値、直接付与・継承元IDを正規化grammarへ照合し、レベル値を境界付きで完全引用させている。`reviewer_estimate`も推定レベルの完全引用と日本語根拠を要求し、偽名称・部分ID・部分レベルの回帰テストを確認した。
- **R7-03 — 一部解消・一部未解消**: CLI-05、set_check report、cleanup warning、AUD-09、disputeの主要な手組みは製品生成または`tests/fixtures/cli/`・`machine/`へ移された。一方、CI-R-03メタ検査は5種類の完全キー集合だけを検出するため、部分的なmachine reportを含む製品JSON直書きを見逃し、実際にテスト入力として残している（R8-02）。
- **R7-04 — 解消**: 固定9 CLIのプロセス起動テストにCI-R-01が記載され、設計文書第2・第3節の必須テストID集合と実装側ID集合の差分も検査されている。別途、各テスト関数がIDを持つことまでは検査しておらず、TST-08の追跡漏れをR8-03に記録する。

## 独立検査の新規指摘

## R8-01 — major — `scripts/flow_control.py:1099` / `tests/generate_assets.py:203` / `tests/fixtures/reviews/pass_q01_gen1.json:103`

1. **指摘内容**: CHK-03インベントリを非空にするだけで、列挙済みの上限超過構造をreview自身が見逃した矛盾を正準受理でき、pass fixtureも全文法構造を列挙していない。`review_semantic_problems()`は1100行目で空配列だけを拒否し、L_ctxとの比較は1190行目以降の「レビュアーが既に申告したCHK-03 violation」に対してしか行わない。A2候補`replay_q01_pass.json`のインベントリを、正規化grammarと完全一致する`gp:84`・`B2.2`・構造名`受動態(助動詞+完了)`・evidence・候補中の完全span `I`へ置き換え、CHK-03=`pass`、verdict=`pass`、violations=`[]`のまま渡すと、意味検証は問題0件（`[]`）を返した。これは列挙結果から機械的に導出できる`B2.2 > L_ctx(A2.2)`を見逃し、上限超過候補を確定できる経路である。さらに`pass_grammar_inventory()`は候補ごとに最初の1構造だけを返す。例えば`I will accept your plan today.`には少なくとも`will accept`の`gp:141`に加え`your`の`gp:6`（正規化名`人称代名詞所有格(my/our/your/her/their)`）があるが、fixtureは`gp:141` 1件だけである。23件のpass review fixtureは同じ単一要素生成を使い、追加テストも18件が「非空」であることしか確認しないため、RPL正常系はRR-02の全列挙を満たさないレビューを合格入力としている。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-03手順1〜3・RR-02・RR-03・RR-05・INF-05、`docs/cefrj-validation-spec.md` LVL-08・LVL-11・LS-01〜LS-03・MAT-03、`docs/testing-and-acceptance.md` RPL-01〜RPL-10・FIX-06、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 全インベントリ要素の導入レベルをL_ctxと比較し、超過要素ごとにCHK-03 failと同一span・level_source・根拠を持つviolationが1件以上あることを意味検証で必須化する。超過要素があるのにCHK-03/verdictがpass、または対応violationが欠けるreviewはINF-05として3回の監査付き中止へ送る。固定候補のpass review fixtureは教員版粒度の全構造を列挙するよう再作成し、少なくともq01の`gp:6`と`gp:141`の双方、全23件の非空性と既知構造集合、上限超過インベントリをpassとする負ケースを検査する。

## R8-02 — major — `tests/unit/test_fixtures.py:34` / `tests/unit/test_flow_control.py:1094`

1. **指摘内容**: CI-R-03メタ検査が「テストコード内JSON不在」を検査できず、R7-03で禁止対象となった手組み入力が残っている。`contract_dict_violations()`はCLI-05完全形、6フィールドviolation、W-CLEANUP-01完全形、6フィールドdispute、process_failure完全形の5つのキー集合を全て含むdict literalだけを検出する。そのため、1094行目の`machine = {"violations": [{"code": ..., "location": ...}]}`は、`review_semantic_problems()`へ渡す製品machine report相当のテスト入力でありながら検出されない。実際に同じ部分machine dictを`contract_dict_violations(..., TEST_PRODUCT_CONTRACTS)`へ渡すと違反リストは空だった。完全なmachine reportでも`violations=[]`なら固定シグネチャに一致せず、review_resultなど他のトップレベル製品文書も検出対象ではない。自己負ケース3件が通っても、CI-R-03の限定と実際のテスト入力全体を保証しない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08・FIX-01〜FIX-06、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 1094行目の部分machine入力は、該当するmachine violationを製品`machine_check.py`から生成した直前段出力、またはindex登録済みの完全な正準fixtureへ置き換える。メタ検査は数個の完全キー集合ではなく、candidate・machine_report・review_request・review_result・set・CLI-05・AUD-09/11・FIN-01・flow actionのトップレベル形と、部分的な`violations`/`checks`/`machine_check_disputes`構築も対象にする。少なくとも今回の部分machine dict、violations空の完全machine report、完全review_resultを自己負ケースに追加し、CI-SCH-03で明示的に許可した必須欠落・型不正生成だけを狭く除外する。

## R8-03 — minor — `tests/unit/test_fixtures.py:184`

1. **指摘内容**: 追加されたTST-08メタ検査は必須IDの集合がどこか1関数に現れることしか確認せず、個々のテスト関数が対応IDを持つことを確認しない。各関数の抽出IDを空のまま集合へ加えても情報が失われるため、`tests/unit/test_flow_control.py`には関数名・docstringのどちらにも`CI-*`または`RPL-*`を持たないテスト関数が36件残る。例として87行目のガード検査、484行目の設定スナップショット検査、1008行目の意味契約再試行検査が該当する。これらは第1・第2層契約を分割して検査する実装だが、どのテストIDの一部か追跡できず、「1テストIDを複数関数に分割する場合は全関数に同一IDを記載する」というTST-08を満たさない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` TST-08、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 全`test_`関数について関数名またはdocstringから抽出した設計上のテストIDが1件以上あることをメタ検査で要求し、36件を対応する既存CI/RPL IDへ紐付ける。既存目録に対応先がない独立要件を正式な第1層テストとして保持する場合は、PLN-05でテストID追加を先に決定して`docs/testing-and-acceptance.md`へ反映する。

## 確認できた正常系・DoD再確認

- `.venv/bin/python -m pytest tests/unit tests/replay -q`は237件全てpassした（592.37秒）。ただしR8-01・R8-02の負ケースとR8-03の関数単位追跡は既存テストに含まれないため、このpassだけではM8 DoD 1を満たさない。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/unit/*.py tests/replay/*.py tests/generate_assets.py`と`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- A2候補へ完全な`gp:84`・`B2.2`根拠を持つインベントリを置きながらCHK-03/verdictをpassとしたreviewが、`review_semantic_problems()`で問題0件になることを再現した。
- 部分machine reportのJSON直書きをCI-R-03メタ検査へ渡しても違反0件になること、およびテストIDを持たないテスト関数が36件あることをAST走査で再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。

## 要約

- blocker: 0件
- major: 2件
- minor: 1件
- **判定**: `$m-fix` で修正が必要
