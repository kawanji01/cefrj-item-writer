# M8 コードレビュー R9

- **対象差分**: `b3c61b13d6e5b1a768831e4a93339f5d26f91702`（M7確定コミット）から現在の作業ツリーまで。レビュー時の `HEAD` は `c7d35ac66edbc6789e9629fc6526d0aadb6e62d1`（M8初回実装コミット、annotated tag `v1.0.0`）で、R1〜R8対応の未コミット差分と未追跡ファイルを含む。
- **レビュー日**: 2026-08-22
- **仕様の正**: `docs/`、`schemas/`
- **M8完了条件**: `IMPLEMENTATION_PLAN.md` M8 DoD
- **前回レビュー**: `reviews/m8-r8.md`
- **前回指摘の解消判定サマリー**: R8-03は解消。R8-01はインベントリからの上限超過導出と要素別violation照合は解消したが、固定fixtureが教員版に項目のない構造を列挙できず「全文法構造」を満たさないため一部未解消。R8-02は部分・完全なdict literalの検出範囲を拡張したが、同じ製品JSONを`dict(...)`で構築するとメタ検査を通過するため一部未解消（1件解消、2件一部未解消）。未解消部分はR9-01・R9-02に記録する。
- **レビュー結果**: blocker 0件、major 3件、minor 1件を検出した。

## 前回指摘の個別解消判定

- **R8-01 — 一部解消・一部未解消**: `review_semantic_problems()`はインベントリ各要素の導入レベルをL_ctxへ比較し、超過要素ごとにCHK-03 failと対応violationを要求するようになった。複数超過要素の負ケースも追加された。一方、fixture生成器は`PASS_GRAMMAR_SPANS`の全要素を`kyoinban`としてしか生成できず、CHK-03手順1〜2が明記する「教員版に項目のない構造」を列挙できない。実際にq02の現在時制`has`がpass/fail reviewの双方から欠落している（R9-01）。
- **R8-02 — 一部解消・一部未解消**: 部分machine reportの実入力は製品出力・完全fixtureへ移され、ASTメタ検査はcandidate・machine・review・set・CLI-05・監査・確定・flow actionと部分配列要素のdict literalを検出する。一方、検査対象が`ast.Dict`だけなので、等価な`dict(...)`直書きを検出しない（R9-02）。
- **R8-03 — 解消**: 全`tests/unit`・`tests/replay`の`test_`関数について、関数名またはdocstringに既存CI/RPL IDがあることを関数単位で検査し、必須ID集合の欠落と未定義IDも拒否している。

## 独立検査の新規指摘

## R9-01 — major — `tests/generate_assets.py:271` / `tests/unit/test_fixtures.py:296` / `tests/fixtures/reviews/pass_q02_gen1.json:103`

1. **指摘内容**: R8-01対応後も、固定review fixtureのCHK-03インベントリが全文法構造を列挙していない。`PASS_GRAMMAR_SPANS`は`(span, gp ID)`だけを持ち、`pass_grammar_inventory()`は全要素を正規化grammarの`kyoinban`項目として生成するため、教員版256項目に該当しない構造を`reviewer_estimate`で記録できない。具体例としてq02の英文`She has the ability to help us.`には主動詞`has`の3人称単数現在時制があるが、教員版の一般動詞3人称単数`gp:60`は明示的にbe/do/haveを対象外としており、CHK-03手順2に従えば教員版非該当構造として`reviewer_estimate`で列挙する必要がある。現在の`pass_q02_gen1.json`は`us`・`the`・`to help`・S+V+Oの4件だけで、`has`を含まない。同じ生成元を使うq02のpass 3件・fail 3件と、`He has the ability to speak three languages.`を使う資産も同じ欠落を持つ。追加されたCI-FIX-01検査の`known["q02"]`も同じ4件を期待しているため、生成器と期待値が同じ欠落を共有したまま全件passする。

2. **根拠となる設計文書の規則ID**: `docs/subagent-review-spec.md` CHK-03手順1〜3・RR-02・RR-03、`docs/cefrj-validation-spec.md` LS-01〜LS-03・LVL-08・MAT-03、`docs/testing-and-acceptance.md` FIX-06・RPL-01〜RPL-10、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 固定文ごとの目録定義を`kyoinban`専用のgp ID列から、`kyoinban`と`reviewer_estimate`の両方を表現できる完全なinventory item定義へ変更する。q02の`has`を含め、全固定英文について教員版非該当構造も9段階の推定導入レベルと日本語根拠付きで再監査し、pass/fail review fixtureを再生成する。メタ検査はpass 23件だけでなくfail reviewも対象にし、ID/spanのfrozensetだけでなく`structure`・`level_source`・`level`・`evidence`を含む完全な既知集合と、製品`review_semantic_problems(..., grammar_index=...)`への適合を検査する。

## R9-02 — major — `tests/unit/test_fixtures.py:103`

1. **指摘内容**: CI-R-03メタ検査はPythonのdict literalだけを走査するため、同じ製品JSONを`dict(...)`で直書きすると検出を完全に迂回できる。`contract_dict_violations()`は110〜112行目で`ast.Dict`以外を無条件に除外する。実際に`machine = dict(violations=[dict(code='V-LEX-02', location='x')])`を`TEST_PRODUCT_CONTRACTS`へ渡すと違反リストは`[]`になった。この形はR8-02が禁止対象とした部分machine reportと意味的に同一であり、テストコード内JSON不在を検査する必須メタテストとしては構文表現に依存しすぎている。自己負ケースも波括弧literalだけなのでこの穴を検出しない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` CI-R-03・TST-08、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: `ast.Dict`に加えて、組込み`dict`のkeyword構築、辞書へのsubscript代入・`update()`、辞書unpack/mergeで製品契約キーが組み立てられる経路を検出する。少なくとも部分machine reportと完全review_resultについて、`dict(...)`、段階的代入、dict mergeの自己負ケースを追加する。静的な局所キー集合では十分な精度を保てない場合は、製品入力を取得できるAPIをfixture loader・製品生成関数へ限定し、その利用箇所をASTでallowlist検査する方式へ置き換える。

## R9-03 — major — `tests/unit/test_flow_control.py:243`

1. **指摘内容**: マージ条件の全テストが安定して通過しない。`.venv/bin/python -m pytest tests/unit tests/replay -q`の独立実行で、Claude側の`test_m8_reviewer_wrapper_timeout_preserves_raw_stderr`が`stderr_base64 == ""`となり、238 pass / 1 failだった。同じテストだけを直後に再実行するとpassした。試験は`review_timeout_seconds=1`を設定し、別Pythonインタプリタの偽`claude`が起動してstderrを書き出すまでが1秒以内であることを暗黙の合否条件にしている。コールドスタートやホスト負荷で子がstderrを書き出す前に壁時計期限へ到達すると、製品ラッパーは実際に得た空stderrを正しく監査する一方、試験だけが失敗する。したがって現在の239件passという結果は実行ごとに再現できず、M8 DoD 1とマージ条件を満たさない。

2. **根拠となる設計文書の規則ID**: `docs/testing-and-acceptance.md` TST-05・RPL-05、`IMPLEMENTATION_PLAN.md` M8 DoD 1。

3. **具体的な修正案**: 偽実行ファイルを低オーバーヘッドで確実にstderrへ書ける実装にし、子起動猶予を十分に取ったテスト用timeoutへ変更するか、stderr出力完了を同期した既起動プロセスに対して停止・pipe drain部分を検査する決定的テストへ分離する。期限超過そのもののCLI統合試験と、生stderr drainの単体試験を分けてもよい。修正後は単独試験だけでなく、マージ条件と同一の全コマンドを複数回実行して安定通過を確認する。

## R9-04 — minor — `.gitignore:1` / `.agents/skills/cefrj-author/SKILL.md:1`

1. **指摘内容**: 対象作業ツリーにM8成果物として整理されていないトップレベル追加物が残っている。未追跡の`.tmp-slide-grill.YjawYm/`（約43 MiB）と`.tmp-slide-rebuild.h9zDOp/`（約206 MiB）はスライドのPDF・PPTX・大量のpreview/layoutを含む一時作業ディレクトリだが、`.gitignore`の対象ではない。また`.agents/skills/cefrj-author/SKILL.md`は新しいトップレベル配線要素であるにもかかわらず、`DECISIONS.md`に追加決定がなく、設計文書・CHANGELOGからも参照されない。レビュー対象を「現在の作業ツリーまで」としている以上、この状態ではM8確定コミットへ誤って混入する余地があり、ARC-13の追加手続も満たさない。

2. **根拠となる設計文書の規則ID**: `docs/architecture.md` ARC-13、`IMPLEMENTATION_PLAN.md` M8成果物一覧・M8 DoD 4。

3. **具体的な修正案**: スライド一時ディレクトリをM8作業ツリーから除外し、再生成され得るなら限定的な`.gitignore`規則を追加する。`.agents/`を製品配線として追跡する意図がある場合はPLN-05/ARC-13に従って決定記録と影響先を先に確定し、意図がなければM8コミット対象から除外する。コミット前に追跡予定ファイル一覧を確認し、M8成果物以外が含まれないことを検査する。

## 確認できた正常系・DoD再確認

- R8-01の意味検証について、インベントリ自身からL_ctx超過を導出し、CHK-03 failと対応violationを要求する実装、および複数超過要素の負ケースを確認した。
- R8-03について、全unit/replayテスト関数の関数名・docstringに既存CI/RPL IDがあり、必須ID集合の欠落と未定義IDを拒否するメタ検査を確認した。
- `.venv/bin/python -m pytest tests/unit tests/replay -q`は238件pass・1件fail（588.12秒）。失敗したClaude timeout試験の単独再実行は1件pass（6.64秒）で、R9-03の非決定性を確認した。
- ゴールデン候補・machine互換の対象2件は2件passした。
- `.venv/bin/python scripts/doctor.py`は12 pass / 0 failだった。
- `.venv/bin/python scripts/build_normalized.py --diff`は語彙7,988件・文法501件、追加・削除・レベル変更0件、`written=[]`だった。
- `.venv/bin/python -m py_compile scripts/*.py .claude/*.py .claude/hooks/*.py .codex/*.py tests/unit/*.py tests/replay/*.py tests/generate_assets.py`と`git diff --check b3c61b13d6e5b1a768831e4a93339f5d26f91702`はpassした。
- `dict(...)`で部分machine reportを構築した自己負入力がCI-R-03メタ検査で違反0件になることを再現した。
- M8D-11に従うA-01〜A-15再実施、`tests/acceptance/records/v1.0.1.md`、最終CHANGELOG、annotated tag `v1.0.1`、更新手順確認はコードレビュー収束後のリリースゲートとして未実施であり、現時点では合格・リリース完了を主張できない。

## 要約

- blocker: 0件
- major: 3件
- minor: 1件
- **判定**: `$m-fix` で修正が必要
